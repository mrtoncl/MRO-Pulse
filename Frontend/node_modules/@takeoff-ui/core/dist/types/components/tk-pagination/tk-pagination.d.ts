import { ComponentInterface, EventEmitter } from '../../stencil-public-runtime';
/**
 * TkPagination component description.
 * @react `import { TkPagination } from '@takeoff-ui/react'`
 * @vue `import { TkPagination } from '@takeoff-ui/vue'`
 * @angular `import { TkPagination } from '@takeoff-ui/angular'`
 */
export declare class TkPagination implements ComponentInterface {
    private ellipsis;
    inputValue: string;
    internalCurrentPage: number;
    internalCurrentPageChanged(newValue: number): void;
    /**
     * Whether the pagination elements should have rounded corners
     * @defaultValue false
     */
    rounded: boolean;
    /**
     * Number of items per page.
     * @defaultValue 10
     */
    rowsPerPage: number;
    /**
     * Number of total items.
     * @defaultValue 0
     */
    totalItems: number;
    /**
     * Number of items per page options
     * @defaultValue [5, 10, 20, 50]
     */
    rowsPerPageOptions: number[];
    /**
     * The type of the pagination
     * @defaultValue 'outlined'
     */
    type: 'outlined' | 'text' | 'grouped';
    /**
     * The mode of the pagination
     * @defaultValue 'outlined'
     */
    mode: 'compact' | 'compact-expanded';
    /**
     * Template string for current page report in pagination.
     * Available placeholders: {currentPage}, {totalPages}
     * @defaultValue 'page: {currentPage} of {totalPages}'
     */
    pageReportTemplate: string;
    /**
     * Template string for items report in pagination.
     * Available placeholders: {startItem}, {endItem}, {totalItems}
     * @defaultValue 'item: {startItem}-{endItem} of {totalItems}'
     */
    itemsReportTemplate: string;
    /**
     * The current page of the pagination.
     * @defaultValue 1
     */
    currentPage: number;
    currentPageChanged(newValue: number): void;
    /**
     * Pagination next button click event
     */
    tkNextPage: EventEmitter<{
        page: number;
    }>;
    /**
     * Pagination page change event
     */
    tkPageChange: EventEmitter<{
        page: number;
        totalPages: number;
        startItem: number;
        endItem: number;
    }>;
    /**
     * Pagination prev button click event
     */
    tkPrevPage: EventEmitter<{
        page: number;
    }>;
    /**
     * RowsPerPage change event
     */
    tkRowsPerPageChange: EventEmitter<number>;
    componentWillLoad(): void;
    private updateInputValue;
    private getTotalPages;
    private getPageNumbers;
    private generatePageNumbersForMediumRange;
    private generatePageNumbersForLargeRange;
    private validateAndUpdatePage;
    private emitPageChangeEvents;
    private handlePageClick;
    private handlePrevClick;
    private handleNextClick;
    private handlePageInputChange;
    private createPageNumbers;
    private renderTag;
    private renderContent;
    private renderSelect;
    private renderInput;
    render(): any;
}
