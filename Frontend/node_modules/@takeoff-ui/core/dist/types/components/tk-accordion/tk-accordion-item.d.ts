import { ComponentInterface, type EventEmitter } from '../../stencil-public-runtime';
import { IIconOptions, IMultiIconOptions } from '../../global/interfaces/IIconOptions';
/**
 * @slot header - Custom header template that overrides the header prop if provided.
 * @slot content - Custom content template.
 */
export declare class TkAccordionItem implements ComponentInterface {
    private parentEl;
    el: HTMLTkAccordionItemElement;
    type: 'grouped' | 'divided';
    arrowPosition: 'left' | 'right';
    expandIcon: string | IIconOptions;
    collapseIcon: string | IIconOptions;
    hideArrows: boolean;
    hasHeaderSlot: boolean;
    mode: 'default' | 'compact';
    /**
     * Sets if the accordion is active.
     * @defaultValue false
     */
    active: boolean;
    activeChanged(newValue: boolean, oldValue: boolean): void;
    /**
     * Optional key for the accordion item.
     */
    itemKey?: string | number;
    /**
     * Header text to display.
     */
    header?: string;
    /**
     * Sets size for the component.
     * @defaultValue 'base'
     */
    size: 'base' | 'large';
    /**
     * Icon for accordion component.
     */
    icon?: string | IIconOptions | IMultiIconOptions;
    /**
     * Sets the data-testid attribute on the root container element.
     */
    dataTestid?: string;
    /**
     * Emitted when an active index is changed
     */
    tkActiveChange: EventEmitter<boolean>;
    componentWillLoad(): void;
    private renderCollapseIcon;
    private createHeader;
    render(): any;
}
