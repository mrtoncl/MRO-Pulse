import { ComponentInterface, EventEmitter } from '../../stencil-public-runtime';
import { IIconOptions, IMultiIconOptions } from '../../global/interfaces/IIconOptions';
import { CSSStyleProperties } from '../../global/types';
/**
 * TkButton is an extension to standard input element with icons and theming.
 * @react `import { TkButton } from '@takeoff-ui/react'`
 * @vue `import { TkButton } from '@takeoff-ui/vue'`
 * @angular `import { TkButton } from '@takeoff-ui/angular'`
 */
export declare class TkButton implements ComponentInterface {
    el: HTMLTkButtonElement;
    /**
     * Disables the button, preventing user interaction.
     */
    disabled: boolean;
    /**
     * Sets the button to full-width mode, making it span the container.
     */
    fullWidth: boolean;
    /**
     * This field specifies the design type of the component.
     * @defaultValue filled
     */
    type: 'filled' | 'filledLight' | 'outlined' | 'text';
    /**
     * The style attribute of container element
     */
    containerStyle?: CSSStyleProperties;
    /**
     * Applies the animated button style, adapting to the selected variant.
     * Only the `filled` and `outlined` types are animated.
     */
    animated: boolean;
    /**
     * Specifies a material icon name to be displayed.
     */
    icon?: string | IIconOptions | IMultiIconOptions;
    /**
     * Defines the position of the icon.
     * @defaultValue 'left'
     */
    iconPosition: 'left' | 'right';
    /**
     * Sets the URL the button should navigate to when clicked (for type="link" buttons).
     */
    href: string;
    /**
     * Specifies where to open the linked document (for type="link" buttons).
     */
    target: string;
    /**
     * Applies underline styling to the button label regardless of mode.
     */
    underline: boolean;
    /**
     * Label text displayed inside the button.
     */
    label: string;
    /**
     * Shows a loading icon inside the button.
     */
    loading: boolean;
    /**
     * Makes the button round with an icon-only style.
     */
    rounded: boolean;
    /**
     * Sets size for the component.
     * @defaultValue 'base'
     */
    size: 'large' | 'base' | 'small';
    /**
     * Sets the button type.
     * @defaultValue 'button'
     */
    mode: 'button' | 'submit' | 'reset' | 'link';
    /**
     * Determines the button's variant for different styles.
     */
    variant: 'primary' | 'secondary' | 'neutral' | 'info' | 'success' | 'danger' | 'warning' | 'purple' | 'white' | 'black';
    /**
     * Sets the data-testid attribute on the root container element.
     */
    dataTestid?: string;
    /**
     * Emitted when the button click.
     */
    tkClick: EventEmitter<MouseEvent>;
    componentWillLoad(): void;
    private handleClick;
    /**
     * The animated style is only applied to the `filled` and `outlined` types.
     */
    private get isAnimated();
    private getButtonIconColor;
    render(): any;
}
