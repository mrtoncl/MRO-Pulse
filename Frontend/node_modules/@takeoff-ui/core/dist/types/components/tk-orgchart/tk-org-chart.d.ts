import { EventEmitter, ComponentInterface } from '../../stencil-public-runtime';
/**
 * The TkOrgChart component allows users to visualize organizational data using d3-org-chart.
 * @react `import { TkOrgChart } from '@takeoff-ui/react'`
 * @vue `import { TkOrgChart } from '@takeoff-ui/vue'`
 * @angular `import { TkOrgChart } from '@takeoff-ui/angular'`
 */
export declare class TkOrgChart implements ComponentInterface {
    private orgChartContainerRef?;
    private orgChartInstance?;
    private buttonUpdateTimeout?;
    private nodeWidth;
    private nodeHeight;
    private nodeButtonWidth;
    private nodeButtonHeight;
    private nodeButtonX;
    private childrenMargin;
    private compact;
    el: HTMLElement;
    internalOptions: any;
    /**
     * Chart data should be an array of node objects with at least id, parentId (optional for root), and name properties.
     */
    data: any[];
    dataChanged(): void;
    /**
     * Chart options for d3-org-chart customization
     */
    options?: any;
    optionsChanged(): void;
    /**
     * Enable or disable expand/collapse buttons functionality
     * When disabled, all nodes will be automatically expanded
     */
    collapsible?: boolean;
    collapsibleChanged(): void;
    /**
     * Accessibility label for the chart
     */
    accessibilityLabel?: string;
    /**
     * Node click event
     */
    tkNodeClick: EventEmitter<any>;
    componentWillLoad(): void;
    componentDidLoad(): void;
    disconnectedCallback(): void;
    /**
     * Get the chart instance
     */
    getOrgChart(): Promise<any>;
    /**
     * Refresh the chart
     */
    refresh(): Promise<void>;
    /**
     * Add node to organizational chart
     */
    addNode(node: any): Promise<void>;
    /**
     * Fit chart to screen
     */
    fit(): Promise<void>;
    private needsNormalising;
    private flattenTree;
    private normaliseData;
    private mergeOptions;
    private updateOrgChart;
    private defaultNodeHTML;
    private defaultButtonHTML;
    private updateButtonsState;
    render(): any;
}
