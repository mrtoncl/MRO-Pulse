import { IGanttColumn, GanttWeekStartDay } from './types';
/** Default row height in pixels. */
export declare const DEFAULT_ROW_HEIGHT = 40;
/** Default width of the left-side panel in pixels. */
export declare const DEFAULT_PANEL_WIDTH = 320;
/** Default header row height in pixels (each of the two header layers). */
export declare const DEFAULT_HEADER_HEIGHT = 36;
/** Default week start day (1 = Monday). */
export declare const DEFAULT_WEEK_START_DAY: GanttWeekStartDay;
/** Base unit width in pixels per day for each view type. */
export declare const VIEW_TYPE_DAY_WIDTH: Record<string, number>;
/** Default columns shown in the left panel. */
export declare const DEFAULT_COLUMNS: IGanttColumn[];
