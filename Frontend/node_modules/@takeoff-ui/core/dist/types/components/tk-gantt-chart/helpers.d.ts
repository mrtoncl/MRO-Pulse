import { IGanttTask, IGanttFlatRow, IGanttHeaderCell, GanttViewType, GanttSecondaryHeaderMode, GanttWeekStartDay } from './types';
/** Strip time portion and return a pure-date Date at midnight UTC. */
export declare function toDateOnly(input: string | Date): Date;
/** Number of calendar days between two dates (inclusive of start, exclusive of end). */
export declare function daysBetween(a: Date, b: Date): number;
/** Add N days to a date (immutable). */
export declare function addDays(date: Date, days: number): Date;
/** Is the given date a Saturday (6) or Sunday (0)? */
export declare function isWeekend(date: Date): boolean;
/** Get the ISO week number for a given date. */
export declare function getWeekNumber(date: Date): number;
/** Get the start of the week containing `date` based on the configured start day. */
export declare function getWeekStart(date: Date, weekStartDay: GanttWeekStartDay): Date;
/** Get the quarter (1-4) for a date. */
export declare function getQuarter(date: Date): number;
/** Compute the overall min/max date range across all tasks (recursively). */
export declare function getDateRange(tasks: IGanttTask[]): {
    min: Date;
    max: Date;
};
/** Determine the best view type based on the task date range span. */
export declare function autoFitViewType(tasks: IGanttTask[]): GanttViewType;
/** Flatten the task tree into rows, respecting expanded state. */
export declare function flattenTasks(tasks: IGanttTask[], expandedIds: Set<string>, depth?: number): IGanttFlatRow[];
/** Returns left offset (px) and width (px) for a task bar relative to minDate. */
export declare function getTaskBarPosition(task: IGanttTask, minDate: Date, viewType: GanttViewType): {
    left: number;
    width: number;
};
/** Returns the pixel left offset and width for a date range relative to the timeline start. */
export declare function getBarPositionFromDates(startDate: string, endDate: string, minDate: Date, viewType: GanttViewType): {
    left: number;
    width: number;
};
/** Returns the pixel left offset for an indicator date. */
export declare function getIndicatorPosition(dateStr: string, minDate: Date, viewType: GanttViewType): number;
/** Compute padded timeline start/end based on view type. */
export declare function getTimelineRange(tasks: IGanttTask[], viewType: GanttViewType, weekStartDay: GanttWeekStartDay): {
    start: Date;
    end: Date;
    totalDays: number;
};
/** Generate primary (upper) header cells. */
export declare function generatePrimaryHeaders(start: Date, end: Date, viewType: GanttViewType, locale: string, _weekStartDay: GanttWeekStartDay): IGanttHeaderCell[];
/** Generate secondary (lower) header cells. */
export declare function generateSecondaryHeaders(start: Date, end: Date, viewType: GanttViewType, locale: string, weekStartDay: GanttWeekStartDay, secondaryMode?: GanttSecondaryHeaderMode): IGanttHeaderCell[];
/** Check whether a date is in the holidays list. */
export declare function isHoliday(date: Date, holidays: {
    date: string;
    label?: string;
}[]): boolean;
/** Find the holiday entry for a specific date, if any. */
export declare function findHoliday(date: Date, holidays: {
    date: string;
    label?: string;
}[]): {
    date: string;
    label?: string;
} | undefined;
