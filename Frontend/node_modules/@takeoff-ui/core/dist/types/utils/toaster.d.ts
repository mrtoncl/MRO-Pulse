import { IAlertActionButton } from '../components/tk-alert/types';
import { CSSStyleProperties } from '../global/types';
export interface IToast {
    position?: string;
    header?: string;
    message?: string;
    alignItems?: 'start' | 'center' | 'end';
    actions?: IAlertActionButton[];
    variant: 'success' | 'warning' | 'info' | 'danger' | 'neutral';
    icon?: string;
    iconSize?: 'small' | 'base' | 'large';
    type: 'filled' | 'filledlight' | 'outlined' | 'gradient';
    removable?: boolean;
    timeout?: number;
    persistentId?: string;
    persistent?: boolean;
    containerStyle?: CSSStyleProperties;
}
export declare const createToast: (options: IToast) => void;
export declare const showPersistentToast: (options: IToast & {
    persistentId: string;
}) => boolean;
export declare const isPersistentToastVisible: (persistentId: string) => boolean;
export declare const dismissAllPersistentToasts: () => void;
