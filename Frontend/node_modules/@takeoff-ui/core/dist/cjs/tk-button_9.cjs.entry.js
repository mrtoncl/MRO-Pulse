'use strict';

var index = require('./index-DrXiaPrw.js');
var index$1 = require('./index-DmcCMEYj.js');
var iconUtils = require('./icon-utils-rHaJpUOe.js');
var testIdUtils = require('./test-id-utils-CAIe6ggJ.js');
var v4 = require('./v4-_2BfiRUa.js');
var clickoutsideMixin = require('./clickoutside-mixin-BGIuHTh6.js');
var positionUtils = require('./position-utils-DTbIFs63.js');
var objectUtils = require('./object-utils-BBYkjAJt.js');
var hintUtils = require('./hint-utils-D3UOnOkU.js');
var isEqual = require('./isEqual-BbHXdG2o.js');
var _Uint8Array = require('./_Uint8Array-C9PPQ5_G.js');
require('./_baseFor-BrHdJpyG.js');

const tkButtonCss = () => `@font-face{font-family:"Material Symbols Outlined";src:url("./assets/fonts/material-symbols/Material-Symbols-Outlined.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-outlined{font-family:"Material Symbols Outlined";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-outlined{font-variation-settings:"FILL" 0}.material-symbols-outlined.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Rounded";src:url("./assets/fonts/material-symbols/Material-Symbols-Rounded.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-rounded{font-family:"Material Symbols Rounded";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-rounded{font-variation-settings:"FILL" 0}.material-symbols-rounded.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Sharp";src:url("./assets/fonts/material-symbols/Material-Symbols-Sharp.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-sharp{font-family:"Material Symbols Sharp";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-sharp{font-variation-settings:"FILL" 0}.material-symbols-sharp.fill{font-variation-settings:"FILL" 1}:host{display:inline-block}:host(.full-width){width:100%}:host(.full-width) .tk-button{width:100%}.tk-button{font-family:var(--desktop-body-m-base-font);font-weight:400;transition:all 200ms cubic-bezier(0.17, 0.67, 0.83, 0.67);display:inline-flex;align-items:center;justify-content:center;outline:none;overflow:hidden;height:100%;position:relative}.tk-button.underline span{text-decoration:underline}.tk-button:disabled,.tk-button[disabled]{cursor:not-allowed;pointer-events:none}.tk-button.primary{border:0px;color:var(--static-white)}.tk-button.primary.filled{background-color:var(--primary-base)}.tk-button.primary.filled:hover{background-color:var(--primary-darkest);cursor:pointer}.tk-button.primary.filled:focus-visible{box-shadow:var(--primary-focus)}.tk-button.primary.filledLight{color:var(--primary-500);background-color:var(--primary-lightest)}.tk-button.primary.filledLight:hover{background-color:var(--primary-light);cursor:pointer}.tk-button.primary.filledLight:focus-visible{box-shadow:var(--primary-focus)}.tk-button.primary.outlined{background-color:transparent;color:var(--primary-base);border:1px solid var(--primary-sub-base)}.tk-button.primary.outlined:hover{background-color:var(--primary-lightest);cursor:pointer}.tk-button.primary.outlined:focus-visible{box-shadow:var(--primary-focus)}.tk-button.primary.outlined:disabled,.tk-button.primary.outlined[disabled]{color:var(--text-sub-base);background-color:transparent;border-color:var(--border-sub-base)}.tk-button.primary.text{background-color:transparent;color:var(--primary-base);box-shadow:none}.tk-button.primary.text:hover{background-color:var(--primary-lightest);cursor:pointer}.tk-button.primary.text:focus-visible{box-shadow:var(--primary-focus)}.tk-button.primary.text:disabled,.tk-button.primary.text[disabled]{background-color:transparent;color:var(--text-sub-base)}.tk-button.primary.link{color:var(--primary-base)}.tk-button.primary.link:hover{color:var(--primary-dark);cursor:pointer}.tk-button.primary:disabled,.tk-button.primary[disabled]{color:var(--static-white);background-color:var(--background-sub-base)}.tk-button.secondary{border:0px;color:var(--text-lightest);background-color:var(--secondary-dark)}.tk-button.secondary.filled:hover{background-color:var(--secondary-darkest);cursor:pointer}.tk-button.secondary.filled:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.secondary.filledLight{color:var(--secondary-darkest);background-color:var(--secondary-lightest)}.tk-button.secondary.filledLight:hover{background-color:var(--secondary-light);cursor:pointer}.tk-button.secondary.filledLight:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.secondary.outlined{background-color:transparent;color:var(--secondary-darkest);border:1px solid var(--secondary-sub-base)}.tk-button.secondary.outlined:hover{background-color:var(--secondary-lightest);cursor:pointer}.tk-button.secondary.outlined:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.secondary.outlined:disabled,.tk-button.secondary.outlined[disabled]{color:var(--text-sub-base);background-color:transparent;border-color:var(--border-sub-base)}.tk-button.secondary.text{background-color:transparent;color:var(--secondary-darkest);box-shadow:none}.tk-button.secondary.text:hover{background-color:var(--secondary-lightest);cursor:pointer}.tk-button.secondary.text:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.secondary.text:disabled,.tk-button.secondary.text[disabled]{background-color:transparent;color:var(--text-sub-base)}.tk-button.secondary.link{color:var(--secondary-dark)}.tk-button.secondary.link:hover{color:var(--secondary-darkest);cursor:pointer}.tk-button.secondary:disabled,.tk-button.secondary[disabled]{color:var(--text-lightest);background-color:var(--background-sub-base)}.tk-button.neutral{border:0px;color:var(--text-darkest)}.tk-button.neutral.filled{color:var(--text-lightest);background-color:var(--background-base)}.tk-button.neutral.filled:hover{background-color:var(--background-darkest);cursor:pointer}.tk-button.neutral.filled:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.neutral.filledLight{background-color:var(--background-lightest)}.tk-button.neutral.filledLight:hover{background-color:var(--background-light);cursor:pointer}.tk-button.neutral.filledLight:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.neutral.outlined{background-color:transparent;border:1px solid var(--background-sub-base)}.tk-button.neutral.outlined:hover{background-color:var(--background-lightest);cursor:pointer}.tk-button.neutral.outlined:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.neutral.outlined:disabled,.tk-button.neutral.outlined[disabled]{color:var(--text-sub-base);background-color:transparent;border-color:var(--border-sub-base)}.tk-button.neutral.text{background-color:transparent;box-shadow:none}.tk-button.neutral.text:hover{background-color:var(--background-lightest);cursor:pointer}.tk-button.neutral.text:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.neutral.text:disabled,.tk-button.neutral.text[disabled]{background-color:transparent;color:var(--text-sub-base)}.tk-button.neutral.link{color:var(--text-dark)}.tk-button.neutral.link:hover{color:var(--text-darkest);cursor:pointer}.tk-button.neutral:disabled,.tk-button.neutral[disabled]{color:var(--static-white);background-color:var(--background-sub-base)}.tk-button.info{border:0px;color:var(--text-lightest)}.tk-button.info.filled{background-color:var(--states-info-dark)}.tk-button.info.filled:hover{background-color:var(--states-info-darkest);cursor:pointer}.tk-button.info.filled:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.info.filledLight{color:var(--states-info-dark);background-color:var(--states-info-lightest)}.tk-button.info.filledLight:hover{background-color:var(--states-info-light);cursor:pointer}.tk-button.info.filledLight:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.info.outlined{background-color:transparent;color:var(--states-info-dark);border:1px solid var(--states-info-sub-base)}.tk-button.info.outlined:hover{background-color:var(--states-info-lightest);cursor:pointer}.tk-button.info.outlined:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.info.outlined:disabled,.tk-button.info.outlined[disabled]{color:var(--text-sub-base);background-color:transparent;border-color:var(--border-sub-base)}.tk-button.info.text{background-color:transparent;color:var(--states-info-dark);box-shadow:none}.tk-button.info.text:hover{background-color:var(--states-info-lightest);cursor:pointer}.tk-button.info.text:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.info.text:disabled,.tk-button.info.text[disabled]{background-color:transparent;color:var(--text-sub-base)}.tk-button.info.link{color:var(--states-info-dark)}.tk-button.info.link:hover{color:var(--states-info-darkest);cursor:pointer}.tk-button.info:disabled,.tk-button.info[disabled]{color:var(--text-lightest);background-color:var(--background-sub-base)}.tk-button.success{border:0px;color:var(--text-lightest);background-color:var(--states-success-dark)}.tk-button.success.filled:hover{background-color:var(--states-success-darkest);cursor:pointer}.tk-button.success.filled:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.success.filledLight{color:var(--states-success-dark);background-color:var(--states-success-lightest)}.tk-button.success.filledLight:hover{background-color:var(--states-success-light);cursor:pointer}.tk-button.success.filledLight:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.success.outlined{background-color:transparent;color:var(--states-success-dark);border:1px solid var(--states-success-sub-base)}.tk-button.success.outlined:hover{background-color:var(--states-success-lightest);cursor:pointer}.tk-button.success.outlined:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.success.outlined:disabled,.tk-button.success.outlined[disabled]{color:var(--icon-sub-base);background-color:transparent;border-color:var(--border-sub-base)}.tk-button.success.text{background-color:transparent;color:var(--states-success-dark);box-shadow:none}.tk-button.success.text:hover{background-color:var(--states-success-lightest);cursor:pointer}.tk-button.success.text:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.success.text:disabled,.tk-button.success.text[disabled]{background-color:transparent;color:var(--icon-sub-base)}.tk-button.success.link{color:var(--states-success-dark)}.tk-button.success.link:hover{color:var(--states-success-darkest);cursor:pointer}.tk-button.success:disabled,.tk-button.success[disabled]{color:var(--text-lightest);background-color:var(--background-sub-base)}.tk-button.danger{border:0px;color:var(--text-lightest);background-color:var(--states-danger-dark)}.tk-button.danger.filled:hover{background-color:var(--states-danger-darkest);cursor:pointer}.tk-button.danger.filled:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.danger.filledLight{color:var(--states-danger-dark);background-color:var(--states-danger-lightest)}.tk-button.danger.filledLight:hover{background-color:var(--states-danger-light);cursor:pointer}.tk-button.danger.filledLight:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.danger.outlined{background-color:transparent;color:var(--states-danger-dark);border:1px solid var(--states-danger-sub-base)}.tk-button.danger.outlined:hover{background-color:var(--states-danger-lightest);cursor:pointer}.tk-button.danger.outlined:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.danger.outlined:disabled,.tk-button.danger.outlined[disabled]{color:var(--background-sub-base);background-color:transparent;border-color:var(--background-sub-base)}.tk-button.danger.text{background-color:transparent;color:var(--states-danger-dark);box-shadow:none}.tk-button.danger.text:hover{background-color:var(--states-danger-lightest);cursor:pointer}.tk-button.danger.text:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.danger.text:disabled,.tk-button.danger.text[disabled]{background-color:transparent;color:var(--background-sub-base)}.tk-button.danger.link{color:var(--states-danger-dark)}.tk-button.danger.link:hover{color:var(--states-danger-darkest);cursor:pointer}.tk-button.danger:disabled,.tk-button.danger[disabled]{color:var(--text-lightest);background-color:var(--background-sub-base)}.tk-button.warning{border:0px;color:var(--states-warning-dark)}.tk-button.warning.filled{color:var(--text-lightest);background-color:var(--states-warning-dark)}.tk-button.warning.filled:hover{background-color:var(--states-warning-darkest);cursor:pointer}.tk-button.warning.filled:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.warning.filledLight{background-color:var(--states-warning-lightest)}.tk-button.warning.filledLight:hover{background-color:var(--states-warning-light);cursor:pointer}.tk-button.warning.filledLight:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.warning.outlined{background-color:transparent;border:1px solid var(--states-warning-sub-base)}.tk-button.warning.outlined:hover{background-color:var(--states-warning-lightest);cursor:pointer}.tk-button.warning.outlined:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.warning.outlined:disabled,.tk-button.warning.outlined[disabled]{color:var(--text-light);background-color:transparent;border-color:var(--border-sub-base)}.tk-button.warning.text{background-color:transparent;box-shadow:none}.tk-button.warning.text:hover{background-color:var(--states-warning-lightest);cursor:pointer}.tk-button.warning.text:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.warning.text:disabled,.tk-button.warning.text[disabled]{background-color:transparent;color:var(--text-light)}.tk-button.warning.link{color:var(--states-warning-dark)}.tk-button.warning.link:hover{color:var(--states-warning-darkest);cursor:pointer}.tk-button.warning:disabled,.tk-button.warning[disabled]{color:var(--text-lightest);background-color:var(--background-sub-base)}.tk-button.white{border:0px;background-color:var(--static-light);color:var(--text-darkest)}.tk-button.white.filled:hover{background-color:var(--background-lightest);cursor:pointer}.tk-button.white.filled:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.white.filledLight{background-color:var(--overlay-white-lightest);color:var(--text-lightest)}.tk-button.white.filledLight:hover{background-color:var(--overlay-white-light);cursor:pointer}.tk-button.white.filledLight:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.white.outlined{background-color:transparent;color:var(--text-lightest);border:1px solid var(--text-lightest)}.tk-button.white.outlined:hover{background-color:var(--overlay-white-light);cursor:pointer}.tk-button.white.outlined:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.white.outlined:disabled,.tk-button.white.outlined[disabled]{background-color:transparent;border-color:var(--overlay-white-lightest)}.tk-button.white.text{background-color:transparent;color:var(--text-lightest);box-shadow:none}.tk-button.white.text:hover{color:var(--text-lightest);background-color:var(--overlay-white-light);cursor:pointer}.tk-button.white.text:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.white.text:disabled,.tk-button.white.text[disabled]{background-color:transparent}.tk-button.white.link{color:var(--text-lightest)}.tk-button.white.link:hover{color:var(--text-light);cursor:pointer}.tk-button.white:disabled,.tk-button.white[disabled]{background-color:var(--overlay-white-lightest);color:var(--overlay-white-light)}.tk-button.black{border:0px;color:var(--text-darkest)}.tk-button.black.filled{background-color:var(--background-darkest);color:var(--text-lightest)}.tk-button.black.filled:hover{background-color:var(--static-dark);cursor:pointer}.tk-button.black.filled:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.black.filledLight{background-color:var(--overlay-black-xlightest)}.tk-button.black.filledLight:hover{background-color:var(--overlay-black-lightest);cursor:pointer}.tk-button.black.filledLight:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.black.outlined{background-color:transparent;border:1px solid var(--icon-darkest)}.tk-button.black.outlined:hover{background-color:var(--overlay-black-lightest);cursor:pointer}.tk-button.black.outlined:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.black.outlined:disabled,.tk-button.black.outlined[disabled]{background-color:transparent;border-color:var(--overlay-black-xlightest)}.tk-button.black.text{background-color:transparent;box-shadow:none}.tk-button.black.text:hover{color:var(--text-darkest);background-color:var(--overlay-black-lightest);cursor:pointer}.tk-button.black.text:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.black.text:disabled,.tk-button.black.text[disabled]{background-color:transparent}.tk-button.black.link{color:var(--text-dark)}.tk-button.black.link:hover{color:var(--text-darkest);cursor:pointer}.tk-button.black:disabled,.tk-button.black[disabled]{color:var(--overlay-black-lightest);background-color:var(--overlay-black-xlightest)}.tk-button.purple{border:0px;color:var(--static-white)}.tk-button.purple.filled{background-color:var(--purple-600)}.tk-button.purple.filled:hover{background-color:var(--purple-800);cursor:pointer}.tk-button.purple.filled:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.purple.filledLight{color:var(--purple-700);background-color:var(--purple-100)}.tk-button.purple.filledLight:hover{background-color:var(--purple-200);cursor:pointer}.tk-button.purple.filledLight:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.purple.outlined{background-color:transparent;color:var(--purple-600);border:1px solid var(--purple-300)}.tk-button.purple.outlined:hover{background-color:var(--purple-50);cursor:pointer}.tk-button.purple.outlined:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.purple.outlined:disabled,.tk-button.purple.outlined[disabled]{color:var(--text-sub-base);background-color:transparent;border-color:var(--border-sub-base)}.tk-button.purple.text{background-color:transparent;color:var(--purple-600);box-shadow:none}.tk-button.purple.text:hover{background-color:var(--purple-50);cursor:pointer}.tk-button.purple.text:focus-visible{box-shadow:var(--secondary-focus)}.tk-button.purple.text:disabled,.tk-button.purple.text[disabled]{background-color:transparent;color:var(--text-sub-base)}.tk-button.purple.link{color:var(--purple-600)}.tk-button.purple.link:hover{color:var(--purple-800);cursor:pointer}.tk-button.purple:disabled,.tk-button.purple[disabled]{color:var(--static-white);background-color:var(--background-sub-base)}.tk-button.animated{isolation:isolate;--tk-button-animated-content-color:var(--text-lightest);--tk-button-animated-outlined-content-color:var(--primary-darkest);--tk-button-animated-surface-middle:var(--primary-base);--tk-button-animated-surface-bottom:var(--primary-darkest);--tk-button-animated-glow-dark:var(--primary-900);--tk-button-animated-glow-light:var(--primary-300);--tk-button-animated-shadow-dark:var(--shadow-primary-alpha-dark);--tk-button-animated-shadow-base:var(--shadow-primary-alpha-base);--tk-button-animated-shadow-light:var(--shadow-primary-alpha-light);--tk-button-animated-focus-shadow:var(--primary-focus);--tk-button-animated-glow:0px 2px 2px 0px var(--tk-button-animated-shadow-dark), 0px 6px 12px 0px var(--tk-button-animated-shadow-base), 0px 8px 20px 6px var(--tk-button-animated-shadow-light);transition:color 200ms cubic-bezier(0.17, 0.67, 0.83, 0.67), box-shadow 200ms cubic-bezier(0.17, 0.67, 0.83, 0.67)}.tk-button.animated>*{position:relative;z-index:2}.tk-button.animated.secondary{--tk-button-animated-outlined-content-color:var(--secondary-darkest);--tk-button-animated-surface-middle:var(--secondary-base);--tk-button-animated-surface-bottom:var(--secondary-darkest);--tk-button-animated-glow-dark:var(--secondary-900);--tk-button-animated-glow-light:var(--secondary-300);--tk-button-animated-shadow-dark:var(--alpha-brand-secondary-500-16);--tk-button-animated-shadow-base:var(--alpha-brand-secondary-500-8);--tk-button-animated-shadow-light:var(--alpha-brand-secondary-500-8);--tk-button-animated-focus-shadow:var(--secondary-focus)}.tk-button.animated.neutral{--tk-button-animated-content-color:var(--text-lightest);--tk-button-animated-outlined-content-color:var(--text-darkest);--tk-button-animated-surface-middle:var(--background-base);--tk-button-animated-surface-bottom:var(--background-darkest);--tk-button-animated-glow-dark:var(--background-darkest);--tk-button-animated-glow-light:var(--background-light);--tk-button-animated-shadow-dark:color-mix(in srgb, var(--background-base) 16%, transparent);--tk-button-animated-shadow-base:color-mix(in srgb, var(--background-base) 8%, transparent);--tk-button-animated-shadow-light:color-mix(in srgb, var(--background-base) 8%, transparent);--tk-button-animated-focus-shadow:var(--secondary-focus)}.tk-button.animated.info{--tk-button-animated-content-color:var(--text-lightest);--tk-button-animated-outlined-content-color:var(--states-info-dark);--tk-button-animated-surface-middle:var(--states-info-base);--tk-button-animated-surface-bottom:var(--states-info-darkest);--tk-button-animated-glow-dark:var(--states-info-darkest);--tk-button-animated-glow-light:var(--states-info-light);--tk-button-animated-shadow-dark:color-mix(in srgb, var(--states-info-base) 16%, transparent);--tk-button-animated-shadow-base:color-mix(in srgb, var(--states-info-base) 8%, transparent);--tk-button-animated-shadow-light:color-mix(in srgb, var(--states-info-base) 8%, transparent);--tk-button-animated-focus-shadow:var(--secondary-focus)}.tk-button.animated.success{--tk-button-animated-content-color:var(--text-lightest);--tk-button-animated-outlined-content-color:var(--states-success-dark);--tk-button-animated-surface-middle:var(--states-success-base);--tk-button-animated-surface-bottom:var(--states-success-darkest);--tk-button-animated-glow-dark:var(--states-success-darkest);--tk-button-animated-glow-light:var(--states-success-light);--tk-button-animated-shadow-dark:color-mix(in srgb, var(--states-success-base) 16%, transparent);--tk-button-animated-shadow-base:color-mix(in srgb, var(--states-success-base) 8%, transparent);--tk-button-animated-shadow-light:color-mix(in srgb, var(--states-success-base) 8%, transparent);--tk-button-animated-focus-shadow:var(--secondary-focus)}.tk-button.animated.danger{--tk-button-animated-content-color:var(--text-lightest);--tk-button-animated-outlined-content-color:var(--states-danger-dark);--tk-button-animated-surface-middle:var(--states-danger-base);--tk-button-animated-surface-bottom:var(--states-danger-darkest);--tk-button-animated-glow-dark:var(--states-danger-darkest);--tk-button-animated-glow-light:var(--states-danger-light);--tk-button-animated-shadow-dark:color-mix(in srgb, var(--states-danger-base) 16%, transparent);--tk-button-animated-shadow-base:color-mix(in srgb, var(--states-danger-base) 8%, transparent);--tk-button-animated-shadow-light:color-mix(in srgb, var(--states-danger-base) 8%, transparent);--tk-button-animated-focus-shadow:var(--secondary-focus)}.tk-button.animated.warning{--tk-button-animated-content-color:var(--text-lightest);--tk-button-animated-outlined-content-color:var(--states-warning-dark);--tk-button-animated-surface-middle:var(--states-warning-base);--tk-button-animated-surface-bottom:var(--states-warning-darkest);--tk-button-animated-glow-dark:var(--states-warning-darkest);--tk-button-animated-glow-light:var(--states-warning-light);--tk-button-animated-shadow-dark:color-mix(in srgb, var(--states-warning-base) 16%, transparent);--tk-button-animated-shadow-base:color-mix(in srgb, var(--states-warning-base) 8%, transparent);--tk-button-animated-shadow-light:color-mix(in srgb, var(--states-warning-base) 8%, transparent);--tk-button-animated-focus-shadow:var(--secondary-focus)}.tk-button.animated.white{--tk-button-animated-content-color:var(--text-darkest);--tk-button-animated-outlined-content-color:var(--text-lightest);--tk-button-animated-surface-middle:var(--static-white);--tk-button-animated-surface-bottom:var(--background-light);--tk-button-animated-glow-dark:var(--background-base);--tk-button-animated-glow-light:var(--static-white);--tk-button-animated-shadow-dark:color-mix(in srgb, var(--background-base) 16%, transparent);--tk-button-animated-shadow-base:color-mix(in srgb, var(--background-base) 8%, transparent);--tk-button-animated-shadow-light:color-mix(in srgb, var(--background-base) 8%, transparent);--tk-button-animated-focus-shadow:var(--secondary-focus)}.tk-button.animated.black{--tk-button-animated-content-color:var(--text-lightest);--tk-button-animated-outlined-content-color:var(--text-darkest);--tk-button-animated-surface-middle:var(--background-dark);--tk-button-animated-surface-bottom:var(--static-dark);--tk-button-animated-glow-dark:var(--static-dark);--tk-button-animated-glow-light:var(--background-light);--tk-button-animated-shadow-dark:color-mix(in srgb, var(--background-darkest) 16%, transparent);--tk-button-animated-shadow-base:color-mix(in srgb, var(--background-darkest) 8%, transparent);--tk-button-animated-shadow-light:color-mix(in srgb, var(--background-darkest) 8%, transparent);--tk-button-animated-focus-shadow:var(--secondary-focus)}.tk-button.animated.purple{--tk-button-animated-content-color:var(--text-lightest);--tk-button-animated-outlined-content-color:var(--purple-900);--tk-button-animated-surface-middle:var(--purple-500);--tk-button-animated-surface-bottom:var(--purple-900);--tk-button-animated-glow-dark:var(--purple-900);--tk-button-animated-glow-light:var(--purple-300);--tk-button-animated-shadow-dark:color-mix(in srgb, var(--purple-500) 16%, transparent);--tk-button-animated-shadow-base:color-mix(in srgb, var(--purple-500) 8%, transparent);--tk-button-animated-shadow-light:color-mix(in srgb, var(--purple-500) 8%, transparent);--tk-button-animated-focus-shadow:var(--secondary-focus)}.tk-button.animated:not(.outlined):not(.link){color:var(--tk-button-animated-content-color);background:linear-gradient(160deg, var(--tk-button-animated-glow-dark) 0%, var(--tk-button-animated-surface-bottom) 28%, var(--tk-button-animated-surface-middle) 56%, var(--tk-button-animated-surface-middle) 78%, var(--tk-button-animated-glow-dark) 100%) padding-box, linear-gradient(45deg, var(--tk-button-animated-surface-middle) 0%, var(--static-dark) 100%) border-box;border:1px solid transparent;box-shadow:var(--tk-button-animated-glow)}.tk-button.animated:not(.outlined):not(.link)::before{content:"";position:absolute;inset:-96% -64%;pointer-events:none;opacity:1;filter:blur(14px);background:radial-gradient(44% 42% at 22% 68%, var(--tk-button-animated-glow-light) 0%, var(--tk-button-animated-surface-middle) 34%, transparent 70%), radial-gradient(46% 48% at 78% 28%, var(--tk-button-animated-glow-dark) 0%, var(--tk-button-animated-surface-bottom) 48%, transparent 74%), radial-gradient(42% 40% at 54% 52%, var(--tk-button-animated-surface-middle) 0%, transparent 66%);animation:tk-button-animated-swim 3.6s linear infinite;transition:opacity 120ms ease-out;z-index:0;will-change:transform, opacity}.tk-button.animated:not(.outlined):not(.link):hover{color:var(--tk-button-animated-content-color);background:linear-gradient(var(--tk-button-animated-surface-bottom), var(--tk-button-animated-surface-bottom)) padding-box, linear-gradient(45deg, var(--tk-button-animated-surface-middle) 0%, var(--static-dark) 100%) border-box;cursor:pointer}.tk-button.animated:not(.outlined):not(.link):focus-visible{box-shadow:var(--tk-button-animated-focus-shadow), var(--tk-button-animated-glow)}.tk-button.animated:not(.outlined):not(.link):disabled,.tk-button.animated:not(.outlined):not(.link)[disabled]{color:var(--static-white);background:var(--background-sub-base);border-color:transparent;box-shadow:none}.tk-button.animated:not(.outlined):not(.link):disabled::before,.tk-button.animated:not(.outlined):not(.link)[disabled]::before{animation:none;opacity:0}.tk-button.animated.outlined{color:var(--tk-button-animated-outlined-content-color);background:transparent;border:1px solid transparent;box-shadow:var(--tk-button-animated-glow);overflow:visible}.tk-button.animated.outlined::before{content:"";position:absolute;inset:-1px;border-radius:inherit;padding:1px;pointer-events:none;opacity:1;filter:none;background:linear-gradient(45deg, var(--tk-button-animated-surface-middle) 0%, var(--tk-button-animated-surface-middle) 36%, var(--static-dark) 72%, var(--static-dark) 100%) 0% 50%/220% 220%;animation:tk-button-animated-border-flow 3.2s linear infinite;transition:none;z-index:0;will-change:background-position;-webkit-mask:linear-gradient(var(--static-white) 0 0) content-box, linear-gradient(var(--static-white) 0 0);-webkit-mask-composite:xor;mask:linear-gradient(var(--static-white) 0 0) content-box, linear-gradient(var(--static-white) 0 0);mask-composite:exclude}.tk-button.animated.outlined:hover{color:var(--tk-button-animated-outlined-content-color);background:transparent;border-color:transparent;cursor:pointer}.tk-button.animated.outlined:hover::before{background:var(--tk-button-animated-surface-middle);animation:none}.tk-button.animated.outlined:focus-visible{box-shadow:var(--tk-button-animated-focus-shadow), var(--tk-button-animated-glow)}.tk-button.animated.outlined:disabled,.tk-button.animated.outlined[disabled]{color:var(--text-sub-base);background:transparent;border-color:var(--border-sub-base);box-shadow:none}.tk-button.animated.outlined:disabled::before,.tk-button.animated.outlined[disabled]::before{animation:none;opacity:0}.tk-button.small{min-height:32px;font-size:var(--desktop-body-xs-size);border-radius:var(--button-sizes-small-radius);padding:var(--button-sizes-small-v-padding) var(--button-sizes-small-h-padding);gap:var(--button-sizes-small-gap)}.tk-button.small.rounded{height:unset;border-radius:100%}.tk-button.small.icon-only{width:32px;height:32px;box-sizing:border-box;padding:var(--button-sizes-small-full-padding)}.tk-button.small.rounded{padding:var(--fab-button-sizes-small-v-padding) var(--fab-button-sizes-small-h-padding)}.tk-button.base{min-height:40px;font-size:var(--desktop-body-s-size);border-radius:var(--button-sizes-base-radius);padding:var(--button-sizes-base-v-padding) var(--button-sizes-base-h-padding);gap:var(--button-sizes-base-gap)}.tk-button.base.rounded{height:unset;border-radius:100%}.tk-button.base.icon-only{width:40px;height:40px;box-sizing:border-box;padding:var(--button-sizes-base-full-padding)}.tk-button.base.rounded{padding:var(--fab-button-sizes-base-v-padding) var(--fab-button-sizes-base-h-padding)}.tk-button.large{min-height:48px;font-size:var(--desktop-body-m-base-size);border-radius:var(--button-sizes-large-radius);padding:var(--button-sizes-large-v-padding) var(--button-sizes-large-h-padding);gap:var(--button-sizes-large-gap)}.tk-button.large.rounded{height:unset;border-radius:100%}.tk-button.large.icon-only{width:48px;height:48px;box-sizing:border-box;padding:var(--button-sizes-large-full-padding)}.tk-button.large.rounded{padding:var(--fab-button-sizes-large-v-padding) var(--fab-button-sizes-large-h-padding)}.tk-button.link{padding:0;background-color:transparent;text-decoration:none}.tk-button.link.underline span{text-decoration:underline}.tk-button.link:disabled,.tk-button.link[disabled]{color:var(--text-sub-base);background-color:transparent}.tk-button.link.small{height:18px}.tk-button.link.base{height:20px}.tk-button.link.large{height:24px}@keyframes tk-button-animated-border-flow{0%{background-position:0% 50%}50%{background-position:100% 50%}100%{background-position:0% 50%}}@keyframes tk-button-animated-swim{0%{transform:translate3d(16%, -10%, 0) rotate(0deg) scale(1.08)}25%{transform:translate3d(0%, 14%, 0) rotate(90deg) scale(1.14)}50%{transform:translate3d(-18%, 0%, 0) rotate(180deg) scale(1.1)}75%{transform:translate3d(0%, -18%, 0) rotate(270deg) scale(1.16)}100%{transform:translate3d(16%, -10%, 0) rotate(360deg) scale(1.08)}}@media (prefers-reduced-motion: reduce){.tk-button.animated::before{animation:none;transition:none}}`;

const TkButton = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.tkClick = index.createEvent(this, "tk-click", 7);
        /**
         * This field specifies the design type of the component.
         * @defaultValue filled
         */
        this.type = 'filled';
        /**
         * Applies the animated button style, adapting to the selected variant.
         * Only the `filled` and `outlined` types are animated.
         */
        this.animated = false;
        /**
         * Defines the position of the icon.
         * @defaultValue 'left'
         */
        this.iconPosition = 'left';
        /**
         * Label text displayed inside the button.
         */
        this.label = '';
        /**
         * Sets size for the component.
         * @defaultValue 'base'
         */
        this.size = 'base';
        /**
         * Sets the button type.
         * @defaultValue 'button'
         */
        this.mode = 'button';
        /**
         * Determines the button's variant for different styles.
         */
        this.variant = 'primary';
    }
    componentWillLoad() {
        if (this.mode == 'link') {
            this.type = null;
        }
    }
    handleClick(e) {
        if (this.disabled) {
            e.preventDefault();
            e.stopPropagation();
            return;
        }
        this.tkClick.emit(e);
        if (this.mode === 'submit') {
            const form = this.el.closest('form');
            if (form) {
                form.requestSubmit();
            }
        }
        else if (this.mode === 'reset') {
            const form = this.el.closest('form');
            if (form) {
                form.reset();
            }
        }
    }
    /**
     * The animated style is only applied to the `filled` and `outlined` types.
     */
    get isAnimated() {
        return this.animated && (this.type === 'filled' || this.type === 'outlined');
    }
    getButtonIconColor() {
        if (this.disabled) {
            return this.type === 'filled' || this.type === 'filledLight' || (this.isAnimated && this.type !== 'outlined') ? 'var(--static-white)' : 'var(--icon-sub-base)';
        }
        if (this.isAnimated) {
            if (this.type === 'outlined') {
                return 'var(--tk-button-animated-outlined-content-color)';
            }
            return 'var(--tk-button-animated-content-color)';
        }
        if (this.type === 'filled') {
            return this.variant === 'white' ? 'var(--text-darkest)' : 'var(--static-white)';
        }
        return 'currentColor';
    }
    render() {
        var _a;
        const hasMultipleIcons = iconUtils.isMultiIconOptions(this.icon);
        const rootClasses = index$1.classNames('tk-button', this.type, {
            'loading': this.loading && !this.disabled,
            'rounded': this.rounded && this.icon && (this.label == '' || this.label == null || this.label.length <= 0),
            'link': this.mode == 'link',
            'underline': this.underline,
            'animated': this.isAnimated,
            'icon-only': (this.label == '' || this.label == null || this.label.length <= 0) && this.icon && !hasMultipleIcons,
        }, [this.variant], [this.size]);
        let _leftIcon;
        let _rightIcon;
        const spinnerElement = (index.h("tk-spinner", { key: '21a07d11e66d787114ab493ab75a17ff578cfa00', dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'loading-spinner'), size: this.size === 'large' ? 'small' : this.size === 'base' ? 'xsmall' : 'xxsmall' }));
        if (this.loading) {
            _leftIcon = spinnerElement;
        }
        else if (this.icon) {
            const { leftIcon, rightIcon } = iconUtils.renderIcons(this.icon, {
                variant: null,
                additionalProps: {
                    color: this.getButtonIconColor(),
                },
                dataTestid: this.dataTestid,
            }, this.iconPosition);
            _leftIcon = leftIcon;
            _rightIcon = rightIcon;
        }
        let Tag;
        let props;
        if (this.mode == 'link') {
            Tag = 'a';
            props = { href: this.href, target: this.target };
        }
        else {
            Tag = 'button';
            props = { type: this.type };
        }
        let label;
        if (((_a = this.label) === null || _a === void 0 ? void 0 : _a.length) > 0) {
            label = index.h("span", { key: '32c2cb010199ad997fc3e69770aff3acae79da27', "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'label') }, this.label);
        }
        return (index.h(index.Host, { key: '9c900348e97a96f561571cd2f8a929eaac78d1b3', class: { 'full-width': this.fullWidth } }, index.h(Tag, Object.assign({ key: '3b2bba99d3f119c59d9b486c2ad7c3ff230bae89', class: rootClasses }, props, { style: this.containerStyle, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'container'), disabled: this.disabled, onClick: (e) => this.handleClick(e) }), _leftIcon, label, _rightIcon)));
    }
    get el() { return index.getElement(this); }
};
TkButton.style = tkButtonCss();

const tkCheckboxCss = () => `@font-face{font-family:"Material Symbols Outlined";src:url("./assets/fonts/material-symbols/Material-Symbols-Outlined.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-outlined{font-family:"Material Symbols Outlined";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-outlined{font-variation-settings:"FILL" 0}.material-symbols-outlined.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Rounded";src:url("./assets/fonts/material-symbols/Material-Symbols-Rounded.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-rounded{font-family:"Material Symbols Rounded";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-rounded{font-variation-settings:"FILL" 0}.material-symbols-rounded.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Sharp";src:url("./assets/fonts/material-symbols/Material-Symbols-Sharp.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-sharp{font-family:"Material Symbols Sharp";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-sharp{font-variation-settings:"FILL" 0}.material-symbols-sharp.fill{font-variation-settings:"FILL" 1}tk-checkbox{display:inline-block}.tk-checkbox-container{font-family:var(--family-body)}.tk-checkbox-container.card{border-radius:var(--radius-m-base);border:1px solid var(--border-light);background:var(--static-Light);gap:var(--card-switcher-container-gap);padding:var(--card-switcher-container-v-padding) var(--card-switcher-container-h-padding)}.tk-checkbox-container.card:hover{background-color:var(--background-lightest)}.tk-checkbox-container.card .mask{align-self:center}.tk-checkbox-container label{padding:2px;display:flex;flex-direction:row;align-items:center;justify-content:flex-start;gap:var(--spacing-s);color:var(--text-darkest);font-size:var(--size-sm);cursor:pointer;user-select:none}.tk-checkbox-container label input[type=checkbox]{margin:0;appearance:none;outline:none;display:none}.tk-checkbox-container label .mask{background-color:var(--static-light);border:2px solid var(--border-sub-base);border-radius:var(--radius-s);box-sizing:border-box;-moz-box-sizing:border-box;-webkit-box-sizing:border-box}.tk-checkbox-container label .mask .inner-mask{visibility:hidden;background-color:var(--states-info-base);display:flex;border-radius:2px}.tk-checkbox-container label .tk-checkbox-text-holder .tk-checkbox-description{color:var(--text-base);font-size:var(--size-xs);font-weight:var(--weight-regular)}.tk-checkbox-container:hover label .mask{background-color:var(--background-lightest)}.tk-checkbox-container label input:focus-visible+.mask{border-color:var(--states-info-base)}.tk-checkbox-container label input:checked+.mask,.tk-checkbox-container label input:indeterminate+.mask{border-color:var(--states-info-light);background-color:var(--static-light)}.tk-checkbox-container label input:checked+.mask .inner-mask,.tk-checkbox-container label input:indeterminate+.mask .inner-mask{visibility:visible}.tk-checkbox-container label:hover input:checked+.mask,.tk-checkbox-container label:hover input:indeterminate+.mask{border-color:var(--states-info-sub-base);background-color:var(--states-info-light)}.tk-checkbox-container[aria-disabled],.tk-checkbox-container[disabled]{pointer-events:none}.tk-checkbox-container[aria-disabled] label,.tk-checkbox-container[disabled] label{color:var(--text-sub-base)}.tk-checkbox-container[aria-disabled] label input+.mask,.tk-checkbox-container[disabled] label input+.mask{border:2px solid var(--border-sub-base);background-color:var(--background-light)}.tk-checkbox-container[aria-disabled] label input+.mask .inner-mask,.tk-checkbox-container[disabled] label input+.mask .inner-mask{background-color:var(--background-sub-base)}.tk-checkbox-container[aria-invalid] label .mask{border:2px solid var(--states-danger-base);background:var(--static-light)}.tk-checkbox-container[aria-invalid] label input:checked+.mask,.tk-checkbox-container[aria-invalid] label input:indeterminate+.mask{border-color:var(--states-danger-light)}.tk-checkbox-container[aria-invalid] label input:checked+.mask .inner-mask,.tk-checkbox-container[aria-invalid] label input:indeterminate+.mask .inner-mask{background-color:var(--states-danger-base)}.tk-checkbox-container[aria-invalid] label:hover input:checked+.mask,.tk-checkbox-container[aria-invalid] label:hover input:indeterminate+.mask{border-color:var(--states-danger-sub-base);background-color:var(--states-danger-light)}.tk-checkbox-container.base label .mask{border-radius:6px;padding:2px}.tk-checkbox-container.base label .mask .inner-mask{width:12px;height:12px}.tk-checkbox-container.small label .mask{border-radius:4px;padding:1px}.tk-checkbox-container.small label .mask .inner-mask{width:10px;height:10px}`;

const TkCheckbox = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.tkChange = index.createEvent(this, "tk-change", 7);
        if (hostRef.$hostElement$["s-ei"]) {
            this.internals = hostRef.$hostElement$["s-ei"];
        }
        else {
            this.internals = hostRef.$hostElement$.attachInternals();
            hostRef.$hostElement$["s-ei"] = this.internals;
        }
        this.uniqueId = v4.v4();
        /**
         * Controls if checkbox has custom content.
         * @defaultValue false
         */
        this.hasContentSlot = false;
        /**
         * If true, the user cannot interact with the checkbox.
         */
        this.disabled = false;
        /**
         * Indicates whether the input is in an invalid state
         * @defaultValue false
         */
        this.invalid = false;
        /**
         * Determines the appearance types of the checkbox.
         */
        this.type = 'default';
        /**
         * Determines the size of the checkbox.
         */
        this.size = 'base';
        /**
         * If true, the checkbox will be indeterminate.
         */
        this.indeterminate = false;
        /**
         * Sets the checkbox value
         */
        this.value = false;
    }
    indeterminateChanged(newValue, oldValue) {
        if (newValue !== oldValue) {
            this.inputElement.indeterminate = newValue; // update the checkbox's indeterminate property
            if (newValue)
                this.value = null;
        }
    }
    valueChanged(newValue, oldValue) {
        if (newValue !== oldValue) {
            if (newValue !== null) {
                this.indeterminate = false;
            }
        }
    }
    componentWillLoad() {
        if (this.indeterminate)
            this.value = null;
        this.hasContentSlot = !!this.el.querySelector('[slot="content"]');
    }
    formResetCallback() {
        this.handleFormReset();
    }
    handleFormReset() {
        this.value = false;
        this.indeterminate = false;
        this.inputElement.checked = false;
        this.tkChange.emit(this.value);
    }
    handleChange() {
        if (!this.disabled) {
            if (this.inputElement.indeterminate) {
                this.value = null;
                this.indeterminate = false;
            }
            else {
                this.value = this.inputElement.checked;
            }
            this.tkChange.emit(this.value);
        }
    }
    renderInput() {
        return (index.h("input", { id: this.uniqueId, type: "checkbox", ref: el => (this.inputElement = el), checked: this.value, indeterminate: this.indeterminate, disabled: this.disabled, onChange: this.handleChange.bind(this), name: this.name }));
    }
    render() {
        return (index.h("div", { key: '86a49896283f2adb6433b92d44607c10deb67a8c', class: index$1.classNames('tk-checkbox-container', this.type, this.size), "aria-disabled": this.disabled, "aria-invalid": this.invalid }, index.h("label", { key: '1905eb7333677757c7fd1a280d84211d4dcd8273', htmlFor: this.uniqueId }, this.renderInput(), index.h("div", { key: '0c3b71f0d2f6d16fb710e4f9b2760bb6b13dc959', class: "mask" }, index.h("tk-icon", Object.assign({ key: '587ec84435a4b90081b8e658976a33b0ad56f4b9', class: "inner-mask" }, iconUtils.getIconElementProps(this.indeterminate ? 'remove' : this.value ? 'check' : null, {
            color: 'var(--static-light)',
            size: this.size === 'small' ? 'xxsmall' : 'xsmall',
        })))), this.hasContentSlot ? (index.h("slot", { name: "content" })) : (index.h("div", { class: "tk-checkbox-text-holder" }, index.h("div", { class: "tk-checkbox-label" }, this.label), index.h("div", { class: "tk-checkbox-description" }, this.description))))));
    }
    static get formAssociated() { return true; }
    get el() { return index.getElement(this); }
    static get watchers() { return {
        "indeterminate": [{
                "indeterminateChanged": 0
            }],
        "value": [{
                "valueChanged": 0
            }]
    }; }
};
TkCheckbox.style = tkCheckboxCss();

const tkChipsCss = () => `@font-face{font-family:"Material Symbols Outlined";src:url("./assets/fonts/material-symbols/Material-Symbols-Outlined.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-outlined{font-family:"Material Symbols Outlined";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-outlined{font-variation-settings:"FILL" 0}.material-symbols-outlined.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Rounded";src:url("./assets/fonts/material-symbols/Material-Symbols-Rounded.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-rounded{font-family:"Material Symbols Rounded";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-rounded{font-variation-settings:"FILL" 0}.material-symbols-rounded.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Sharp";src:url("./assets/fonts/material-symbols/Material-Symbols-Sharp.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-sharp{font-family:"Material Symbols Sharp";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-sharp{font-variation-settings:"FILL" 0}.material-symbols-sharp.fill{font-variation-settings:"FILL" 1}:host{display:inline-flex}:host(.full-width){width:100%}:host(.full-width) .tk-chips{width:100%}.tk-chips{font-family:var(--family-body);display:inline-flex;align-items:center;justify-content:center;border-radius:var(--chips-large-radius);border-width:0px}.tk-chips.removable{cursor:pointer}.tk-chips tk-icon{user-select:none}.tk-chips.disabled{color:var(--text-sub-base) !important;background-color:var(--frames-light) !important;pointer-events:none}.tk-chips.disabled.avatar,.tk-chips.disabled.outlined{border:1px solid var(--border-sub-base) !important;background-color:var(--static-light) !important}.tk-chips.disabled:active{box-shadow:none !important}.tk-chips.primary{color:var(--primary-base)}.tk-chips.primary.filled{color:var(--static-white);background-color:var(--primary-base)}.tk-chips.primary.filled:hover{background-color:var(--primary-dark)}.tk-chips.primary.outlined{border:1px solid var(--primary-light)}.tk-chips.primary.outlined:hover{background-color:var(--primary-lightest)}.tk-chips.primary.filledlight{background-color:var(--primary-lightest);color:var(--primary-base)}.tk-chips.primary.filledlight:hover{color:var(--primary-dark);background-color:var(--primary-light)}.tk-chips.primary.avatar{border:1px solid var(--primary-base)}.tk-chips.primary.avatar:hover{background-color:var(--primary-lightest)}.tk-chips.primary:active{box-shadow:var(--primary-focus)}.tk-chips.secondary{color:var(--secondary-darkest)}.tk-chips.secondary.filled{background-color:var(--secondary-darkest);color:var(--text-lightest)}.tk-chips.secondary.filled:hover{background-color:var(--secondary-dark)}.tk-chips.secondary.outlined{border:1px solid var(--secondary-sub-base)}.tk-chips.secondary.outlined:hover{background-color:var(--secondary-lightest)}.tk-chips.secondary.filledlight{background-color:var(--secondary-light);color:var(--secondary-darkest)}.tk-chips.secondary.filledlight:hover{color:var(--secondary-darkest);background-color:var(--secondary-sub-base)}.tk-chips.secondary.avatar{border:1px solid var(--secondary-sub-base)}.tk-chips.secondary.avatar:hover{background-color:var(--secondary-lightest)}.tk-chips.secondary:active{box-shadow:var(--secondary-focus)}.tk-chips.neutral{color:var(--text-darkest)}.tk-chips.neutral.filled{background-color:var(--background-darkest);color:var(--text-lightest)}.tk-chips.neutral.filled:hover{background-color:var(--background-dark)}.tk-chips.neutral.outlined{border:1px solid var(--background-sub-base);background-color:var(--static-light)}.tk-chips.neutral.outlined:hover{background-color:var(--background-lightest)}.tk-chips.neutral.filledlight{background-color:var(--icon-light)}.tk-chips.neutral.filledlight:hover{background-color:var(--background-sub-base)}.tk-chips.neutral.avatar{border:1px solid var(--background-sub-base)}.tk-chips.neutral.avatar:hover{background-color:var(--background-lightest)}.tk-chips.neutral:active{box-shadow:var(--secondary-focus)}.tk-chips.info{color:var(--states-info-dark)}.tk-chips.info.filled{color:var(--text-lightest);background-color:var(--states-info-dark)}.tk-chips.info.filled:hover{background-color:var(--states-info-darkest)}.tk-chips.info.outlined{border:1px solid var(--states-info-sub-base)}.tk-chips.info.outlined:hover{background-color:var(--states-info-lightest)}.tk-chips.info.filledlight{background-color:var(--states-info-light)}.tk-chips.info.filledlight:hover{color:var(--states-info-darkest);background-color:var(--states-info-sub-base)}.tk-chips.info.avatar{border:1px solid var(--states-info-sub-base)}.tk-chips.info.avatar:hover{background-color:var(--states-info-lightest)}.tk-chips.info:active{box-shadow:var(--secondary-focus)}.tk-chips.success{color:var(--states-success-dark)}.tk-chips.success.filled{color:var(--text-lightest);background-color:var(--states-success-dark)}.tk-chips.success.filled:hover{background-color:var(--states-success-darkest)}.tk-chips.success.outlined{border:1px solid var(--states-success-sub-base)}.tk-chips.success.outlined:hover{background-color:var(--states-success-lightest)}.tk-chips.success.filledlight{background-color:var(--states-success-light)}.tk-chips.success.filledlight:hover{color:var(--states-success-darkest);background-color:var(--states-success-sub-base)}.tk-chips.success.avatar{border:1px solid var(--states-success-dark)}.tk-chips.success.avatar:hover{background-color:var(--states-success-lightest)}.tk-chips.success:active{box-shadow:var(--secondary-focus)}.tk-chips.danger{color:var(--states-danger-dark)}.tk-chips.danger.filled{color:var(--text-lightest);background-color:var(--states-danger-dark)}.tk-chips.danger.filled:hover{background-color:var(--states-danger-darkest)}.tk-chips.danger.outlined{border:1px solid var(--states-danger-sub-base)}.tk-chips.danger.outlined:hover{background-color:var(--states-danger-lightest)}.tk-chips.danger.filledlight{background-color:var(--states-danger-light)}.tk-chips.danger.filledlight:hover{color:var(--states-danger-darkest);background-color:var(--states-danger-sub-base)}.tk-chips.danger.avatar{border:1px solid var(--states-danger-sub-base)}.tk-chips.danger.avatar:hover{background-color:var(--states-danger-lightest)}.tk-chips.danger:active{box-shadow:var(--secondary-focus)}.tk-chips.warning{color:var(--states-warning-darkest)}.tk-chips.warning.filled{background-color:var(--states-warning-dark);color:var(--text-lightest)}.tk-chips.warning.filled:hover{background-color:var(--states-warning-darkest)}.tk-chips.warning.outlined{border:1px solid var(--states-warning-sub-base)}.tk-chips.warning.outlined:hover{background-color:var(--states-warning-lightest)}.tk-chips.warning.filledlight{background-color:var(--states-warning-lightest)}.tk-chips.warning.filledlight:hover{background-color:var(--states-warning-sub-base)}.tk-chips.warning.avatar{border:1px solid var(--states-warning-sub-base)}.tk-chips.warning.avatar:hover{background-color:var(--states-warning-lightest)}.tk-chips.warning:active{box-shadow:var(--secondary-focus)}.tk-chips.verified{color:var(--states-verified-dark)}.tk-chips.verified.filled{color:var(--text-lightest);background-color:var(--states-verified-dark)}.tk-chips.verified.filled:hover{background-color:var(--states-verified-darkest)}.tk-chips.verified.outlined{border:1px solid var(--states-verified-sub-base)}.tk-chips.verified.outlined:hover{background-color:var(--states-verified-lightest)}.tk-chips.verified.filledlight{background-color:var(--states-verified-lightest)}.tk-chips.verified.filledlight:hover{color:var(--states-verified-darkest);background-color:var(--states-verified-sub-base)}.tk-chips.verified.avatar{border:1px solid var(--states-verified-sub-base)}.tk-chips.verified.avatar:hover{background-color:var(--states-verified-lightest)}.tk-chips.verified:active{box-shadow:var(--secondary-focus)}.tk-chips.small{padding:var(--chips-small-v-padding) var(--chips-small-h-padding);font-size:var(--desktop-body-xs-size);font-style:normal;font-weight:400;line-height:var(--desktop-body-xs-line-height);gap:2px}.tk-chips.base{padding:var(--chips-base-v-padding) var(--chips-base-h-padding);font-size:var(--desktop-body-s-size);font-style:normal;font-weight:400;line-height:var(--desktop-body-s-line-height);gap:4px}.tk-chips.large{padding:var(--chips-large-v-padding) var(--chips-large-h-padding);font-size:var(--desktop-body-s-size);font-style:normal;font-weight:400;line-height:var(--desktop-body-s-line-height);gap:6px}`;

const TkChips = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.tkRemove = index.createEvent(this, "tk-remove", 7);
        /**
         * The disabled status.
         * @defaultValue false
         */
        this.disabled = false;
        /**
         * This field specifies the design type of the component.
         * @defaultValue filled
         */
        this.type = 'filled';
        /**
         * This property determines whether the chip component is removable.
         * @defaultValue false
         */
        this.removable = false;
        /**
         * Determines whether the chip automatically removes itself when the close button is clicked.
         * @defaultValue true
         */
        this.autoSelfDestroy = true;
        /**
         * Sets size for the component.
         * @defaultValue 'base'
         */
        this.size = 'base';
        /**
         * The variant of the chip for styling.
         * @defaultValue 'primary'
         */
        this.variant = 'primary';
        /**
         * The position of the icon relative to the label.
         * @defaultValue 'left'
         */
        this.iconPosition = 'left';
        /**
         * Custom style to apply to the chip component.
         */
        this.containerStyle = null;
        /**
         * If true, the chip will take the full width of its container.
         * @defaultValue false
         */
        this.fullWidth = false;
    }
    componentWillLoad() {
        if (this.value == null)
            this.value = this.label;
    }
    handleClick() {
        var _a;
        this.tkRemove.emit(this.value);
        if (this.autoSelfDestroy)
            (_a = this.el) === null || _a === void 0 ? void 0 : _a.remove();
    }
    handleKeyDown(e) {
        if (e.key === 'Enter' || e.key === ' ') {
            e.preventDefault();
            this.handleClick();
        }
    }
    render() {
        const rootClasses = index$1.classNames('tk-chips', this.variant, this.size, this.type, {
            removable: this.removable,
            disabled: this.disabled,
        });
        const { leftIcon, rightIcon } = iconUtils.renderIcons(this.icon, {
            variant: this.variant,
            size: this.size === 'large' ? 'medium' : this.size,
            additionalProps: { color: this.disabled ? 'var(--icon-sub-base)' : this.type === 'filled' ? 'var(--static-white)' : undefined },
            dataTestid: this.dataTestid,
        }, this.iconPosition);
        return (index.h(index.Host, { key: '95164544389810530977f6319853a102aebbb397', class: { 'full-width': this.fullWidth } }, index.h("div", { key: '1cab6a14305f1ee3cf55ed500d2663bcf5f77867', class: rootClasses, style: this.containerStyle, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'container') }, leftIcon, this.label, rightIcon, this.removable && (index.h("tk-icon", Object.assign({ key: '821075081d9c3950a4e7180dc36974bcd9da226d' }, iconUtils.getIconElementProps('close', {
            variant: this.variant,
            color: this.disabled ? 'var(--icon-sub-base)' : this.type === 'filled' ? 'var(--static-white)' : undefined,
            size: this.size === 'large' ? 'medium' : this.size,
            onClick: () => this.handleClick(),
            onKeyDown: (e) => this.handleKeyDown(e),
            tabIndex: this.disabled ? -1 : 0,
        }), { dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'remove-icon') }))))));
    }
    get el() { return index.getElement(this); }
};
TkChips.style = tkChipsCss();

/**
 * Converts HSL (Hue, Saturation, Lightness) color values to RGB format.
 * @param h - Hue value (0-360)
 * @param s - Saturation value (0-1)
 * @param l - Lightness value (0-1)
 * @returns Object containing r, g, b values (0-255)
 */
function hslToRgb(h, s, l) {
    const c = (1 - Math.abs(2 * l - 1)) * s;
    const hh = h / 60;
    const x = c * (1 - Math.abs((hh % 2) - 1));
    let r = 0, g = 0, b = 0;
    if (hh >= 0 && hh < 1) {
        r = c;
        g = x;
        b = 0;
    }
    else if (hh < 2) {
        r = x;
        g = c;
        b = 0;
    }
    else if (hh < 3) {
        r = 0;
        g = c;
        b = x;
    }
    else if (hh < 4) {
        r = 0;
        g = x;
        b = c;
    }
    else if (hh < 5) {
        r = x;
        g = 0;
        b = c;
    }
    else {
        r = c;
        g = 0;
        b = x;
    }
    const m = l - c / 2;
    return {
        r: Math.round((r + m) * 255),
        g: Math.round((g + m) * 255),
        b: Math.round((b + m) * 255),
    };
}
// ============ HSVA Conversion Functions ============
/**
 * Converts RGB color values to HSVA format.
 * HSVA is commonly used in color pickers for more intuitive color selection.
 * @param r - Red value (0-255)
 * @param g - Green value (0-255)
 * @param b - Blue value (0-255)
 * @param a - Alpha/opacity value (0-1), defaults to 1
 * @returns HSVA color object
 */
function rgbToHsva(r, g, b, a = 1) {
    r /= 255;
    g /= 255;
    b /= 255;
    const max = Math.max(r, g, b);
    const min = Math.min(r, g, b);
    const d = max - min;
    let h = 0;
    const s = max === 0 ? 0 : (d / max) * 100;
    const v = max * 100;
    if (d !== 0) {
        switch (max) {
            case r:
                h = ((g - b) / d + (g < b ? 6 : 0)) * 60;
                break;
            case g:
                h = ((b - r) / d + 2) * 60;
                break;
            case b:
                h = ((r - g) / d + 4) * 60;
                break;
        }
    }
    return { h, s, v, a };
}
/**
 * Converts HSVA color values to RGB format.
 * @param h - Hue value (0-360)
 * @param s - Saturation value (0-100)
 * @param v - Value/Brightness value (0-100)
 * @returns Object containing r, g, b values (0-255)
 */
function hsvaToRgb(h, s, v) {
    h = h / 360;
    s = s / 100;
    v = v / 100;
    let r = 0, g = 0, b = 0;
    const i = Math.floor(h * 6);
    const f = h * 6 - i;
    const p = v * (1 - s);
    const q = v * (1 - f * s);
    const t = v * (1 - (1 - f) * s);
    switch (i % 6) {
        case 0:
            r = v;
            g = t;
            b = p;
            break;
        case 1:
            r = q;
            g = v;
            b = p;
            break;
        case 2:
            r = p;
            g = v;
            b = t;
            break;
        case 3:
            r = p;
            g = q;
            b = v;
            break;
        case 4:
            r = t;
            g = p;
            b = v;
            break;
        case 5:
            r = v;
            g = p;
            b = q;
            break;
    }
    return {
        r: Math.round(r * 255),
        g: Math.round(g * 255),
        b: Math.round(b * 255),
    };
}
/**
 * Converts HSVA color to hexadecimal format.
 * @param hsva - HSVA color object
 * @returns Hex color string (e.g., '#ff0000' or '#ff000080' with alpha)
 */
function hsvaToHex(hsva) {
    const { r, g, b } = hsvaToRgb(hsva.h, hsva.s, hsva.v);
    if (hsva.a < 1) {
        const aa = Math.round(hsva.a * 255)
            .toString(16)
            .padStart(2, '0');
        return `#${[r, g, b].map(x => x.toString(16).padStart(2, '0')).join('')}${aa}`;
    }
    return `#${[r, g, b].map(x => x.toString(16).padStart(2, '0')).join('')}`;
}
/**
 * Converts HSVA color to CSS color string in the specified format.
 * @param hsva - HSVA color object
 * @param format - Output format ('hex' or 'rgba')
 * @returns CSS color string
 */
function hsvaToCss(hsva, format) {
    const { h, s, v, a } = hsva;
    switch (format) {
        case 'hex':
            return hsvaToHex(hsva);
        case 'rgba': {
            const { r, g, b } = hsvaToRgb(h, s, v);
            return `rgba(${r}, ${g}, ${b}, ${a.toFixed(2)})`;
        }
        default:
            return hsvaToHex(hsva);
    }
}
/**
 * Parses a color string (hex, rgb, rgba, hsl, hsla) and converts it to HSVA format.
 * This is the main parsing function used by the color picker component.
 * @param input - Color string in any supported format
 * @returns HSVA color object
 */
function parseColorToHsva(input) {
    if (!input)
        return { h: 0, s: 0, v: 0, a: 1 };
    input = input.trim();
    // Hex
    if (input.startsWith('#')) {
        const hex = input.slice(1);
        let r = 0, g = 0, b = 0, a = 1;
        if (hex.length === 3) {
            r = parseInt(hex[0] + hex[0], 16);
            g = parseInt(hex[1] + hex[1], 16);
            b = parseInt(hex[2] + hex[2], 16);
        }
        else if (hex.length === 6) {
            r = parseInt(hex.slice(0, 2), 16);
            g = parseInt(hex.slice(2, 4), 16);
            b = parseInt(hex.slice(4, 6), 16);
        }
        else if (hex.length === 8) {
            r = parseInt(hex.slice(0, 2), 16);
            g = parseInt(hex.slice(2, 4), 16);
            b = parseInt(hex.slice(4, 6), 16);
            a = parseInt(hex.slice(6, 8), 16) / 255;
        }
        return rgbToHsva(r, g, b, a);
    }
    // rgb(a)
    if (input.startsWith('rgb')) {
        const parts = input.match(/rgba?\(([^)]+)\)/);
        if (parts) {
            const comps = parts[1].split(',').map(x => x.trim());
            const r = Number(comps[0]);
            const g = Number(comps[1]);
            const b = Number(comps[2]);
            const a = comps.length === 4 ? Number(comps[3]) : 1;
            return rgbToHsva(r, g, b, a);
        }
    }
    // hsl(a) - convert to HSVA
    if (input.startsWith('hsl')) {
        const parts = input.match(/hsla?\(([^)]+)\)/);
        if (parts) {
            const comps = parts[1].split(',').map(x => x.trim());
            const h = Number(comps[0]);
            const s = Number(comps[1].replace('%', '')) / 100;
            const l = Number(comps[2].replace('%', '')) / 100;
            const a = comps.length === 4 ? Number(comps[3]) : 1;
            // Convert HSL to RGB then to HSVA
            const { r, g, b } = hslToRgb(h, s, l);
            return rgbToHsva(r, g, b, a);
        }
    }
    return { h: 0, s: 0, v: 0, a: 1 };
}

const tkColorPickerCss = () => `@font-face{font-family:"Material Symbols Outlined";src:url("./assets/fonts/material-symbols/Material-Symbols-Outlined.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-outlined{font-family:"Material Symbols Outlined";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-outlined{font-variation-settings:"FILL" 0}.material-symbols-outlined.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Rounded";src:url("./assets/fonts/material-symbols/Material-Symbols-Rounded.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-rounded{font-family:"Material Symbols Rounded";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-rounded{font-variation-settings:"FILL" 0}.material-symbols-rounded.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Sharp";src:url("./assets/fonts/material-symbols/Material-Symbols-Sharp.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-sharp{font-family:"Material Symbols Sharp";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-sharp{font-variation-settings:"FILL" 0}.material-symbols-sharp.fill{font-variation-settings:"FILL" 1}:host{display:block}.tk-color-picker{display:inline-block;position:relative;width:100%;font-family:var(--family-body)}.tk-color-picker.tk-color-picker-inline{width:auto;display:flex}.tk-color-picker-panel{position:fixed;z-index:800;background:var(--static-light, #ffffff);border:1px solid var(--border-light, #e1e4ea);border-radius:16px;box-shadow:0px 8px 20px rgba(82, 88, 102, 0.08);display:flex;flex-direction:column;overflow:hidden;width:280px}.tk-color-picker-panel.tk-color-picker-panel-inline{position:relative;z-index:1;box-shadow:none;margin-top:0}.tk-color-picker-panel-header{display:flex;align-items:center;justify-content:space-between;padding:4px 12px}.tk-color-picker-panel-header .tk-color-picker-title{font-family:var(--desktop-body-m-base-font);font-size:var(--desktop-body-m-base-size, 16px);font-weight:400;line-height:var(--desktop-body-m-base-line-height, 24px);color:var(--text-darkest, #222530)}.tk-color-picker-panel-header.tk-color-picker-panel-header-basic{background:var(--static-light, #ffffff)}.tk-color-picker-panel-header.tk-color-picker-panel-header-divided{background:var(--static-light, #ffffff);border-bottom:var(--spacing-px, 1px) solid var(--border-light, #e1e4ea)}.tk-color-picker-panel-header.tk-color-picker-panel-header-light{background:var(--background-lightest, #f9fafc);border-bottom:var(--spacing-px, 1px) solid var(--border-light, #e1e4ea)}.tk-color-picker-panel-header.tk-color-picker-panel-header-dark{background:var(--neutral-800, #222530)}.tk-color-picker-panel-header.tk-color-picker-panel-header-dark .tk-color-picker-title{color:var(--static-white, #ffffff)}.tk-color-picker-panel-header.tk-color-picker-panel-header-primary{background:var(--primary-base, #c90019)}.tk-color-picker-panel-header.tk-color-picker-panel-header-primary .tk-color-picker-title{color:var(--static-white, #ffffff)}.tk-color-picker-panel-body{padding:8px;display:flex;flex-direction:column;gap:16px}.tk-color-picker-body-vertical{display:flex;flex-direction:column;gap:16px}.tk-color-picker-body-vertical .tk-color-picker-saturation-container{width:100%;height:264px}.tk-color-picker-body-vertical .tk-color-picker-controls-bottom{display:flex;flex-direction:column;gap:16px}.tk-color-picker-body-vertical .tk-color-picker-controls-bottom .tk-color-picker-eyedropper-row{display:flex;gap:8px;align-items:center}.tk-color-picker-body-vertical .tk-color-picker-controls-bottom .tk-color-picker-eyedropper-row .tk-color-picker-sliders{flex:1;display:flex;flex-direction:column;gap:8px}.tk-color-picker-body-horizontal{display:flex;gap:8px;height:264px}.tk-color-picker-body-horizontal .tk-color-picker-controls-side{display:flex;flex-direction:column;gap:16px;width:32px;align-items:center}.tk-color-picker-body-horizontal .tk-color-picker-controls-side .tk-color-picker-sliders-vertical-layout{flex:1;display:flex;flex-direction:row;gap:8px;width:100%;justify-content:center}.tk-color-picker-body-horizontal .tk-color-picker-saturation-wrapper{flex:1;height:100%}.tk-color-picker-body-horizontal .tk-color-picker-saturation-wrapper .tk-color-picker-saturation-container{width:100%;height:100%}.tk-color-picker-saturation-container{position:relative;border-radius:8px;border:1px solid var(--border-light, #e1e4ea);overflow:hidden;cursor:crosshair}.tk-color-picker-saturation-container .tk-color-picker-saturation-white{position:absolute;inset:0;background:linear-gradient(to right, #fff, rgba(255, 255, 255, 0))}.tk-color-picker-saturation-container .tk-color-picker-saturation-black{position:absolute;inset:0;background:linear-gradient(to top, #000, rgba(0, 0, 0, 0))}.tk-color-picker-saturation-container .tk-color-picker-saturation-pointer{position:absolute;width:14px;height:14px;border:2px solid white;border-radius:50%;transform:translate(-50%, -50%);box-shadow:0 0 2px rgba(0, 0, 0, 0.5);pointer-events:none}.tk-color-picker-preview-box{width:32px;height:32px;border-radius:8px;border:1px solid var(--border-light, #e1e4ea);flex-shrink:0}.tk-color-picker-slider{position:relative;height:12px;border-radius:24px;cursor:pointer;width:100%;border:1px solid var(--overlay-black-xlightest, rgba(0, 0, 0, 0.08))}.tk-color-picker-slider.tk-color-picker-hue{background:linear-gradient(to right, red 0%, #ff0 17%, lime 33%, cyan 50%, blue 67%, #f0f 83%, red 100%)}.tk-color-picker-slider.tk-color-picker-slider-vertical{width:12px;height:100%}.tk-color-picker-slider.tk-color-picker-slider-vertical.tk-color-picker-hue{background:linear-gradient(to bottom, red 0%, #ff0 17%, lime 33%, cyan 50%, blue 67%, #f0f 83%, red 100%)}.tk-color-picker-slider .tk-color-picker-alpha-bg{position:absolute;inset:0;border-radius:24px;background-image:linear-gradient(45deg, #ccc 25%, transparent 25%), linear-gradient(-45deg, #ccc 25%, transparent 25%), linear-gradient(45deg, transparent 75%, #ccc 75%), linear-gradient(-45deg, transparent 75%, #ccc 75%);background-size:8px 8px;background-position:0 0, 0 4px, 4px -4px, -4px 0}.tk-color-picker-slider .tk-color-picker-alpha-overlay{position:absolute;inset:0;border-radius:24px}.tk-color-picker-slider .tk-color-picker-slider-thumb{position:absolute;width:10px;height:10px;padding:4px;border-radius:50%;border:1px solid var(--border-light, #e1e4ea);background:var(--static-white, #fff);box-shadow:0 0 20px 3px var(--shadow-black-alpha-light, rgba(82, 88, 102, 0.04));transform:translate(-50%, -50%);pointer-events:none;display:flex;align-items:center;justify-content:center}.tk-color-picker-slider .tk-color-picker-slider-thumb .tk-color-picker-slider-thumb-inner{width:8px;height:8px;border-radius:50%;background:var(--blue-600, #326fd1)}.tk-color-picker-inputs{display:flex;gap:8px;align-items:center}.tk-color-picker-inputs .tk-color-picker-format-select{width:65px;flex-shrink:0}.tk-color-picker-inputs .tk-color-picker-input-fields{flex:1;display:flex;gap:4px;min-width:0}.tk-color-picker-inputs .tk-color-picker-input-fields tk-input{width:100%}.tk-color-picker-inputs .tk-color-picker-input-fields .tk-color-picker-rgb-inputs{flex:1;min-width:0;display:flex;gap:4px}.tk-color-picker-inputs .tk-color-picker-input-fields .tk-color-picker-rgb-inputs .tk-color-picker-rgb-input{flex:1;min-width:38px}.tk-color-picker-inputs .tk-color-picker-input-fields .tk-color-picker-hex-input{flex:1}.tk-color-picker-inputs .tk-color-picker-input-fields .tk-color-picker-alpha-input{width:56px;flex:0 0 56px}.tk-color-picker-presets{display:flex;flex-wrap:wrap;gap:4px;padding:2px}.tk-color-picker-presets .tk-color-picker-preset{width:20px;height:20px;border-radius:6px;cursor:pointer}.tk-color-picker-panel-footer{padding:8px;display:flex;justify-content:flex-end;align-items:center;gap:var(--card-footer-base-gap, 8px)}.tk-color-picker-panel-footer ::slotted([slot=footer-actions]){display:flex;align-items:center;gap:var(--card-footer-base-gap, 8px)}.tk-color-picker-panel-footer.tk-color-picker-panel-footer-basic{background:var(--static-light, #ffffff)}.tk-color-picker-panel-footer.tk-color-picker-panel-footer-divided{background:var(--static-light, #ffffff);border-top:var(--spacing-px, 1px) solid var(--border-light, #e1e4ea)}.tk-color-picker-panel-footer.tk-color-picker-panel-footer-light{background:var(--background-lightest, #f9fafc);border-top:var(--spacing-px, 1px) solid var(--border-light, #e1e4ea)}`;

const TkColorPicker = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.tkChange = index.createEvent(this, "tk-change", 7);
        this.tkOpen = index.createEvent(this, "tk-open", 7);
        this.tkClose = index.createEvent(this, "tk-close", 7);
        this.tkApply = index.createEvent(this, "tk-apply", 7);
        this.tkCancel = index.createEvent(this, "tk-cancel", 7);
        this.uniqueId = v4.v4();
        this.isDragging = false;
        this.tempInputVal = null;
        this.pendingValue = null;
        this.isOpen = false;
        this.internalHSVA = { h: 0, s: 0, v: 0, a: 1 };
        this.currentFormat = 'hex';
        this.hasHeaderSlot = false;
        this.hasHeaderActionsSlot = false;
        this.hasFooterSlot = false;
        this.hasFooterActionsSlot = false;
        this.triggerInputValue = '';
        this.isTriggerInputFocused = false;
        /**
         * The current color value (supports HEX and RGB formats)
         */
        this.value = '';
        /**
         * Displays a red asterisk (*) next to the label for visual emphasis.
         * @defaultValue false
         */
        this.showAsterisk = false;
        /**
         * If `true`, the user cannot modify the value.
         * @defaultValue false
         */
        this.readonly = false;
        /**
         * Indicates whether the input is in an invalid state
         * @defaultValue false
         */
        this.invalid = false;
        /**
         * Disables the color picker
         * @defaultValue false
         */
        this.disabled = false;
        /**
         * Sets size for the component.
         * @defaultValue base
         */
        this.size = 'base';
        /**
         * Displays the picker inline without trigger
         * @defaultValue false
         */
        this.inline = false;
        /**
         * Panel layout orientation
         * @defaultValue 'vertical'
         */
        this.orientation = 'vertical';
        /**
         * Color format for display and output
         * @defaultValue 'hex'
         */
        this.format = 'hex';
        /**
         * Array of preset color values
         */
        this.presets = ['#326FD1', '#C79807', '#A45E3C', '#119C8D', '#EDBBA3', '#ABC9FB', '#D0E1FD', '#FF6259', '#717784', '#85B2F9', '#EAD6FD'];
        /**
         * Controls whether the header is shown
         * @defaultValue false
         */
        this.showHeader = false;
        /**
         * Shows/hides the alpha slider
         * @defaultValue true
         */
        this.showAlphaSlider = true;
        /**
         * Shows/hides the preset colors section
         * @defaultValue true
         */
        this.showPresets = true;
        /**
         * Shows/hides the format selector
         * @defaultValue true
         */
        this.showFormatSelector = true;
        /**
         * The type of the header styling
         * @defaultValue 'basic'
         */
        this.headerType = 'basic';
        /**
         * The type of the footer styling
         * @defaultValue 'basic'
         */
        this.footerType = 'basic';
        /**
         * Prevents the panel from being dismissed by clicking outside.
         * Use with footer actions (Apply/Cancel buttons) for controlled closing.
         * @defaultValue false
         */
        this.preventDismiss = false;
        /**
         * Controls whether the close button is shown in the header.
         * Set to false when using footer actions for controlled closing.
         * @defaultValue false
         */
        this.showCloseButton = false;
        this.handleDocumentMouseMove = (e) => {
            if (this.isDragging && this.currentDragHandler) {
                e.preventDefault();
                this.currentDragHandler(e);
            }
        };
        this.handleDocumentMouseUp = () => {
            this.stopDragging();
        };
        this.handleClickOutside = (_) => {
            this.handleApply();
        };
        this.handleTriggerClick = (e) => {
            e.stopPropagation();
            if (this.disabled || this.readonly || this.inline)
                return;
            if (!this.isOpen) {
                this.pendingValue = Object.assign({}, this.internalHSVA);
                this.isOpen = true;
            }
        };
        this.handleSaturationPointer = (e) => {
            if (!this.saturationAreaRef)
                return;
            const rect = this.saturationAreaRef.getBoundingClientRect();
            const x = Math.max(0, Math.min(1, (e.clientX - rect.left) / rect.width));
            const y = Math.max(0, Math.min(1, (e.clientY - rect.top) / rect.height));
            this.updateColor({
                s: Math.round(x * 100),
                v: Math.round((1 - y) * 100),
            });
        };
        this.handleSaturationMouseDown = (e) => {
            this.saturationAreaRef = e.currentTarget;
            this.startDragging(ev => {
                this.handleSaturationPointer(ev);
            }, e);
        };
        this.handleHueChange = (e) => {
            if (!this.hueSliderRef)
                return;
            const rect = this.hueSliderRef.getBoundingClientRect();
            const isVerticalSlider = this.hueSliderRef.classList.contains('tk-color-picker-slider-vertical');
            let percent;
            if (isVerticalSlider) {
                percent = Math.max(0, Math.min(1, (e.clientY - rect.top) / rect.height));
            }
            else {
                percent = Math.max(0, Math.min(1, (e.clientX - rect.left) / rect.width));
            }
            this.updateColor({ h: percent * 360 });
        };
        this.handleHueMouseDown = (e) => {
            this.hueSliderRef = e.currentTarget;
            this.startDragging(this.handleHueChange, e);
        };
        this.handleAlphaChange = (e) => {
            if (!this.alphaSliderRef)
                return;
            const rect = this.alphaSliderRef.getBoundingClientRect();
            const isVerticalSlider = this.alphaSliderRef.classList.contains('tk-color-picker-slider-vertical');
            let percent;
            if (isVerticalSlider) {
                percent = Math.max(0, Math.min(1, (e.clientY - rect.top) / rect.height));
            }
            else {
                percent = Math.max(0, Math.min(1, (e.clientX - rect.left) / rect.width));
            }
            this.updateColor({ a: percent });
        };
        this.handleAlphaMouseDown = (e) => {
            this.alphaSliderRef = e.currentTarget;
            this.startDragging(this.handleAlphaChange, e);
        };
        this.handleRgbChange = (component, value) => {
            const val = Math.max(0, Math.min(255, parseInt(value) || 0));
            const { r, g, b } = hsvaToRgb(this.internalHSVA.h, this.internalHSVA.s, this.internalHSVA.v);
            let newR = r;
            let newG = g;
            let newB = b;
            if (component === 'r')
                newR = val;
            if (component === 'g')
                newG = val;
            if (component === 'b')
                newB = val;
            const newHsva = rgbToHsva(newR, newG, newB, this.internalHSVA.a);
            // Preserve hue when color becomes achromatic (gray/black/white)
            if (newHsva.s === 0 || newHsva.v === 0) {
                newHsva.h = this.internalHSVA.h;
            }
            this.internalHSVA = newHsva;
        };
        this.handleInputBlur = (type) => {
            if (this.tempInputVal === null)
                return;
            const val = String(this.tempInputVal);
            this.tempInputVal = null;
            if (type === 'hex') {
                const newHsva = parseColorToHsva('#' + val);
                // Preserve hue when color becomes achromatic
                if (newHsva.s === 0 || newHsva.v === 0) {
                    newHsva.h = this.internalHSVA.h;
                }
                this.internalHSVA = newHsva;
            }
            else if (type === 'a') {
                const a = Math.max(0, Math.min(100, Number(val))) / 100;
                this.updateColor({ a });
            }
            else {
                this.handleRgbChange(type, val);
            }
        };
        this.handleEyeDropper = async () => {
            if (!window.EyeDropper)
                return;
            try {
                const eyeDropper = new window.EyeDropper();
                const result = await eyeDropper.open();
                this.internalHSVA = parseColorToHsva(result.sRGBHex);
            }
            catch (_a) {
                // User canceled or browser does not support
            }
        };
        this.handleFormatChange = (e) => {
            this.currentFormat = e.detail;
            e.stopPropagation();
        };
        this.handleApply = () => {
            const colorValue = hsvaToCss(this.internalHSVA, this.currentFormat);
            this.value = colorValue;
            this.pendingValue = null;
            this.tkApply.emit(colorValue);
            this.tkChange.emit(colorValue);
            this.isOpen = false;
        };
        this.handleCancel = () => {
            if (this.pendingValue) {
                this.internalHSVA = this.pendingValue;
                this.pendingValue = null;
            }
            this.tkCancel.emit();
            this.isOpen = false;
        };
        this.handleCloseClick = () => {
            this.handleCancel();
        };
        this.handleTriggerInputFocus = () => {
            this.isTriggerInputFocused = true;
            // Only set triggerInputValue if there's an actual value
            // If value is empty, keep triggerInputValue empty so user can type from scratch
            this.triggerInputValue = this.value || '';
        };
        this.handleTriggerInputBlur = () => {
            this.isTriggerInputFocused = false;
            // Apply the typed value on blur
            this.applyTriggerInputValue();
        };
        this.handleTriggerInputChange = (e) => {
            const input = e.detail;
            this.triggerInputValue = input.value;
        };
        this.handleTriggerInputKeyDown = (e) => {
            if (e.key === 'Enter') {
                e.preventDefault();
                this.applyTriggerInputValue();
            }
            else if (e.key === 'Escape') {
                e.preventDefault();
                // Reset to current color value
                const colorCss = hsvaToCss(this.internalHSVA, this.currentFormat);
                this.triggerInputValue = colorCss;
            }
        };
    }
    isOpenChanged(newVal) {
        if (newVal) {
            this.tkOpen.emit();
        }
        else {
            this.tkClose.emit();
        }
    }
    valueChanged(newVal) {
        if (this.isDragging)
            return;
        this.internalHSVA = parseColorToHsva(newVal);
    }
    disabledChanged(newValue) {
        if (newValue)
            this.isOpen = false;
    }
    componentWillLoad() {
        this.internalHSVA = parseColorToHsva(this.value || '#000000');
        this.currentFormat = this.format === 'hex' || this.format === 'rgba' ? this.format : 'hex';
        this.hasHeaderSlot = !!this.el.querySelector(':scope > [slot="header"]');
        this.hasHeaderActionsSlot = !!this.el.querySelector(':scope > [slot="header-actions"]');
        this.hasFooterSlot = !!this.el.querySelector(':scope > [slot="footer"]');
        this.hasFooterActionsSlot = !!this.el.querySelector(':scope > [slot="footer-actions"]');
    }
    componentDidLoad() {
        var _a;
        const tkInputArea = (_a = this.inputRef) === null || _a === void 0 ? void 0 : _a.querySelector('.tk-input');
        this.clickOutsideMixin = new clickoutsideMixin.ClickOutsideMixin({
            referenceElement: tkInputArea !== null && tkInputArea !== void 0 ? tkInputArea : this.el,
            handler: this.handleClickOutside,
            disabled: !this.isOpen || this.disabled || this.readonly || this.inline || this.preventDismiss,
        });
    }
    componentDidUpdate() {
        var _a, _b, _c;
        (_a = this.clickOutsideMixin) === null || _a === void 0 ? void 0 : _a.updateConfig({
            disabled: !this.isOpen || this.disabled || this.readonly || this.inline || this.preventDismiss,
            ignoredElements: this.panelRef ? [this.panelRef] : [],
        });
        if (this.isOpen && this.inputRef && this.panelRef) {
            // Clean up old floating UI listeners before setting up new ones
            (_b = this.cleanup) === null || _b === void 0 ? void 0 : _b.call(this);
            this.updatePosition();
        }
        else {
            // Remove floating UI listeners when dropdown closes
            (_c = this.cleanup) === null || _c === void 0 ? void 0 : _c.call(this);
            // Clear reference to allow garbage collection
            this.cleanup = null;
        }
    }
    disconnectedCallback() {
        var _a, _b;
        (_a = this.clickOutsideMixin) === null || _a === void 0 ? void 0 : _a.disconnectedCallback();
        // Clean up floating UI listeners on component unmount
        (_b = this.cleanup) === null || _b === void 0 ? void 0 : _b.call(this);
        // Clear reference to allow garbage collection
        this.cleanup = null;
        this.stopDragging();
    }
    /**
     * Opens the color picker panel
     */
    async open() {
        if (!this.disabled && !this.readonly && !this.inline) {
            this.pendingValue = Object.assign({}, this.internalHSVA);
            this.isOpen = true;
        }
    }
    /**
     * Closes the color picker panel
     */
    async close() {
        this.isOpen = false;
    }
    /**
     * Gets the current color value in the specified format
     * @param format - Output format (defaults to current format)
     */
    async getValue(format) {
        return hsvaToCss(this.internalHSVA, format || this.currentFormat);
    }
    /**
     * Applies the current color selection and closes the panel
     * Use this method in custom footer actions
     */
    async apply() {
        this.handleApply();
    }
    /**
     * Cancels the color selection, reverts to previous value, and closes the panel
     * Use this method in custom footer actions
     */
    async cancel() {
        this.handleCancel();
    }
    startDragging(handler, initialEvent) {
        this.isDragging = true;
        this.currentDragHandler = handler;
        handler(initialEvent);
        document.addEventListener('mousemove', this.handleDocumentMouseMove);
        document.addEventListener('mouseup', this.handleDocumentMouseUp);
    }
    stopDragging() {
        this.isDragging = false;
        this.currentDragHandler = undefined;
        document.removeEventListener('mousemove', this.handleDocumentMouseMove);
        document.removeEventListener('mouseup', this.handleDocumentMouseUp);
    }
    updatePosition() {
        const inputEl = this.inputRef.querySelector('.tk-input');
        this.cleanup = positionUtils.floatingElementAutoUpdate(inputEl, this.panelRef, undefined, {
            placement: 'bottom-end',
        });
    }
    /**
     * Updates internal HSVA state with partial values
     */
    updateColor(partial) {
        this.internalHSVA = Object.assign(Object.assign({}, this.internalHSVA), partial);
    }
    applyTriggerInputValue() {
        if (!this.triggerInputValue)
            return;
        const parsed = parseColorToHsva(this.triggerInputValue);
        // Check if parsed successfully (not all zeros unless input was black)
        if (parsed.h !== 0 || parsed.s !== 0 || parsed.v !== 0 || this.triggerInputValue.toLowerCase().includes('000')) {
            this.internalHSVA = parsed;
            const newValue = hsvaToCss(this.internalHSVA, this.currentFormat);
            this.value = newValue;
            this.tkChange.emit(newValue);
        }
    }
    handlePresetSelect(color) {
        this.internalHSVA = parseColorToHsva(color);
    }
    createEyedropperButton() {
        return (index.h("tk-button", { variant: "neutral", type: "outlined", size: "small", icon: "colorize", "onTk-click": this.handleEyeDropper, disabled: this.disabled, dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'eyedropper-button') }));
    }
    createColorPreview() {
        return (index.h("div", { class: "tk-color-picker-preview-box", style: { backgroundColor: hsvaToCss(this.internalHSVA, this.currentFormat) }, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'preview') }));
    }
    createSaturation() {
        const { h: hue, s, v } = this.internalHSVA;
        return (index.h("div", { class: "tk-color-picker-saturation-container", style: { backgroundColor: `hsl(${hue}, 100%, 50%)` }, onMouseDown: this.handleSaturationMouseDown, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'saturation') }, index.h("div", { class: "tk-color-picker-saturation-white", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'white') }), index.h("div", { class: "tk-color-picker-saturation-black", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'black') }), index.h("div", { class: "tk-color-picker-saturation-pointer", style: {
                left: `${s}%`,
                top: `${100 - v}%`,
            }, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'pointer') })));
    }
    createSliders() {
        const isHorizontalLayout = this.orientation === 'horizontal';
        const sliderClass = index$1.classNames('tk-color-picker-slider', {
            'tk-color-picker-slider-vertical': isHorizontalLayout,
        });
        const huePercent = (this.internalHSVA.h / 360) * 100;
        return (index.h("div", { class: index$1.classNames('tk-color-picker-sliders', { 'tk-color-picker-sliders-vertical-layout': isHorizontalLayout }), "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'sliders') }, index.h("div", { class: `${sliderClass} tk-color-picker-hue`, ref: el => (this.hueSliderRef = el), onMouseDown: this.handleHueMouseDown, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'hue') }, index.h("div", { class: "tk-color-picker-slider-thumb", style: isHorizontalLayout ? { top: `${huePercent}%`, left: '50%' } : { left: `${huePercent}%`, top: '50%' }, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'hue-thumb') }, index.h("div", { class: "tk-color-picker-slider-thumb-inner", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'hue-inner-thumb') }))), this.showAlphaSlider && (index.h("div", { class: sliderClass, ref: el => (this.alphaSliderRef = el), onMouseDown: this.handleAlphaMouseDown, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'alpha') }, index.h("div", { class: "tk-color-picker-alpha-bg", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'slider') }), index.h("div", { class: "tk-color-picker-alpha-overlay", style: { background: `linear-gradient(${isHorizontalLayout ? 'to bottom' : 'to right'}, transparent, ${hsvaToHex(this.internalHSVA)})` }, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'overlay') }), index.h("div", { class: "tk-color-picker-slider-thumb", style: isHorizontalLayout ? { top: `${this.internalHSVA.a * 100}%`, left: '50%' } : { left: `${this.internalHSVA.a * 100}%`, top: '50%' }, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'alpha-thumb') }, index.h("div", { class: "tk-color-picker-slider-thumb-inner", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'alpha-inner-thumb') }))))));
    }
    createAlphaInput() {
        if (!this.showAlphaSlider)
            return null;
        return (index.h("tk-input", { class: "tk-color-picker-alpha-input", mode: "number", size: "small", min: "0", max: "100", value: (this.internalHSVA.a * 100).toFixed(0), disabled: this.disabled, "onTk-change": (e) => (this.tempInputVal = e.detail), "onTk-blur": () => this.handleInputBlur('a'), dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'alpha-input') }));
    }
    createInputs() {
        const { r, g, b } = hsvaToRgb(this.internalHSVA.h, this.internalHSVA.s, this.internalHSVA.v);
        return (index.h("div", { class: "tk-color-picker-inputs", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'inputs') }, this.showFormatSelector && (index.h("tk-select", { class: "tk-color-picker-format-select", size: "small", value: this.currentFormat, optionValueKey: "value", disabled: this.disabled, options: [
                { value: 'hex', label: 'HEX' },
                { value: 'rgba', label: 'RGB' },
            ], "onTk-change": this.handleFormatChange, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'format-select') })), index.h("div", { class: "tk-color-picker-input-fields", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'input-fields') }, this.currentFormat === 'hex' && (index.h(index.Fragment, null, index.h("tk-input", { class: "tk-color-picker-hex-input", size: "small", icon: "tag", iconPosition: "left", value: hsvaToHex(this.internalHSVA).replace('#', ''), disabled: this.disabled, "onTk-change": (e) => (this.tempInputVal = e.detail), "onTk-blur": () => this.handleInputBlur('hex'), dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'hex-input') }), this.createAlphaInput())), this.currentFormat === 'rgba' && (index.h("div", { class: "tk-color-picker-rgb-inputs", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'rgb') }, index.h("tk-input", { class: "tk-color-picker-rgb-input", size: "small", value: r.toString(), disabled: this.disabled, "onTk-change": (e) => (this.tempInputVal = e.detail), "onTk-blur": () => this.handleInputBlur('r'), dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'red-input') }), index.h("tk-input", { class: "tk-color-picker-rgb-input", size: "small", value: g.toString(), disabled: this.disabled, "onTk-change": (e) => (this.tempInputVal = e.detail), "onTk-blur": () => this.handleInputBlur('g'), dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'green-input') }), index.h("tk-input", { class: "tk-color-picker-rgb-input", size: "small", value: b.toString(), disabled: this.disabled, "onTk-change": (e) => (this.tempInputVal = e.detail), "onTk-blur": () => this.handleInputBlur('b'), dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'blue-input') }), this.createAlphaInput())))));
    }
    createPresets() {
        var _a;
        if (!this.showPresets || !((_a = this.presets) === null || _a === void 0 ? void 0 : _a.length))
            return null;
        return (index.h("div", { class: "tk-color-picker-presets", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'presets') }, this.presets.map((color, index$1) => (index.h("div", { class: "tk-color-picker-preset", style: { backgroundColor: color }, onClick: () => this.handlePresetSelect(color), "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'preset', index$1.toString()) })))));
    }
    createFooter() {
        if (this.hasFooterSlot) {
            return index.h("slot", { name: "footer" });
        }
        if (this.hasFooterActionsSlot) {
            const footerClasses = index$1.classNames('tk-color-picker-panel-footer', `tk-color-picker-panel-footer-${this.footerType}`);
            return (index.h("div", { class: footerClasses, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'footer-actions') }, index.h("slot", { name: "footer-actions" })));
        }
        // No footer by default - footer is entirely slot-based
        return null;
    }
    createPanelHeader() {
        if (!this.showHeader)
            return null;
        if (this.hasHeaderSlot) {
            return index.h("slot", { name: "header" });
        }
        else {
            const headerClasses = index$1.classNames('tk-color-picker-panel-header', `tk-color-picker-panel-header-${this.headerType}`);
            return (index.h("div", { class: headerClasses, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'header') }, index.h("span", { class: "tk-color-picker-title", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'title') }, this.header), this.hasHeaderActionsSlot ? (index.h("slot", { name: "header-actions" })) : (!this.inline &&
                this.showCloseButton && (index.h("tk-button", { variant: "neutral", type: "text", size: "small", icon: "close", onClick: this.handleCloseClick, dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'close-button') })))));
        }
    }
    createPanelBody() {
        if (this.orientation === 'horizontal') {
            return (index.h(index.Fragment, null, index.h("div", { class: "tk-color-picker-body-horizontal", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'horizontal') }, index.h("div", { class: "tk-color-picker-controls-side", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'controls') }, this.createColorPreview(), this.createSliders(), this.createEyedropperButton()), index.h("div", { class: "tk-color-picker-saturation-wrapper", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'saturation-wrapper') }, this.createSaturation())), this.createInputs(), this.createPresets()));
        }
        return (index.h(index.Fragment, null, index.h("div", { class: "tk-color-picker-body-vertical", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'vertical') }, this.createSaturation(), index.h("div", { class: "tk-color-picker-controls-bottom", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'controls') }, index.h("div", { class: "tk-color-picker-eyedropper-row", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'eyedropper-row') }, this.createEyedropperButton(), this.createSliders(), this.createColorPreview()), this.createInputs())), this.createPresets()));
    }
    renderInput() {
        if (this.inline)
            return null;
        const displayValue = this.isTriggerInputFocused ? this.triggerInputValue : this.value;
        const iconColor = this.value || '#000000';
        return (index.h("tk-input", { ref: el => (this.inputRef = el), label: this.label, readonly: this.readonly, disabled: this.disabled, invalid: this.invalid, error: this.error, hint: this.hint, showAsterisk: this.showAsterisk, name: this.name, size: this.size, placeholder: this.placeholder, icon: {
                left: {
                    name: 'circle',
                    color: iconColor,
                    style: 'outlined',
                    fill: true,
                },
                right: this.isOpen ? 'keyboard_arrow_up' : 'keyboard_arrow_down',
            }, value: displayValue, "aria-describedby": "dropdown", "aria-expanded": this.isOpen, onClick: this.handleTriggerClick, "onTk-focus": this.handleTriggerInputFocus, "onTk-blur": this.handleTriggerInputBlur, "onTk-change": this.handleTriggerInputChange, onKeyDown: this.handleTriggerInputKeyDown, dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'trigger-input') }));
    }
    renderPanel() {
        const panelCls = index$1.classNames('tk-color-picker-panel', {
            'tk-color-picker-panel-inline': this.inline,
            'tk-color-picker-panel-horizontal': this.orientation === 'horizontal',
        });
        return (index.h("div", { class: panelCls, ref: el => (this.panelRef = el), "data-tk-id": this.uniqueId, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'panel') }, this.createPanelHeader(), index.h("div", { class: "tk-color-picker-panel-body", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'body') }, this.createPanelBody()), this.createFooter()));
    }
    render() {
        const rootClasses = index$1.classNames('tk-color-picker', {
            'tk-color-picker-inline': this.inline,
            'tk-color-picker-horizontal': this.orientation === 'horizontal',
        });
        return (index.h("div", { key: 'dedcdb5205a6aa90c5d8c83791d7dbaec2d51678', class: rootClasses, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'container') }, this.renderInput(), (this.isOpen || this.inline) && this.renderPanel()));
    }
    get el() { return index.getElement(this); }
    static get watchers() { return {
        "isOpen": [{
                "isOpenChanged": 0
            }],
        "value": [{
                "valueChanged": 0
            }],
        "disabled": [{
                "disabledChanged": 0
            }]
    }; }
};
TkColorPicker.style = tkColorPickerCss();

const tkDividerCss = () => `@font-face{font-family:"Material Symbols Outlined";src:url("./assets/fonts/material-symbols/Material-Symbols-Outlined.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-outlined{font-family:"Material Symbols Outlined";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-outlined{font-variation-settings:"FILL" 0}.material-symbols-outlined.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Rounded";src:url("./assets/fonts/material-symbols/Material-Symbols-Rounded.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-rounded{font-family:"Material Symbols Rounded";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-rounded{font-variation-settings:"FILL" 0}.material-symbols-rounded.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Sharp";src:url("./assets/fonts/material-symbols/Material-Symbols-Sharp.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-sharp{font-family:"Material Symbols Sharp";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-sharp{font-variation-settings:"FILL" 0}.material-symbols-sharp.fill{font-variation-settings:"FILL" 1}:host{display:block}.tk-divider.tk-divider-horizontal{margin:16px 0;border-top:var(--spacing-px) solid var(--background-light)}.tk-divider.tk-divider-horizontal:has(.tk-divider-content){border-top:0 !important}.tk-divider.tk-divider-horizontal .tk-divider-content{display:flex;align-items:center;color:var(--text-base);font-family:var(--desktop-body-2xs-font);font-size:var(--desktop-body-2xs-size, 11px);font-style:normal;font-weight:400;line-height:var(--line-height-xxs-normal, 16px)}.tk-divider.tk-divider-horizontal .tk-divider-content::before{flex:1 1 0%;height:1px;content:"";border-top:var(--spacing-px) solid var(--background-light);margin-right:8px}.tk-divider.tk-divider-horizontal .tk-divider-content::after{flex:1 1 0%;content:"";border-top:var(--spacing-px) solid var(--background-light);margin-left:8px}.tk-divider.tk-divider-vertical{position:relative;display:flex;justify-content:center;min-height:100%;margin:0 16px}.tk-divider.tk-divider-vertical::before{content:"";position:absolute;display:block;height:100%;inset-block-start:0;inset-inline-start:50%;border-inline-start:var(--spacing-px) solid var(--background-light);box-sizing:border-box}`;

const TkDivider = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        /**
         * Controls the orientation of the divider component.
         * @defaultValue horizontal
         */
        this.orientation = 'horizontal';
    }
    componentWillLoad() {
        this.hasDefaultSlot = Array.from(this.el.childNodes).some(node => { var _a; return (node.nodeType === Node.ELEMENT_NODE && !node.hasAttribute('slot')) || (node.nodeType === Node.TEXT_NODE && ((_a = node.textContent) === null || _a === void 0 ? void 0 : _a.trim().length) > 0); });
    }
    getRootStyles() {
        const styles = {};
        if (this.my) {
            const space = typeof this.my === 'number' ? `${this.my}px` : this.my;
            styles.marginTop = space;
            styles.marginBottom = space;
        }
        if (this.mx) {
            const space = typeof this.mx === 'number' ? `${this.mx}px` : this.mx;
            styles.marginLeft = space;
            styles.marginRight = space;
        }
        return styles;
    }
    render() {
        return (index.h("div", { key: '97acde09e55e7023ca88acd4defbfffaae305734', class: index$1.classNames('tk-divider', `tk-divider-${this.orientation}`), role: "separator", "aria-orientation": this.orientation, style: this.getRootStyles(), "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'container') }, this.hasDefaultSlot && (index.h("div", { key: '691294120af2399ecfecb9529c3acc4f669e4d72', class: "tk-divider-content", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'content') }, index.h("slot", { key: '8364eafbd690450674ca08f50acbf55d8404f742' })))));
    }
    get el() { return index.getElement(this); }
};
TkDivider.style = tkDividerCss();

const tkIconCss = () => `@font-face{font-family:"Material Symbols Outlined";src:url("./assets/fonts/material-symbols/Material-Symbols-Outlined.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-outlined{font-family:"Material Symbols Outlined";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-outlined{font-variation-settings:"FILL" 0}.material-symbols-outlined.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Rounded";src:url("./assets/fonts/material-symbols/Material-Symbols-Rounded.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-rounded{font-family:"Material Symbols Rounded";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-rounded{font-variation-settings:"FILL" 0}.material-symbols-rounded.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Sharp";src:url("./assets/fonts/material-symbols/Material-Symbols-Sharp.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-sharp{font-family:"Material Symbols Sharp";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-sharp{font-variation-settings:"FILL" 0}.material-symbols-sharp.fill{font-variation-settings:"FILL" 1}tk-icon{display:inline-flex}.tk-icon[class*=material-symbols]{display:inline-flex;align-items:center;justify-content:center;text-align:center;line-height:1}.tk-icon.tk-icon-xxsmall{font-size:var(--sign-xxsmall-icon-size)}.tk-icon.tk-icon-xsmall{font-size:var(--sign-xsmall-icon-size)}.tk-icon.tk-icon-small{font-size:var(--sign-small-icon-size)}.tk-icon.tk-icon-base{font-size:var(--sign-base-icon-size)}.tk-icon.tk-icon-medium{font-size:var(--sign-base-icon-size)}.tk-icon.tk-icon-large{font-size:var(--sign-large-icon-size)}.tk-icon.tk-icon-xlarge{font-size:var(--sign-xlarge-icon-size)}.tk-icon.tk-icon-xxlarge{font-size:var(--sign-2xlarge-icon-size)}.tk-icon.tk-icon-primary{color:var(--primary-base)}.tk-icon.tk-icon-secondary{color:var(--secondary-base)}.tk-icon.tk-icon-neutral{color:var(--background-base)}.tk-icon.tk-icon-info{color:var(--states-info-dark)}.tk-icon.tk-icon-success{color:var(--states-success-dark)}.tk-icon.tk-icon-danger{color:var(--states-danger-dark)}.tk-icon.tk-icon-warning{color:var(--states-warning-dark)}.tk-icon.tk-icon-white{color:var(--secondary-sub-base)}.tk-icon.tk-icon-sign{background-color:var(--background-lightest);border:1px solid var(--border-sub-base);box-sizing:border-box;overflow:hidden}.tk-icon.tk-icon-sign.tk-icon-xxsmall{width:calc(var(--sign-xxsmall-icon-size) + var(--sign-xxsmall-h-padding) * 2);height:calc(var(--sign-xxsmall-icon-size) + var(--sign-xxsmall-v-padding) * 2);border-radius:var(--sign-xxsmall-radius);padding:var(--sign-xxsmall-v-padding) var(--sign-xxsmall-h-padding)}.tk-icon.tk-icon-sign.tk-icon-xsmall{width:calc(var(--sign-xsmall-icon-size) + var(--sign-xsmall-h-padding) * 2);height:calc(var(--sign-xsmall-icon-size) + var(--sign-xsmall-v-padding) * 2);border-radius:var(--sign-xsmall-radius);padding:var(--sign-xsmall-v-padding) var(--sign-xsmall-h-padding)}.tk-icon.tk-icon-sign.tk-icon-small{width:calc(var(--sign-small-icon-size) + var(--sign-small-h-padding) * 2);height:calc(var(--sign-small-icon-size) + var(--sign-small-v-padding) * 2);border-radius:var(--sign-small-radius);padding:var(--sign-small-v-padding) var(--sign-small-h-padding)}.tk-icon.tk-icon-sign.tk-icon-base{width:calc(var(--sign-base-icon-size) + var(--sign-base-h-padding) * 2);height:calc(var(--sign-base-icon-size) + var(--sign-base-v-padding) * 2);border-radius:var(--sign-base-radius);padding:var(--sign-base-v-padding) var(--sign-base-h-padding)}.tk-icon.tk-icon-sign.tk-icon-medium{width:calc(var(--sign-base-icon-size) + var(--sign-base-h-padding) * 2);height:calc(var(--sign-base-icon-size) + var(--sign-base-v-padding) * 2);border-radius:var(--sign-base-radius);padding:var(--sign-base-v-padding) var(--sign-base-h-padding)}.tk-icon.tk-icon-sign.tk-icon-large{width:calc(var(--sign-large-icon-size) + var(--sign-large-h-padding) * 2);height:calc(var(--sign-large-icon-size) + var(--sign-large-v-padding) * 2);border-radius:var(--sign-large-radius);padding:var(--sign-large-v-padding) var(--sign-large-h-padding)}.tk-icon.tk-icon-sign.tk-icon-xlarge{width:calc(var(--sign-xlarge-icon-size) + var(--sign-xlarge-h-padding) * 2);height:calc(var(--sign-xlarge-icon-size) + var(--sign-xlarge-v-padding) * 2);border-radius:var(--sign-xlarge-radius);padding:var(--sign-xlarge-v-padding) var(--sign-xlarge-h-padding)}.tk-icon.tk-icon-sign.tk-icon-xxlarge{width:calc(var(--sign-2xlarge-icon-size) + var(--sign-2xlarge-h-padding) * 2);height:calc(var(--sign-2xlarge-icon-size) + var(--sign-2xlarge-v-padding) * 2);border-radius:var(--sign-2xlarge-radius);padding:var(--sign-2xlarge-v-padding) var(--sign-2xlarge-h-padding)}.tk-icon.tk-icon-sign.tk-icon-primary{border-color:var(--primary-sub-base)}.tk-icon.tk-icon-sign.tk-icon-secondary{border-color:var(--secondary-sub-base)}.tk-icon.tk-icon-sign.tk-icon-neutral{border-color:var(--background-sub-base)}.tk-icon.tk-icon-sign.tk-icon-info{border-color:var(--states-info-sub-base)}.tk-icon.tk-icon-sign.tk-icon-success{border-color:var(--states-success-sub-base)}.tk-icon.tk-icon-sign.tk-icon-danger{border-color:var(--states-danger-sub-base)}.tk-icon.tk-icon-sign.tk-icon-warning{border-color:var(--states-warning-sub-base)}.tk-icon.tk-icon-sign.tk-icon-white{border-color:var(--secondary-light)}`;

const TkIcon = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        /**
         * Controls whether the icon is shown as a sign (previously 'card' type)
         * @defaultValue false
         */
        this.sign = false;
        /**
         * The variant of the icon.
         * @defaultValue 'primary'
         */
        this.variant = 'primary';
        /**
         * Sets size for the component.
         * @defaultValue 'base'
         */
        this.size = 'base';
        /**
         * Specifies the type of the icon to be displayed.
         */
        this.iconType = 'outlined';
        /**
         * The HTML tag to use for the icon element.
         * @defaultValue 'i'
         */
        this.iconTag = 'i';
        this.getIconStyles = () => {
            const style = {};
            if (this.color) {
                style.color = this.color;
            }
            else if (this.iconColor) {
                style.color = this.iconColor;
            }
            if (this.sign) {
                if (this.borderColor) {
                    style.borderColor = this.borderColor;
                }
                if (this.backgroundColor) {
                    style.backgroundColor = this.backgroundColor;
                }
            }
            return { style };
        };
    }
    render() {
        const iconClasses = index$1.classNames(`material-symbols-${this.iconType}`, 'tk-icon', this.variant && `tk-icon-${this.variant}`, `tk-icon-${this.size}`, {
            'tk-icon-sign': this.sign,
            'fill': this.fill,
        });
        const iconProps = Object.assign(Object.assign({ 'class': iconClasses }, this.getIconStyles()), { 'data-testid': testIdUtils.getDataTestId(this.dataTestid, 'content') });
        const IconTag = this.iconTag;
        const iconElement = index.h(IconTag, Object.assign({ key: '9e315ca03e832dfefde4b88f904f0eb9743d5bd9' }, iconProps), this.icon);
        return iconElement;
    }
    get el() { return index.getElement(this); }
};
TkIcon.style = tkIconCss();

var commonjsGlobal = typeof window !== 'undefined' ? window : typeof global !== 'undefined' ? global : typeof self !== 'undefined' ? self : {};

var NumeralFormatter = function (numeralDecimalMark,
                                 numeralIntegerScale,
                                 numeralDecimalScale,
                                 numeralThousandsGroupStyle,
                                 numeralPositiveOnly,
                                 stripLeadingZeroes,
                                 prefix,
                                 signBeforePrefix,
                                 tailPrefix,
                                 delimiter) {
    var owner = this;

    owner.numeralDecimalMark = numeralDecimalMark || '.';
    owner.numeralIntegerScale = numeralIntegerScale > 0 ? numeralIntegerScale : 0;
    owner.numeralDecimalScale = numeralDecimalScale >= 0 ? numeralDecimalScale : 2;
    owner.numeralThousandsGroupStyle = numeralThousandsGroupStyle || NumeralFormatter.groupStyle.thousand;
    owner.numeralPositiveOnly = !!numeralPositiveOnly;
    owner.stripLeadingZeroes = stripLeadingZeroes !== false;
    owner.prefix = (prefix || prefix === '') ? prefix : '';
    owner.signBeforePrefix = !!signBeforePrefix;
    owner.tailPrefix = !!tailPrefix;
    owner.delimiter = (delimiter || delimiter === '') ? delimiter : ',';
    owner.delimiterRE = delimiter ? new RegExp('\\' + delimiter, 'g') : '';
};

NumeralFormatter.groupStyle = {
    thousand: 'thousand',
    lakh:     'lakh',
    wan:      'wan',
    none:     'none'    
};

NumeralFormatter.prototype = {
    getRawValue: function (value) {
        return value.replace(this.delimiterRE, '').replace(this.numeralDecimalMark, '.');
    },

    format: function (value) {
        var owner = this, parts, partSign, partSignAndPrefix, partInteger, partDecimal = '';

        // strip alphabet letters
        value = value.replace(/[A-Za-z]/g, '')
            // replace the first decimal mark with reserved placeholder
            .replace(owner.numeralDecimalMark, 'M')

            // strip non numeric letters except minus and "M"
            // this is to ensure prefix has been stripped
            .replace(/[^\dM-]/g, '')

            // replace the leading minus with reserved placeholder
            .replace(/^\-/, 'N')

            // strip the other minus sign (if present)
            .replace(/\-/g, '')

            // replace the minus sign (if present)
            .replace('N', owner.numeralPositiveOnly ? '' : '-')

            // replace decimal mark
            .replace('M', owner.numeralDecimalMark);

        // strip any leading zeros
        if (owner.stripLeadingZeroes) {
            value = value.replace(/^(-)?0+(?=\d)/, '$1');
        }

        partSign = value.slice(0, 1) === '-' ? '-' : '';
        if (typeof owner.prefix != 'undefined') {
            if (owner.signBeforePrefix) {
                partSignAndPrefix = partSign + owner.prefix;
            } else {
                partSignAndPrefix = owner.prefix + partSign;
            }
        } else {
            partSignAndPrefix = partSign;
        }
        
        partInteger = value;

        if (value.indexOf(owner.numeralDecimalMark) >= 0) {
            parts = value.split(owner.numeralDecimalMark);
            partInteger = parts[0];
            partDecimal = owner.numeralDecimalMark + parts[1].slice(0, owner.numeralDecimalScale);
        }

        if(partSign === '-') {
            partInteger = partInteger.slice(1);
        }

        if (owner.numeralIntegerScale > 0) {
          partInteger = partInteger.slice(0, owner.numeralIntegerScale);
        }

        switch (owner.numeralThousandsGroupStyle) {
        case NumeralFormatter.groupStyle.lakh:
            partInteger = partInteger.replace(/(\d)(?=(\d\d)+\d$)/g, '$1' + owner.delimiter);

            break;

        case NumeralFormatter.groupStyle.wan:
            partInteger = partInteger.replace(/(\d)(?=(\d{4})+$)/g, '$1' + owner.delimiter);

            break;

        case NumeralFormatter.groupStyle.thousand:
            partInteger = partInteger.replace(/(\d)(?=(\d{3})+$)/g, '$1' + owner.delimiter);

            break;
        }

        if (owner.tailPrefix) {
            return partSign + partInteger.toString() + (owner.numeralDecimalScale > 0 ? partDecimal.toString() : '') + owner.prefix;
        }

        return partSignAndPrefix + partInteger.toString() + (owner.numeralDecimalScale > 0 ? partDecimal.toString() : '');
    }
};

var NumeralFormatter_1 = NumeralFormatter;

var DateFormatter = function (datePattern, dateMin, dateMax) {
    var owner = this;

    owner.date = [];
    owner.blocks = [];
    owner.datePattern = datePattern;
    owner.dateMin = dateMin
      .split('-')
      .reverse()
      .map(function(x) {
        return parseInt(x, 10);
      });
    if (owner.dateMin.length === 2) owner.dateMin.unshift(0);

    owner.dateMax = dateMax
      .split('-')
      .reverse()
      .map(function(x) {
        return parseInt(x, 10);
      });
    if (owner.dateMax.length === 2) owner.dateMax.unshift(0);
    
    owner.initBlocks();
};

DateFormatter.prototype = {
    initBlocks: function () {
        var owner = this;
        owner.datePattern.forEach(function (value) {
            if (value === 'Y') {
                owner.blocks.push(4);
            } else {
                owner.blocks.push(2);
            }
        });
    },

    getISOFormatDate: function () {
        var owner = this,
            date = owner.date;

        return date[2] ? (
            date[2] + '-' + owner.addLeadingZero(date[1]) + '-' + owner.addLeadingZero(date[0])
        ) : '';
    },

    getBlocks: function () {
        return this.blocks;
    },

    getValidatedDate: function (value) {
        var owner = this, result = '';

        value = value.replace(/[^\d]/g, '');

        owner.blocks.forEach(function (length, index) {
            if (value.length > 0) {
                var sub = value.slice(0, length),
                    sub0 = sub.slice(0, 1),
                    rest = value.slice(length);

                switch (owner.datePattern[index]) {
                case 'd':
                    if (sub === '00') {
                        sub = '01';
                    } else if (parseInt(sub0, 10) > 3) {
                        sub = '0' + sub0;
                    } else if (parseInt(sub, 10) > 31) {
                        sub = '31';
                    }

                    break;

                case 'm':
                    if (sub === '00') {
                        sub = '01';
                    } else if (parseInt(sub0, 10) > 1) {
                        sub = '0' + sub0;
                    } else if (parseInt(sub, 10) > 12) {
                        sub = '12';
                    }

                    break;
                }

                result += sub;

                // update remaining string
                value = rest;
            }
        });

        return this.getFixedDateString(result);
    },

    getFixedDateString: function (value) {
        var owner = this, datePattern = owner.datePattern, date = [],
            dayIndex = 0, monthIndex = 0, yearIndex = 0,
            dayStartIndex = 0, monthStartIndex = 0, yearStartIndex = 0,
            day, month, year, fullYearDone = false;

        // mm-dd || dd-mm
        if (value.length === 4 && datePattern[0].toLowerCase() !== 'y' && datePattern[1].toLowerCase() !== 'y') {
            dayStartIndex = datePattern[0] === 'd' ? 0 : 2;
            monthStartIndex = 2 - dayStartIndex;
            day = parseInt(value.slice(dayStartIndex, dayStartIndex + 2), 10);
            month = parseInt(value.slice(monthStartIndex, monthStartIndex + 2), 10);

            date = this.getFixedDate(day, month, 0);
        }

        // yyyy-mm-dd || yyyy-dd-mm || mm-dd-yyyy || dd-mm-yyyy || dd-yyyy-mm || mm-yyyy-dd
        if (value.length === 8) {
            datePattern.forEach(function (type, index) {
                switch (type) {
                case 'd':
                    dayIndex = index;
                    break;
                case 'm':
                    monthIndex = index;
                    break;
                default:
                    yearIndex = index;
                    break;
                }
            });

            yearStartIndex = yearIndex * 2;
            dayStartIndex = (dayIndex <= yearIndex) ? dayIndex * 2 : (dayIndex * 2 + 2);
            monthStartIndex = (monthIndex <= yearIndex) ? monthIndex * 2 : (monthIndex * 2 + 2);

            day = parseInt(value.slice(dayStartIndex, dayStartIndex + 2), 10);
            month = parseInt(value.slice(monthStartIndex, monthStartIndex + 2), 10);
            year = parseInt(value.slice(yearStartIndex, yearStartIndex + 4), 10);

            fullYearDone = value.slice(yearStartIndex, yearStartIndex + 4).length === 4;

            date = this.getFixedDate(day, month, year);
        }

        // mm-yy || yy-mm
        if (value.length === 4 && (datePattern[0] === 'y' || datePattern[1] === 'y')) {
            monthStartIndex = datePattern[0] === 'm' ? 0 : 2;
            yearStartIndex = 2 - monthStartIndex;
            month = parseInt(value.slice(monthStartIndex, monthStartIndex + 2), 10);
            year = parseInt(value.slice(yearStartIndex, yearStartIndex + 2), 10);

            fullYearDone = value.slice(yearStartIndex, yearStartIndex + 2).length === 2;

            date = [0, month, year];
        }

        // mm-yyyy || yyyy-mm
        if (value.length === 6 && (datePattern[0] === 'Y' || datePattern[1] === 'Y')) {
            monthStartIndex = datePattern[0] === 'm' ? 0 : 4;
            yearStartIndex = 2 - 0.5 * monthStartIndex;
            month = parseInt(value.slice(monthStartIndex, monthStartIndex + 2), 10);
            year = parseInt(value.slice(yearStartIndex, yearStartIndex + 4), 10);

            fullYearDone = value.slice(yearStartIndex, yearStartIndex + 4).length === 4;

            date = [0, month, year];
        }

        date = owner.getRangeFixedDate(date);
        owner.date = date;

        var result = date.length === 0 ? value : datePattern.reduce(function (previous, current) {
            switch (current) {
            case 'd':
                return previous + (date[0] === 0 ? '' : owner.addLeadingZero(date[0]));
            case 'm':
                return previous + (date[1] === 0 ? '' : owner.addLeadingZero(date[1]));
            case 'y':
                return previous + (fullYearDone ? owner.addLeadingZeroForYear(date[2], false) : '');
            case 'Y':
                return previous + (fullYearDone ? owner.addLeadingZeroForYear(date[2], true) : '');
            }
        }, '');

        return result;
    },

    getRangeFixedDate: function (date) {
        var owner = this,
            datePattern = owner.datePattern,
            dateMin = owner.dateMin || [],
            dateMax = owner.dateMax || [];

        if (!date.length || (dateMin.length < 3 && dateMax.length < 3)) return date;

        if (
          datePattern.find(function(x) {
            return x.toLowerCase() === 'y';
          }) &&
          date[2] === 0
        ) return date;

        if (dateMax.length && (dateMax[2] < date[2] || (
          dateMax[2] === date[2] && (dateMax[1] < date[1] || (
            dateMax[1] === date[1] && dateMax[0] < date[0]
          ))
        ))) return dateMax;

        if (dateMin.length && (dateMin[2] > date[2] || (
          dateMin[2] === date[2] && (dateMin[1] > date[1] || (
            dateMin[1] === date[1] && dateMin[0] > date[0]
          ))
        ))) return dateMin;

        return date;
    },

    getFixedDate: function (day, month, year) {
        day = Math.min(day, 31);
        month = Math.min(month, 12);
        year = parseInt((year || 0), 10);

        if ((month < 7 && month % 2 === 0) || (month > 8 && month % 2 === 1)) {
            day = Math.min(day, month === 2 ? (this.isLeapYear(year) ? 29 : 28) : 30);
        }

        return [day, month, year];
    },

    isLeapYear: function (year) {
        return ((year % 4 === 0) && (year % 100 !== 0)) || (year % 400 === 0);
    },

    addLeadingZero: function (number) {
        return (number < 10 ? '0' : '') + number;
    },

    addLeadingZeroForYear: function (number, fullYearMode) {
        if (fullYearMode) {
            return (number < 10 ? '000' : (number < 100 ? '00' : (number < 1000 ? '0' : ''))) + number;
        }

        return (number < 10 ? '0' : '') + number;
    }
};

var DateFormatter_1 = DateFormatter;

var TimeFormatter = function (timePattern, timeFormat) {
    var owner = this;

    owner.time = [];
    owner.blocks = [];
    owner.timePattern = timePattern;
    owner.timeFormat = timeFormat;
    owner.initBlocks();
};

TimeFormatter.prototype = {
    initBlocks: function () {
        var owner = this;
        owner.timePattern.forEach(function () {
            owner.blocks.push(2);
        });
    },

    getISOFormatTime: function () {
        var owner = this,
            time = owner.time;

        return time[2] ? (
            owner.addLeadingZero(time[0]) + ':' + owner.addLeadingZero(time[1]) + ':' + owner.addLeadingZero(time[2])
        ) : '';
    },

    getBlocks: function () {
        return this.blocks;
    },

    getTimeFormatOptions: function () {
        var owner = this;
        if (String(owner.timeFormat) === '12') {
            return {
                maxHourFirstDigit: 1,
                maxHours: 12,
                maxMinutesFirstDigit: 5,
                maxMinutes: 60
            };
        }

        return {
            maxHourFirstDigit: 2,
            maxHours: 23,
            maxMinutesFirstDigit: 5,
            maxMinutes: 60
        };
    },

    getValidatedTime: function (value) {
        var owner = this, result = '';

        value = value.replace(/[^\d]/g, '');

        var timeFormatOptions = owner.getTimeFormatOptions();

        owner.blocks.forEach(function (length, index) {
            if (value.length > 0) {
                var sub = value.slice(0, length),
                    sub0 = sub.slice(0, 1),
                    rest = value.slice(length);

                switch (owner.timePattern[index]) {

                case 'h':
                    if (parseInt(sub0, 10) > timeFormatOptions.maxHourFirstDigit) {
                        sub = '0' + sub0;
                    } else if (parseInt(sub, 10) > timeFormatOptions.maxHours) {
                        sub = timeFormatOptions.maxHours + '';
                    }

                    break;

                case 'm':
                case 's':
                    if (parseInt(sub0, 10) > timeFormatOptions.maxMinutesFirstDigit) {
                        sub = '0' + sub0;
                    } else if (parseInt(sub, 10) > timeFormatOptions.maxMinutes) {
                        sub = timeFormatOptions.maxMinutes + '';
                    }
                    break;
                }

                result += sub;

                // update remaining string
                value = rest;
            }
        });

        return this.getFixedTimeString(result);
    },

    getFixedTimeString: function (value) {
        var owner = this, timePattern = owner.timePattern, time = [],
            secondIndex = 0, minuteIndex = 0, hourIndex = 0,
            secondStartIndex = 0, minuteStartIndex = 0, hourStartIndex = 0,
            second, minute, hour;

        if (value.length === 6) {
            timePattern.forEach(function (type, index) {
                switch (type) {
                case 's':
                    secondIndex = index * 2;
                    break;
                case 'm':
                    minuteIndex = index * 2;
                    break;
                case 'h':
                    hourIndex = index * 2;
                    break;
                }
            });

            hourStartIndex = hourIndex;
            minuteStartIndex = minuteIndex;
            secondStartIndex = secondIndex;

            second = parseInt(value.slice(secondStartIndex, secondStartIndex + 2), 10);
            minute = parseInt(value.slice(minuteStartIndex, minuteStartIndex + 2), 10);
            hour = parseInt(value.slice(hourStartIndex, hourStartIndex + 2), 10);

            time = this.getFixedTime(hour, minute, second);
        }

        if (value.length === 4 && owner.timePattern.indexOf('s') < 0) {
            timePattern.forEach(function (type, index) {
                switch (type) {
                case 'm':
                    minuteIndex = index * 2;
                    break;
                case 'h':
                    hourIndex = index * 2;
                    break;
                }
            });

            hourStartIndex = hourIndex;
            minuteStartIndex = minuteIndex;

            second = 0;
            minute = parseInt(value.slice(minuteStartIndex, minuteStartIndex + 2), 10);
            hour = parseInt(value.slice(hourStartIndex, hourStartIndex + 2), 10);

            time = this.getFixedTime(hour, minute, second);
        }

        owner.time = time;

        return time.length === 0 ? value : timePattern.reduce(function (previous, current) {
            switch (current) {
            case 's':
                return previous + owner.addLeadingZero(time[2]);
            case 'm':
                return previous + owner.addLeadingZero(time[1]);
            case 'h':
                return previous + owner.addLeadingZero(time[0]);
            }
        }, '');
    },

    getFixedTime: function (hour, minute, second) {
        second = Math.min(parseInt(second || 0, 10), 60);
        minute = Math.min(minute, 60);
        hour = Math.min(hour, 60);

        return [hour, minute, second];
    },

    addLeadingZero: function (number) {
        return (number < 10 ? '0' : '') + number;
    }
};

var TimeFormatter_1 = TimeFormatter;

var PhoneFormatter = function (formatter, delimiter) {
    var owner = this;

    owner.delimiter = (delimiter || delimiter === '') ? delimiter : ' ';
    owner.delimiterRE = delimiter ? new RegExp('\\' + delimiter, 'g') : '';

    owner.formatter = formatter;
};

PhoneFormatter.prototype = {
    setFormatter: function (formatter) {
        this.formatter = formatter;
    },

    format: function (phoneNumber) {
        var owner = this;

        owner.formatter.clear();

        // only keep number and +
        phoneNumber = phoneNumber.replace(/[^\d+]/g, '');

        // strip non-leading +
        phoneNumber = phoneNumber.replace(/^\+/, 'B').replace(/\+/g, '').replace('B', '+');

        // strip delimiter
        phoneNumber = phoneNumber.replace(owner.delimiterRE, '');

        var result = '', current, validated = false;

        for (var i = 0, iMax = phoneNumber.length; i < iMax; i++) {
            current = owner.formatter.inputDigit(phoneNumber.charAt(i));

            // has ()- or space inside
            if (/[\s()-]/g.test(current)) {
                result = current;

                validated = true;
            } else {
                if (!validated) {
                    result = current;
                }
                // else: over length input
                // it turns to invalid number again
            }
        }

        // strip ()
        // e.g. US: 7161234567 returns (716) 123-4567
        result = result.replace(/[()]/g, '');
        // replace library delimiter with user customized delimiter
        result = result.replace(/[\s-]/g, owner.delimiter);

        return result;
    }
};

var PhoneFormatter_1 = PhoneFormatter;

var CreditCardDetector = {
    blocks: {
        uatp:          [4, 5, 6],
        amex:          [4, 6, 5],
        diners:        [4, 6, 4],
        discover:      [4, 4, 4, 4],
        mastercard:    [4, 4, 4, 4],
        dankort:       [4, 4, 4, 4],
        instapayment:  [4, 4, 4, 4],
        jcb15:         [4, 6, 5],
        jcb:           [4, 4, 4, 4],
        maestro:       [4, 4, 4, 4],
        visa:          [4, 4, 4, 4],
        mir:           [4, 4, 4, 4],
        unionPay:      [4, 4, 4, 4],
        general:       [4, 4, 4, 4]
    },

    re: {
        // starts with 1; 15 digits, not starts with 1800 (jcb card)
        uatp: /^(?!1800)1\d{0,14}/,

        // starts with 34/37; 15 digits
        amex: /^3[47]\d{0,13}/,

        // starts with 6011/65/644-649; 16 digits
        discover: /^(?:6011|65\d{0,2}|64[4-9]\d?)\d{0,12}/,

        // starts with 300-305/309 or 36/38/39; 14 digits
        diners: /^3(?:0([0-5]|9)|[689]\d?)\d{0,11}/,

        // starts with 51-55/2221–2720; 16 digits
        mastercard: /^(5[1-5]\d{0,2}|22[2-9]\d{0,1}|2[3-7]\d{0,2})\d{0,12}/,

        // starts with 5019/4175/4571; 16 digits
        dankort: /^(5019|4175|4571)\d{0,12}/,

        // starts with 637-639; 16 digits
        instapayment: /^63[7-9]\d{0,13}/,

        // starts with 2131/1800; 15 digits
        jcb15: /^(?:2131|1800)\d{0,11}/,

        // starts with 2131/1800/35; 16 digits
        jcb: /^(?:35\d{0,2})\d{0,12}/,

        // starts with 50/56-58/6304/67; 16 digits
        maestro: /^(?:5[0678]\d{0,2}|6304|67\d{0,2})\d{0,12}/,

        // starts with 22; 16 digits
        mir: /^220[0-4]\d{0,12}/,

        // starts with 4; 16 digits
        visa: /^4\d{0,15}/,

        // starts with 62/81; 16 digits
        unionPay: /^(62|81)\d{0,14}/
    },

    getStrictBlocks: function (block) {
      var total = block.reduce(function (prev, current) {
        return prev + current;
      }, 0);

      return block.concat(19 - total);
    },

    getInfo: function (value, strictMode) {
        var blocks = CreditCardDetector.blocks,
            re = CreditCardDetector.re;

        // Some credit card can have up to 19 digits number.
        // Set strictMode to true will remove the 16 max-length restrain,
        // however, I never found any website validate card number like
        // this, hence probably you don't want to enable this option.
        strictMode = !!strictMode;

        for (var key in re) {
            if (re[key].test(value)) {
                var matchedBlocks = blocks[key];
                return {
                    type: key,
                    blocks: strictMode ? this.getStrictBlocks(matchedBlocks) : matchedBlocks
                };
            }
        }

        return {
            type: 'unknown',
            blocks: strictMode ? this.getStrictBlocks(blocks.general) : blocks.general
        };
    }
};

var CreditCardDetector_1 = CreditCardDetector;

var Util = {
    noop: function () {
    },

    strip: function (value, re) {
        return value.replace(re, '');
    },

    getPostDelimiter: function (value, delimiter, delimiters) {
        // single delimiter
        if (delimiters.length === 0) {
            return value.slice(-delimiter.length) === delimiter ? delimiter : '';
        }

        // multiple delimiters
        var matchedDelimiter = '';
        delimiters.forEach(function (current) {
            if (value.slice(-current.length) === current) {
                matchedDelimiter = current;
            }
        });

        return matchedDelimiter;
    },

    getDelimiterREByDelimiter: function (delimiter) {
        return new RegExp(delimiter.replace(/([.?*+^$[\]\\(){}|-])/g, '\\$1'), 'g');
    },

    getNextCursorPosition: function (prevPos, oldValue, newValue, delimiter, delimiters) {
      // If cursor was at the end of value, just place it back.
      // Because new value could contain additional chars.
      if (oldValue.length === prevPos) {
          return newValue.length;
      }

      return prevPos + this.getPositionOffset(prevPos, oldValue, newValue, delimiter ,delimiters);
    },

    getPositionOffset: function (prevPos, oldValue, newValue, delimiter, delimiters) {
        var oldRawValue, newRawValue, lengthOffset;

        oldRawValue = this.stripDelimiters(oldValue.slice(0, prevPos), delimiter, delimiters);
        newRawValue = this.stripDelimiters(newValue.slice(0, prevPos), delimiter, delimiters);
        lengthOffset = oldRawValue.length - newRawValue.length;

        return (lengthOffset !== 0) ? (lengthOffset / Math.abs(lengthOffset)) : 0;
    },

    stripDelimiters: function (value, delimiter, delimiters) {
        var owner = this;

        // single delimiter
        if (delimiters.length === 0) {
            var delimiterRE = delimiter ? owner.getDelimiterREByDelimiter(delimiter) : '';

            return value.replace(delimiterRE, '');
        }

        // multiple delimiters
        delimiters.forEach(function (current) {
            current.split('').forEach(function (letter) {
                value = value.replace(owner.getDelimiterREByDelimiter(letter), '');
            });
        });

        return value;
    },

    headStr: function (str, length) {
        return str.slice(0, length);
    },

    getMaxLength: function (blocks) {
        return blocks.reduce(function (previous, current) {
            return previous + current;
        }, 0);
    },

    // strip prefix
    // Before type  |   After type    |     Return value
    // PEFIX-...    |   PEFIX-...     |     ''
    // PREFIX-123   |   PEFIX-123     |     123
    // PREFIX-123   |   PREFIX-23     |     23
    // PREFIX-123   |   PREFIX-1234   |     1234
    getPrefixStrippedValue: function (value, prefix, prefixLength, prevResult, delimiter, delimiters, noImmediatePrefix, tailPrefix, signBeforePrefix) {
        // No prefix
        if (prefixLength === 0) {
          return value;
        }

        // Value is prefix
        if (value === prefix && value !== '') {
          return '';
        }

        if (signBeforePrefix && (value.slice(0, 1) == '-')) {
            var prev = (prevResult.slice(0, 1) == '-') ? prevResult.slice(1) : prevResult;
            return '-' + this.getPrefixStrippedValue(value.slice(1), prefix, prefixLength, prev, delimiter, delimiters, noImmediatePrefix, tailPrefix, signBeforePrefix);
        }

        // Pre result prefix string does not match pre-defined prefix
        if (prevResult.slice(0, prefixLength) !== prefix && !tailPrefix) {
            // Check if the first time user entered something
            if (noImmediatePrefix && !prevResult && value) return value;
            return '';
        } else if (prevResult.slice(-prefixLength) !== prefix && tailPrefix) {
            // Check if the first time user entered something
            if (noImmediatePrefix && !prevResult && value) return value;
            return '';
        }

        var prevValue = this.stripDelimiters(prevResult, delimiter, delimiters);

        // New value has issue, someone typed in between prefix letters
        // Revert to pre value
        if (value.slice(0, prefixLength) !== prefix && !tailPrefix) {
            return prevValue.slice(prefixLength);
        } else if (value.slice(-prefixLength) !== prefix && tailPrefix) {
            return prevValue.slice(0, -prefixLength - 1);
        }

        // No issue, strip prefix for new value
        return tailPrefix ? value.slice(0, -prefixLength) : value.slice(prefixLength);
    },

    getFirstDiffIndex: function (prev, current) {
        var index = 0;

        while (prev.charAt(index) === current.charAt(index)) {
            if (prev.charAt(index++) === '') {
                return -1;
            }
        }

        return index;
    },

    getFormattedValue: function (value, blocks, blocksLength, delimiter, delimiters, delimiterLazyShow) {
        var result = '',
            multipleDelimiters = delimiters.length > 0,
            currentDelimiter = '';

        // no options, normal input
        if (blocksLength === 0) {
            return value;
        }

        blocks.forEach(function (length, index) {
            if (value.length > 0) {
                var sub = value.slice(0, length),
                    rest = value.slice(length);

                if (multipleDelimiters) {
                    currentDelimiter = delimiters[delimiterLazyShow ? (index - 1) : index] || currentDelimiter;
                } else {
                    currentDelimiter = delimiter;
                }

                if (delimiterLazyShow) {
                    if (index > 0) {
                        result += currentDelimiter;
                    }

                    result += sub;
                } else {
                    result += sub;

                    if (sub.length === length && index < blocksLength - 1) {
                        result += currentDelimiter;
                    }
                }

                // update remaining string
                value = rest;
            }
        });

        return result;
    },

    // move cursor to the end
    // the first time user focuses on an input with prefix
    fixPrefixCursor: function (el, prefix, delimiter, delimiters) {
        if (!el) {
            return;
        }

        var val = el.value,
            appendix = delimiter || (delimiters[0] || ' ');

        if (!el.setSelectionRange || !prefix || (prefix.length + appendix.length) <= val.length) {
            return;
        }

        var len = val.length * 2;

        // set timeout to avoid blink
        setTimeout(function () {
            el.setSelectionRange(len, len);
        }, 1);
    },

    // Check if input field is fully selected
    checkFullSelection: function(value) {
      try {
        var selection = window.getSelection() || document.getSelection() || {};
        return selection.toString().length === value.length;
      } catch (ex) {
        // Ignore
      }

      return false;
    },

    setSelection: function (element, position, doc) {
        if (element !== this.getActiveElement(doc)) {
            return;
        }

        // cursor is already in the end
        if (element && element.value.length <= position) {
          return;
        }

        if (element.createTextRange) {
            var range = element.createTextRange();

            range.move('character', position);
            range.select();
        } else {
            try {
                element.setSelectionRange(position, position);
            } catch (e) {
                // eslint-disable-next-line
                console.warn('The input element type does not support selection');
            }
        }
    },

    getActiveElement: function(parent) {
        var activeElement = parent.activeElement;
        if (activeElement && activeElement.shadowRoot) {
            return this.getActiveElement(activeElement.shadowRoot);
        }
        return activeElement;
    },

    isAndroid: function () {
        return navigator && /android/i.test(navigator.userAgent);
    },

    // On Android chrome, the keyup and keydown events
    // always return key code 229 as a composition that
    // buffers the user’s keystrokes
    // see https://github.com/nosir/cleave.js/issues/147
    isAndroidBackspaceKeydown: function (lastInputValue, currentInputValue) {
        if (!this.isAndroid() || !lastInputValue || !currentInputValue) {
            return false;
        }

        return currentInputValue === lastInputValue.slice(0, -1);
    }
};

var Util_1 = Util;

/**
 * Props Assignment
 *
 * Separate this, so react module can share the usage
 */
var DefaultProperties = {
    // Maybe change to object-assign
    // for now just keep it as simple
    assign: function (target, opts) {
        target = target || {};
        opts = opts || {};

        // credit card
        target.creditCard = !!opts.creditCard;
        target.creditCardStrictMode = !!opts.creditCardStrictMode;
        target.creditCardType = '';
        target.onCreditCardTypeChanged = opts.onCreditCardTypeChanged || (function () {});

        // phone
        target.phone = !!opts.phone;
        target.phoneRegionCode = opts.phoneRegionCode || 'AU';
        target.phoneFormatter = {};

        // time
        target.time = !!opts.time;
        target.timePattern = opts.timePattern || ['h', 'm', 's'];
        target.timeFormat = opts.timeFormat || '24';
        target.timeFormatter = {};

        // date
        target.date = !!opts.date;
        target.datePattern = opts.datePattern || ['d', 'm', 'Y'];
        target.dateMin = opts.dateMin || '';
        target.dateMax = opts.dateMax || '';
        target.dateFormatter = {};

        // numeral
        target.numeral = !!opts.numeral;
        target.numeralIntegerScale = opts.numeralIntegerScale > 0 ? opts.numeralIntegerScale : 0;
        target.numeralDecimalScale = opts.numeralDecimalScale >= 0 ? opts.numeralDecimalScale : 2;
        target.numeralDecimalMark = opts.numeralDecimalMark || '.';
        target.numeralThousandsGroupStyle = opts.numeralThousandsGroupStyle || 'thousand';
        target.numeralPositiveOnly = !!opts.numeralPositiveOnly;
        target.stripLeadingZeroes = opts.stripLeadingZeroes !== false;
        target.signBeforePrefix = !!opts.signBeforePrefix;
        target.tailPrefix = !!opts.tailPrefix;

        // others
        target.swapHiddenInput = !!opts.swapHiddenInput;
        
        target.numericOnly = target.creditCard || target.date || !!opts.numericOnly;

        target.uppercase = !!opts.uppercase;
        target.lowercase = !!opts.lowercase;

        target.prefix = (target.creditCard || target.date) ? '' : (opts.prefix || '');
        target.noImmediatePrefix = !!opts.noImmediatePrefix;
        target.prefixLength = target.prefix.length;
        target.rawValueTrimPrefix = !!opts.rawValueTrimPrefix;
        target.copyDelimiter = !!opts.copyDelimiter;

        target.initValue = (opts.initValue !== undefined && opts.initValue !== null) ? opts.initValue.toString() : '';

        target.delimiter =
            (opts.delimiter || opts.delimiter === '') ? opts.delimiter :
                (opts.date ? '/' :
                    (opts.time ? ':' :
                        (opts.numeral ? ',' :
                            (opts.phone ? ' ' :
                                ' '))));
        target.delimiterLength = target.delimiter.length;
        target.delimiterLazyShow = !!opts.delimiterLazyShow;
        target.delimiters = opts.delimiters || [];

        target.blocks = opts.blocks || [];
        target.blocksLength = target.blocks.length;

        target.root = (typeof commonjsGlobal === 'object' && commonjsGlobal) ? commonjsGlobal : window;
        target.document = opts.document || target.root.document;

        target.maxLength = 0;

        target.backspace = false;
        target.result = '';

        target.onValueChanged = opts.onValueChanged || (function () {});

        return target;
    }
};

var DefaultProperties_1 = DefaultProperties;

/**
 * Construct a new Cleave instance by passing the configuration object
 *
 * @param {String | HTMLElement} element
 * @param {Object} opts
 */
var Cleave = function (element, opts) {
    var owner = this;
    var hasMultipleElements = false;

    if (typeof element === 'string') {
        owner.element = document.querySelector(element);
        hasMultipleElements = document.querySelectorAll(element).length > 1;
    } else {
      if (typeof element.length !== 'undefined' && element.length > 0) {
        owner.element = element[0];
        hasMultipleElements = element.length > 1;
      } else {
        owner.element = element;
      }
    }

    if (!owner.element) {
        throw new Error('[cleave.js] Please check the element');
    }

    if (hasMultipleElements) {
      try {
        // eslint-disable-next-line
        console.warn('[cleave.js] Multiple input fields matched, cleave.js will only take the first one.');
      } catch (e) {
        // Old IE
      }
    }

    opts.initValue = owner.element.value;

    owner.properties = Cleave.DefaultProperties.assign({}, opts);

    owner.init();
};

Cleave.prototype = {
    init: function () {
        var owner = this, pps = owner.properties;

        // no need to use this lib
        if (!pps.numeral && !pps.phone && !pps.creditCard && !pps.time && !pps.date && (pps.blocksLength === 0 && !pps.prefix)) {
            owner.onInput(pps.initValue);

            return;
        }

        pps.maxLength = Cleave.Util.getMaxLength(pps.blocks);

        owner.isAndroid = Cleave.Util.isAndroid();
        owner.lastInputValue = '';
        owner.isBackward = '';

        owner.onChangeListener = owner.onChange.bind(owner);
        owner.onKeyDownListener = owner.onKeyDown.bind(owner);
        owner.onFocusListener = owner.onFocus.bind(owner);
        owner.onCutListener = owner.onCut.bind(owner);
        owner.onCopyListener = owner.onCopy.bind(owner);

        owner.initSwapHiddenInput();

        owner.element.addEventListener('input', owner.onChangeListener);
        owner.element.addEventListener('keydown', owner.onKeyDownListener);
        owner.element.addEventListener('focus', owner.onFocusListener);
        owner.element.addEventListener('cut', owner.onCutListener);
        owner.element.addEventListener('copy', owner.onCopyListener);


        owner.initPhoneFormatter();
        owner.initDateFormatter();
        owner.initTimeFormatter();
        owner.initNumeralFormatter();

        // avoid touch input field if value is null
        // otherwise Firefox will add red box-shadow for <input required />
        if (pps.initValue || (pps.prefix && !pps.noImmediatePrefix)) {
            owner.onInput(pps.initValue);
        }
    },

    initSwapHiddenInput: function () {
        var owner = this, pps = owner.properties;
        if (!pps.swapHiddenInput) return;

        var inputFormatter = owner.element.cloneNode(true);
        owner.element.parentNode.insertBefore(inputFormatter, owner.element);

        owner.elementSwapHidden = owner.element;
        owner.elementSwapHidden.type = 'hidden';

        owner.element = inputFormatter;
        owner.element.id = '';
    },

    initNumeralFormatter: function () {
        var owner = this, pps = owner.properties;

        if (!pps.numeral) {
            return;
        }

        pps.numeralFormatter = new Cleave.NumeralFormatter(
            pps.numeralDecimalMark,
            pps.numeralIntegerScale,
            pps.numeralDecimalScale,
            pps.numeralThousandsGroupStyle,
            pps.numeralPositiveOnly,
            pps.stripLeadingZeroes,
            pps.prefix,
            pps.signBeforePrefix,
            pps.tailPrefix,
            pps.delimiter
        );
    },

    initTimeFormatter: function() {
        var owner = this, pps = owner.properties;

        if (!pps.time) {
            return;
        }

        pps.timeFormatter = new Cleave.TimeFormatter(pps.timePattern, pps.timeFormat);
        pps.blocks = pps.timeFormatter.getBlocks();
        pps.blocksLength = pps.blocks.length;
        pps.maxLength = Cleave.Util.getMaxLength(pps.blocks);
    },

    initDateFormatter: function () {
        var owner = this, pps = owner.properties;

        if (!pps.date) {
            return;
        }

        pps.dateFormatter = new Cleave.DateFormatter(pps.datePattern, pps.dateMin, pps.dateMax);
        pps.blocks = pps.dateFormatter.getBlocks();
        pps.blocksLength = pps.blocks.length;
        pps.maxLength = Cleave.Util.getMaxLength(pps.blocks);
    },

    initPhoneFormatter: function () {
        var owner = this, pps = owner.properties;

        if (!pps.phone) {
            return;
        }

        // Cleave.AsYouTypeFormatter should be provided by
        // external google closure lib
        try {
            pps.phoneFormatter = new Cleave.PhoneFormatter(
                new pps.root.Cleave.AsYouTypeFormatter(pps.phoneRegionCode),
                pps.delimiter
            );
        } catch (ex) {
            throw new Error('[cleave.js] Please include phone-type-formatter.{country}.js lib');
        }
    },

    onKeyDown: function (event) {
        var owner = this,
            charCode = event.which || event.keyCode;

        owner.lastInputValue = owner.element.value;
        owner.isBackward = charCode === 8;
    },

    onChange: function (event) {
        var owner = this, pps = owner.properties,
            Util = Cleave.Util;

        owner.isBackward = owner.isBackward || event.inputType === 'deleteContentBackward';

        var postDelimiter = Util.getPostDelimiter(owner.lastInputValue, pps.delimiter, pps.delimiters);

        if (owner.isBackward && postDelimiter) {
            pps.postDelimiterBackspace = postDelimiter;
        } else {
            pps.postDelimiterBackspace = false;
        }

        this.onInput(this.element.value);
    },

    onFocus: function () {
        var owner = this,
            pps = owner.properties;
        owner.lastInputValue = owner.element.value;

        if (pps.prefix && pps.noImmediatePrefix && !owner.element.value) {
            this.onInput(pps.prefix);
        }

        Cleave.Util.fixPrefixCursor(owner.element, pps.prefix, pps.delimiter, pps.delimiters);
    },

    onCut: function (e) {
        if (!Cleave.Util.checkFullSelection(this.element.value)) return;
        this.copyClipboardData(e);
        this.onInput('');
    },

    onCopy: function (e) {
        if (!Cleave.Util.checkFullSelection(this.element.value)) return;
        this.copyClipboardData(e);
    },

    copyClipboardData: function (e) {
        var owner = this,
            pps = owner.properties,
            Util = Cleave.Util,
            inputValue = owner.element.value,
            textToCopy = '';

        if (!pps.copyDelimiter) {
            textToCopy = Util.stripDelimiters(inputValue, pps.delimiter, pps.delimiters);
        } else {
            textToCopy = inputValue;
        }

        try {
            if (e.clipboardData) {
                e.clipboardData.setData('Text', textToCopy);
            } else {
                window.clipboardData.setData('Text', textToCopy);
            }

            e.preventDefault();
        } catch (ex) {
            //  empty
        }
    },

    onInput: function (value) {
        var owner = this, pps = owner.properties,
            Util = Cleave.Util;

        // case 1: delete one more character "4"
        // 1234*| -> hit backspace -> 123|
        // case 2: last character is not delimiter which is:
        // 12|34* -> hit backspace -> 1|34*
        // note: no need to apply this for numeral mode
        var postDelimiterAfter = Util.getPostDelimiter(value, pps.delimiter, pps.delimiters);
        if (!pps.numeral && pps.postDelimiterBackspace && !postDelimiterAfter) {
            value = Util.headStr(value, value.length - pps.postDelimiterBackspace.length);
        }

        // phone formatter
        if (pps.phone) {
            if (pps.prefix && (!pps.noImmediatePrefix || value.length)) {
                pps.result = pps.prefix + pps.phoneFormatter.format(value).slice(pps.prefix.length);
            } else {
                pps.result = pps.phoneFormatter.format(value);
            }
            owner.updateValueState();

            return;
        }

        // numeral formatter
        if (pps.numeral) {
            // Do not show prefix when noImmediatePrefix is specified
            // This mostly because we need to show user the native input placeholder
            if (pps.prefix && pps.noImmediatePrefix && value.length === 0) {
                pps.result = '';
            } else {
                pps.result = pps.numeralFormatter.format(value);
            }
            owner.updateValueState();

            return;
        }

        // date
        if (pps.date) {
            value = pps.dateFormatter.getValidatedDate(value);
        }

        // time
        if (pps.time) {
            value = pps.timeFormatter.getValidatedTime(value);
        }

        // strip delimiters
        value = Util.stripDelimiters(value, pps.delimiter, pps.delimiters);

        // strip prefix
        value = Util.getPrefixStrippedValue(value, pps.prefix, pps.prefixLength, pps.result, pps.delimiter, pps.delimiters, pps.noImmediatePrefix, pps.tailPrefix, pps.signBeforePrefix);

        // strip non-numeric characters
        value = pps.numericOnly ? Util.strip(value, /[^\d]/g) : value;

        // convert case
        value = pps.uppercase ? value.toUpperCase() : value;
        value = pps.lowercase ? value.toLowerCase() : value;

        // prevent from showing prefix when no immediate option enabled with empty input value
        if (pps.prefix) {
            if (pps.tailPrefix) {
                value = value + pps.prefix;
            } else {
                value = pps.prefix + value;
            }


            // no blocks specified, no need to do formatting
            if (pps.blocksLength === 0) {
                pps.result = value;
                owner.updateValueState();

                return;
            }
        }

        // update credit card props
        if (pps.creditCard) {
            owner.updateCreditCardPropsByValue(value);
        }

        // strip over length characters
        value = Util.headStr(value, pps.maxLength);

        // apply blocks
        pps.result = Util.getFormattedValue(
            value,
            pps.blocks, pps.blocksLength,
            pps.delimiter, pps.delimiters, pps.delimiterLazyShow
        );

        owner.updateValueState();
    },

    updateCreditCardPropsByValue: function (value) {
        var owner = this, pps = owner.properties,
            Util = Cleave.Util,
            creditCardInfo;

        // At least one of the first 4 characters has changed
        if (Util.headStr(pps.result, 4) === Util.headStr(value, 4)) {
            return;
        }

        creditCardInfo = Cleave.CreditCardDetector.getInfo(value, pps.creditCardStrictMode);

        pps.blocks = creditCardInfo.blocks;
        pps.blocksLength = pps.blocks.length;
        pps.maxLength = Util.getMaxLength(pps.blocks);

        // credit card type changed
        if (pps.creditCardType !== creditCardInfo.type) {
            pps.creditCardType = creditCardInfo.type;

            pps.onCreditCardTypeChanged.call(owner, pps.creditCardType);
        }
    },

    updateValueState: function () {
        var owner = this,
            Util = Cleave.Util,
            pps = owner.properties;

        if (!owner.element) {
            return;
        }

        var endPos = owner.element.selectionEnd;
        var oldValue = owner.element.value;
        var newValue = pps.result;

        endPos = Util.getNextCursorPosition(endPos, oldValue, newValue, pps.delimiter, pps.delimiters);

        // fix Android browser type="text" input field
        // cursor not jumping issue
        if (owner.isAndroid) {
            window.setTimeout(function () {
                owner.element.value = newValue;
                Util.setSelection(owner.element, endPos, pps.document, false);
                owner.callOnValueChanged();
            }, 1);

            return;
        }

        owner.element.value = newValue;
        if (pps.swapHiddenInput) owner.elementSwapHidden.value = owner.getRawValue();

        Util.setSelection(owner.element, endPos, pps.document, false);
        owner.callOnValueChanged();
    },

    callOnValueChanged: function () {
        var owner = this,
            pps = owner.properties;

        pps.onValueChanged.call(owner, {
            target: {
                name: owner.element.name,
                value: pps.result,
                rawValue: owner.getRawValue()
            }
        });
    },

    setPhoneRegionCode: function (phoneRegionCode) {
        var owner = this, pps = owner.properties;

        pps.phoneRegionCode = phoneRegionCode;
        owner.initPhoneFormatter();
        owner.onChange();
    },

    setRawValue: function (value) {
        var owner = this, pps = owner.properties;

        value = value !== undefined && value !== null ? value.toString() : '';

        if (pps.numeral) {
            value = value.replace('.', pps.numeralDecimalMark);
        }

        pps.postDelimiterBackspace = false;

        owner.element.value = value;
        owner.onInput(value);
    },

    getRawValue: function () {
        var owner = this,
            pps = owner.properties,
            Util = Cleave.Util,
            rawValue = owner.element.value;

        if (pps.rawValueTrimPrefix) {
            rawValue = Util.getPrefixStrippedValue(rawValue, pps.prefix, pps.prefixLength, pps.result, pps.delimiter, pps.delimiters, pps.noImmediatePrefix, pps.tailPrefix, pps.signBeforePrefix);
        }

        if (pps.numeral) {
            rawValue = pps.numeralFormatter.getRawValue(rawValue);
        } else {
            rawValue = Util.stripDelimiters(rawValue, pps.delimiter, pps.delimiters);
        }

        return rawValue;
    },

    getISOFormatDate: function () {
        var owner = this,
            pps = owner.properties;

        return pps.date ? pps.dateFormatter.getISOFormatDate() : '';
    },

    getISOFormatTime: function () {
        var owner = this,
            pps = owner.properties;

        return pps.time ? pps.timeFormatter.getISOFormatTime() : '';
    },

    getFormattedValue: function () {
        return this.element.value;
    },

    destroy: function () {
        var owner = this;

        owner.element.removeEventListener('input', owner.onChangeListener);
        owner.element.removeEventListener('keydown', owner.onKeyDownListener);
        owner.element.removeEventListener('focus', owner.onFocusListener);
        owner.element.removeEventListener('cut', owner.onCutListener);
        owner.element.removeEventListener('copy', owner.onCopyListener);
    },

    toString: function () {
        return '[Cleave Object]';
    }
};

Cleave.NumeralFormatter = NumeralFormatter_1;
Cleave.DateFormatter = DateFormatter_1;
Cleave.TimeFormatter = TimeFormatter_1;
Cleave.PhoneFormatter = PhoneFormatter_1;
Cleave.CreditCardDetector = CreditCardDetector_1;
Cleave.Util = Util_1;
Cleave.DefaultProperties = DefaultProperties_1;

// for angular directive
((typeof commonjsGlobal === 'object' && commonjsGlobal) ? commonjsGlobal : window)['Cleave'] = Cleave;

// CommonJS
var Cleave_1 = Cleave;

/**
 * The base implementation of `_.slice` without an iteratee call guard.
 *
 * @private
 * @param {Array} array The array to slice.
 * @param {number} [start=0] The start position.
 * @param {number} [end=array.length] The end position.
 * @returns {Array} Returns the slice of `array`.
 */
function baseSlice(array, start, end) {
  var index = -1,
      length = array.length;

  if (start < 0) {
    start = -start > length ? 0 : (length + start);
  }
  end = end > length ? length : end;
  if (end < 0) {
    end += length;
  }
  length = start > end ? 0 : ((end - start) >>> 0);
  start >>>= 0;

  var result = Array(length);
  while (++index < length) {
    result[index] = array[index + start];
  }
  return result;
}

/**
 * Gets the last element of `array`.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Array
 * @param {Array} array The array to query.
 * @returns {*} Returns the last element of `array`.
 * @example
 *
 * _.last([1, 2, 3]);
 * // => 3
 */
function last(array) {
  var length = array == null ? 0 : array.length;
  return length ? array[length - 1] : undefined;
}

/**
 * Gets the parent value at `path` of `object`.
 *
 * @private
 * @param {Object} object The object to query.
 * @param {Array} path The path to get the parent value of.
 * @returns {*} Returns the parent value.
 */
function parent(object, path) {
  return path.length < 2 ? object : objectUtils.baseGet(object, baseSlice(path, 0, -1));
}

/**
 * Checks if `value` is `null` or `undefined`.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is nullish, else `false`.
 * @example
 *
 * _.isNil(null);
 * // => true
 *
 * _.isNil(void 0);
 * // => true
 *
 * _.isNil(NaN);
 * // => false
 */
function isNil(value) {
  return value == null;
}

/** Used for built-in method references. */
var objectProto = Object.prototype;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/**
 * The base implementation of `_.unset`.
 *
 * @private
 * @param {Object} object The object to modify.
 * @param {Array|string} path The property path to unset.
 * @returns {boolean} Returns `true` if the property is deleted, else `false`.
 */
function baseUnset(object, path) {
  path = objectUtils.castPath(path, object);

  // Prevent prototype pollution:
  // https://github.com/lodash/lodash/security/advisories/GHSA-xxjr-mmjv-4gpg
  // https://github.com/lodash/lodash/security/advisories/GHSA-f23m-r3pf-42rh
  var index = -1,
      length = path.length;

  if (!length) {
    return true;
  }

  while (++index < length) {
    var key = objectUtils.toKey(path[index]);

    // Always block "__proto__" anywhere in the path if it's not expected
    if (key === '__proto__' && !hasOwnProperty.call(object, '__proto__')) {
      return false;
    }

    // Block constructor/prototype as non-terminal traversal keys to prevent
    // escaping the object graph into built-in constructors and prototypes.
    if ((key === 'constructor' || key === 'prototype') && index < length - 1) {
      return false;
    }
  }

  var obj = parent(object, path);
  return obj == null || delete obj[objectUtils.toKey(last(path))];
}

/** Used for built-in method references. */
var arrayProto = Array.prototype;

/** Built-in value references. */
var splice = arrayProto.splice;

/**
 * The base implementation of `_.pullAt` without support for individual
 * indexes or capturing the removed elements.
 *
 * @private
 * @param {Array} array The array to modify.
 * @param {number[]} indexes The indexes of elements to remove.
 * @returns {Array} Returns `array`.
 */
function basePullAt(array, indexes) {
  var length = array ? indexes.length : 0,
      lastIndex = length - 1;

  while (length--) {
    var index = indexes[length];
    if (length == lastIndex || index !== previous) {
      var previous = index;
      if (_Uint8Array.isIndex(index)) {
        splice.call(array, index, 1);
      } else {
        baseUnset(array, index);
      }
    }
  }
  return array;
}

/**
 * Removes all elements from `array` that `predicate` returns truthy for
 * and returns an array of the removed elements. The predicate is invoked
 * with three arguments: (value, index, array).
 *
 * **Note:** Unlike `_.filter`, this method mutates `array`. Use `_.pull`
 * to pull elements from an array by value.
 *
 * @static
 * @memberOf _
 * @since 2.0.0
 * @category Array
 * @param {Array} array The array to modify.
 * @param {Function} [predicate=_.identity] The function invoked per iteration.
 * @returns {Array} Returns the new array of removed elements.
 * @example
 *
 * var array = [1, 2, 3, 4];
 * var evens = _.remove(array, function(n) {
 *   return n % 2 == 0;
 * });
 *
 * console.log(array);
 * // => [1, 3]
 *
 * console.log(evens);
 * // => [2, 4]
 */
function remove(array, predicate) {
  var result = [];
  if (!(array && array.length)) {
    return result;
  }
  var index = -1,
      indexes = [],
      length = array.length;

  predicate = objectUtils.baseIteratee(predicate);
  while (++index < length) {
    var value = array[index];
    if (predicate(value, index, array)) {
      result.push(value);
      indexes.push(index);
    }
  }
  basePullAt(array, indexes);
  return result;
}

/**
 * Incremental regex matching for input masks.
 *
 * A regex such as `/^[A-Z]{2}[0-9]{4}$/` describes the *final* value, not the
 * intermediate states a user types through. Validating partial input against a
 * full-match regex is impossible with the native `RegExp` engine (e.g. `"A"` and
 * `"1"` both fail `^[A-Z]{2}[0-9]{4}$`, but only `"1"` should be rejected).
 *
 * This module compiles the pattern into a small Thompson NFA and simulates it one
 * character at a time, so it can tell whether the value so far is a complete match
 * (`DONE`), a valid prefix that could still grow (`MORE`), or impossible (`FAILED`).
 *
 * Supported syntax: literals, `.`, escapes (`\d \D \w \W \s \S` and literal
 * escapes), character classes `[...]` / `[^...]` with ranges, groups `(...)` and
 * `(?:...)`, alternation `|`, quantifiers `* + ? {n} {n,} {n,m}`, and anchors
 * `^ $`. Lookarounds and back-references are not supported; patterns using them
 * (or any pattern the native `RegExp` rejects) fall back to no masking.
 */
/** The value is a complete match for the pattern. */
const DONE = 'DONE';
/** The value is a valid prefix; more characters could complete it. */
const MORE = 'MORE';
/** The value can never match the pattern, no matter what is appended. */
const FAILED = 'FAILED';
/**
 * Thrown when the pattern uses constructs that cannot be modeled by a finite
 * automaton (lookarounds, back-references). Callers treat it as "disable masking".
 */
class UnsupportedRegexError extends Error {
}
/** Upper bound on bounded quantifiers ({n,m}); guards against state explosion. */
const MAX_QUANTIFIER = 1000;
// ── Parser: regex source → AST ──────────────────────────────────────────────
const parse = (src) => {
    let i = 0;
    const peek = () => src[i];
    const next = () => src[i++];
    const eof = () => i >= src.length;
    const parseAlt = () => {
        const opts = [parseSeq()];
        while (peek() === '|') {
            next();
            opts.push(parseSeq());
        }
        return opts.length === 1 ? opts[0] : { type: 'alt', opts };
    };
    const parseSeq = () => {
        const items = [];
        while (!eof() && peek() !== '|' && peek() !== ')') {
            items.push(parseQuant());
        }
        return { type: 'seq', items };
    };
    const lazy = () => {
        if (peek() === '?')
            next();
    };
    const parseQuant = () => {
        const node = parseAtom();
        const c = peek();
        if (c === '*') {
            next();
            lazy();
            return { type: 'rep', node, min: 0, max: Infinity };
        }
        if (c === '+') {
            next();
            lazy();
            return { type: 'rep', node, min: 1, max: Infinity };
        }
        if (c === '?') {
            next();
            lazy();
            return { type: 'rep', node, min: 0, max: 1 };
        }
        if (c === '{') {
            const save = i;
            const m = src.slice(i).match(/^\{(\d*)(?:,(\d*))?\}/);
            if (m) {
                i += m[0].length;
                lazy();
                const min = m[1] === '' ? 0 : Number(m[1]);
                const max = m[2] === undefined ? (m[1] === '' ? Infinity : min) : m[2] === '' ? Infinity : Number(m[2]);
                return { type: 'rep', node, min, max };
            }
            i = save; // not a quantifier; treat '{' as a literal handled by parseAtom next time
        }
        return node;
    };
    const parseEsc = (e) => {
        var _a;
        // Back-references (\1..\9, \k<name>) require memory of captured text and cannot
        // be modeled by a finite automaton.
        if (e >= '1' && e <= '9')
            throw new UnsupportedRegexError('back-reference is not supported');
        if (e === 'k')
            throw new UnsupportedRegexError('named back-reference is not supported');
        const classes = {
            d: ch => ch >= '0' && ch <= '9',
            D: ch => !(ch >= '0' && ch <= '9'),
            w: ch => /[A-Za-z0-9_]/.test(ch),
            W: ch => !/[A-Za-z0-9_]/.test(ch),
            s: ch => /\s/.test(ch),
            S: ch => !/\s/.test(ch),
        };
        if (classes[e])
            return { type: 'char', match: classes[e] };
        const lit = (_a = { n: '\n', t: '\t', r: '\r' }[e]) !== null && _a !== void 0 ? _a : e;
        return { type: 'char', match: ch => ch === lit };
    };
    const parseClass = () => {
        var _a;
        next(); // consume '['
        let neg = false;
        if (peek() === '^') {
            neg = true;
            next();
        }
        const tests = [];
        while (!eof() && peek() !== ']') {
            let ch = next();
            if (ch === '\\') {
                const e = next();
                const classes = {
                    d: c => c >= '0' && c <= '9',
                    w: c => /[A-Za-z0-9_]/.test(c),
                    s: c => /\s/.test(c),
                };
                if (classes[e]) {
                    tests.push(classes[e]);
                    continue;
                }
                ch = (_a = { n: '\n', t: '\t', r: '\r' }[e]) !== null && _a !== void 0 ? _a : e;
            }
            if (peek() === '-' && src[i + 1] !== ']' && i + 1 < src.length) {
                next(); // consume '-'
                const end = next();
                const lo = ch;
                const hi = end;
                tests.push(c => c >= lo && c <= hi);
            }
            else {
                const only = ch;
                tests.push(c => c === only);
            }
        }
        next(); // consume ']'
        return {
            type: 'char',
            match: c => {
                const hit = tests.some(t => t(c));
                return neg ? !hit : hit;
            },
        };
    };
    const parseAtom = () => {
        const c = peek();
        if (c === '(') {
            next();
            if (peek() === '?') {
                const flag = src[i + 1];
                if (flag === ':') {
                    i += 2; // non-capturing group (?:...)
                }
                else if (flag === '<' && src[i + 2] !== '=' && src[i + 2] !== '!') {
                    // Named capturing group (?<name>...) — skip up to and including '>'.
                    i += 2;
                    while (!eof() && peek() !== '>')
                        next();
                    next(); // consume '>'
                }
                else {
                    // Lookahead (?=, (?!, lookbehind (?<=, (?<! — cannot be modeled by an NFA.
                    throw new UnsupportedRegexError('lookaround is not supported');
                }
            }
            const inner = parseAlt();
            next(); // consume ')'
            return inner;
        }
        if (c === '[')
            return parseClass();
        if (c === '^' || c === '$') {
            next();
            return { type: 'anchor' };
        }
        if (c === '.') {
            next();
            return { type: 'char', match: ch => ch !== '\n' };
        }
        if (c === '\\') {
            next();
            return parseEsc(next());
        }
        next();
        return { type: 'char', match: ch => ch === c };
    };
    return parseAlt();
};
// ── Normalizer: expand bounded reps into explicit optional chains ───────────
const cloneNode = (n) => {
    switch (n.type) {
        case 'char':
            return { type: 'char', match: n.match };
        case 'anchor':
            return { type: 'anchor' };
        case 'seq':
            return { type: 'seq', items: n.items.map(cloneNode) };
        case 'alt':
            return { type: 'alt', opts: n.opts.map(cloneNode) };
        case 'rep':
            return { type: 'rep', node: cloneNode(n.node), min: n.min, max: n.max };
        case 'opt':
            return { type: 'opt', node: cloneNode(n.node) };
        case 'star':
            return { type: 'star', node: cloneNode(n.node) };
    }
};
const expand = (node) => {
    switch (node.type) {
        case 'seq':
            return { type: 'seq', items: node.items.map(expand) };
        case 'alt':
            return { type: 'alt', opts: node.opts.map(expand) };
        case 'opt':
            return { type: 'opt', node: expand(node.node) };
        case 'star':
            return { type: 'star', node: expand(node.node) };
        case 'rep': {
            const inner = expand(node.node);
            const min = Math.min(node.min, MAX_QUANTIFIER);
            const items = [];
            for (let k = 0; k < min; k++)
                items.push(cloneNode(inner));
            if (node.max === Infinity) {
                items.push({ type: 'star', node: cloneNode(inner) });
            }
            else {
                // (max - min) nested-optional copies: ( inner ( inner ( ... )? )? )?
                const span = Math.min(node.max, MAX_QUANTIFIER) - min;
                let opt = null;
                for (let k = 0; k < span; k++) {
                    const seqItems = [cloneNode(inner)];
                    if (opt)
                        seqItems.push(opt);
                    opt = { type: 'opt', node: { type: 'seq', items: seqItems } };
                }
                if (opt)
                    items.push(opt);
            }
            return { type: 'seq', items };
        }
        default:
            return node;
    }
};
const compile = (ast) => {
    const mk = () => ({ eps: [], cons: null, to: null, accept: false });
    const patch = (outs, t) => outs.forEach(set => set(t));
    const build = (n) => {
        switch (n.type) {
            case 'char': {
                const s = mk();
                return {
                    start: s,
                    outs: [
                        t => {
                            s.cons = n.match;
                            s.to = t;
                        },
                    ],
                };
            }
            case 'anchor': {
                const s = mk();
                return { start: s, outs: [t => s.eps.push(t)] };
            }
            case 'seq': {
                if (!n.items.length) {
                    const s = mk();
                    return { start: s, outs: [t => s.eps.push(t)] };
                }
                let f = build(n.items[0]);
                for (let k = 1; k < n.items.length; k++) {
                    const g = build(n.items[k]);
                    patch(f.outs, g.start);
                    f = { start: f.start, outs: g.outs };
                }
                return f;
            }
            case 'alt': {
                const s = mk();
                const outs = [];
                n.opts.forEach(o => {
                    const f = build(o);
                    s.eps.push(f.start);
                    outs.push(...f.outs);
                });
                return { start: s, outs };
            }
            case 'opt': {
                const f = build(n.node);
                const s = mk();
                s.eps.push(f.start);
                return { start: s, outs: [...f.outs, t => s.eps.push(t)] };
            }
            case 'star': {
                const s = mk();
                const f = build(n.node);
                s.eps.push(f.start);
                patch(f.outs, s);
                return { start: s, outs: [t => s.eps.push(t)] };
            }
            case 'rep': {
                // Reps are removed by expand(); compile the equivalent star as a safety net.
                return build({ type: 'star', node: n.node });
            }
        }
    };
    const f = build(ast);
    const accept = mk();
    accept.accept = true;
    patch(f.outs, accept);
    return f.start;
};
// ── Simulation ──────────────────────────────────────────────────────────────
const epsClosure = (states) => {
    const stack = [...states];
    const seen = new Set(stack);
    while (stack.length) {
        const s = stack.pop();
        for (const t of s.eps) {
            if (!seen.has(t)) {
                seen.add(t);
                stack.push(t);
            }
        }
    }
    return seen;
};
const stepStates = (states, ch) => {
    const nextStates = new Set();
    for (const s of states) {
        if (s.cons && s.to && s.cons(ch))
            nextStates.add(s.to);
    }
    return epsClosure(nextStates);
};
/**
 * Builds a reusable matcher for a pattern.
 *
 * @param source - The regex source string (anchors `^ $` are accepted and treated
 *   as a single-line, fully-anchored match).
 * @returns A `classify(value)` function returning {@link MatchState}, or `null` if
 *   the pattern is unsupported/invalid (caller should skip masking in that case).
 */
const createIncrementalMatcher = (source) => {
    let start;
    try {
        start = compile(expand(parse(source)));
    }
    catch (_b) {
        return null;
    }
    return (value) => {
        let current = epsClosure([start]);
        for (const ch of value) {
            current = stepStates(current, ch);
            if (current.size === 0)
                return FAILED;
        }
        return [...current].some(s => s.accept) ? DONE : MORE;
    };
};
/** Strips leading `^` and trailing `$` for use where anchors are implied. */
const stripAnchors = (source) => source.replace(/^\^/, '').replace(/\$$/, '');

const tkInputCss = () => `@font-face{font-family:"Material Symbols Outlined";src:url("./assets/fonts/material-symbols/Material-Symbols-Outlined.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-outlined{font-family:"Material Symbols Outlined";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-outlined{font-variation-settings:"FILL" 0}.material-symbols-outlined.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Rounded";src:url("./assets/fonts/material-symbols/Material-Symbols-Rounded.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-rounded{font-family:"Material Symbols Rounded";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-rounded{font-variation-settings:"FILL" 0}.material-symbols-rounded.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Sharp";src:url("./assets/fonts/material-symbols/Material-Symbols-Sharp.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-sharp{font-family:"Material Symbols Sharp";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-sharp{font-variation-settings:"FILL" 0}.material-symbols-sharp.fill{font-variation-settings:"FILL" 1}tk-input{display:block}tk-input.tk-select-input:not(.editable-select) .tk-input,tk-input.tk-select-input:not(.editable-select) .tk-input input{cursor:pointer}tk-input.tk-table-input .tk-input-container .tk-input{height:20px;padding-top:0px;padding-bottom:0px;border-radius:var(--spacing-xs)}tk-input.tk-table-input .tk-input-container .tk-input input{font-size:var(--desktop-body-s-size);line-height:100%}.tk-input-container{display:flex;align-items:flex-start;flex-direction:column;gap:var(--input-external-label-large-container-gap);width:100%;font-family:var(--family-body)}.tk-input-container .label{color:var(--text-darkest);font-weight:400}.tk-input-container .label .asterisk{color:var(--states-danger-base);font-size:var(--desktop-label-s-size);font-weight:300;line-height:var(--desktop-label-s-line-height)}.tk-input-container .tk-input{display:flex;align-items:center;border:1px solid var(--border-light);border-radius:var(--spacing-m-base);background-color:var(--static-light);box-sizing:border-box;width:-webkit-fill-available}.tk-input-container .tk-input .tk-input-clear-button{margin-left:auto}.tk-input-container .tk-input tk-icon{user-select:none}.tk-input-container .tk-input tk-icon.clickable:not(.disabled){cursor:pointer}.tk-input-container .tk-input .tk-input-prefix-container{display:inline-flex;font-weight:300}.tk-input-container .tk-input .tk-input-prefix-container.large{font-size:var(--desktop-body-m-base-size);line-height:var(--desktop-body-m-base-line-height)}.tk-input-container .tk-input .tk-input-prefix-container.base{font-size:var(--desktop-body-s-size);line-height:var(--desktop-body-s-line-height)}.tk-input-container .tk-input .tk-input-prefix-container.small{font-size:var(--desktop-body-xs-size);line-height:var(--desktop-body-xs-line-height)}.tk-input-container .tk-input .tk-input-prefix-container .tk-input-prefix-text{color:var(--text-sub-base);size:inherit}.tk-input-container .tk-input .tk-input-prefix-container .tk-input-divider{position:relative;display:flex;justify-content:center;min-height:100%;margin:0 8px}.tk-input-container .tk-input .tk-input-prefix-container .tk-input-divider::before{content:"";position:absolute;display:block;height:100%;inset-block-start:0;inset-inline-start:50%;border-inline-start:var(--spacing-px) solid var(--background-light);box-sizing:border-box}.tk-input-container .tk-input input{outline:none;border:none;background-color:transparent;color:var(--text-darkest);padding:0px;width:100%;font-family:var(--desktop-body-m-base-font);line-height:var(--desktop-body-m-base-line-height)}.tk-input-container .tk-input input::placeholder{color:var(--text-sub-base)}.tk-input-container .tk-input:hover:not(.tk-input-container[aria-disabled] .tk-input){background-color:var(--background-lightest)}.tk-input-container .safety-status{display:flex;gap:var(--spacing-xs);width:100%;margin-top:var(--spacing-xxs)}.tk-input-container .safety-status .line{width:25%;height:4px;border-radius:var(--radius-m-base);background-color:var(--background-light)}.tk-input-container .safety-status .line.weak{background-color:var(--states-danger-base)}.tk-input-container .safety-status .line.medium{background-color:var(--states-warning-base)}.tk-input-container .safety-status .line.strong{background-color:var(--states-success-base)}.tk-input-container.large .label{font-size:var(--desktop-label-l-size);line-height:var(--desktop-label-l-line-height);padding:var(--input-external-label-large-label-v-padding) var(--input-external-label-large-label-h-padding)}.tk-input-container.large .tk-input{gap:var(--input-external-label-large-input-gap);height:48px;padding:var(--input-external-label-large-input-v-padding) var(--input-external-label-large-input-h-padding)}.tk-input-container.large .tk-input input{font-size:var(--desktop-body-m-base-size);line-height:var(--desktop-body-m-base-line-height)}.tk-input-container.base .label{font-size:var(--desktop-label-m-base-size);line-height:var(--desktop-label-m-base-line-height);padding:var(--input-external-label-base-label-v-padding) var(--input-external-label-base-label-h-padding)}.tk-input-container.base .tk-input{gap:var(--input-external-label-base-input-gap);height:40px;padding:var(--input-external-label-base-input-v-padding) var(--input-external-label-base-input-h-padding)}.tk-input-container.base .tk-input input{font-size:var(--desktop-body-s-size);line-height:var(--desktop-body-s-line-height)}.tk-input-container.small .label{font-size:var(--desktop-label-s-size);line-height:var(--desktop-label-s-line-height);padding:var(--input-external-label-small-label-v-padding) var(--input-external-label-small-label-h-padding)}.tk-input-container.small .tk-input{gap:var(--input-external-label-small-input-gap);height:32px;padding:var(--input-external-label-small-input-v-padding) var(--input-external-label-small-input-h-padding)}.tk-input-container.small .tk-input input{font-size:var(--desktop-body-xs-size);line-height:var(--desktop-body-xs-line-height)}.tk-input-container.focus .tk-input{border-color:var(--states-info-base)}.tk-input-container[aria-disabled]{pointer-events:none}.tk-input-container[aria-disabled] .label,.tk-input-container[aria-disabled] .tk-input input{color:var(--text-sub-base);pointer-events:none}.tk-input-container[aria-disabled] .tk-input{background-color:var(--background-lightest)}.tk-input-container[aria-readonly] .label{color:var(--text-sub-base)}.tk-input-container[aria-readonly] .tk-input{background-color:var(--background-lightest)}.tk-input-container[aria-invalid] .tk-input{border-color:var(--states-danger-base)}.tk-input-container input[type=number]{appearance:none}.tk-input-container{}.tk-input-container input[type=number]::-webkit-outer-spin-button,.tk-input-container input[type=number]::-webkit-inner-spin-button{-webkit-appearance:none;margin:0}.tk-input-container.counter input{text-align:center}.tk-input-container.chips .tk-input{flex-wrap:wrap;height:unset}.tk-input-container.chips .tk-input input{flex:1;width:unset;min-width:30px}`;

const TkInput = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.tkChange = index.createEvent(this, "tk-change", 5);
        this.tkBlur = index.createEvent(this, "tk-blur", 7);
        this.tkFocus = index.createEvent(this, "tk-focus", 7);
        this.tkClearClick = index.createEvent(this, "tk-clear-click", 7);
        if (hostRef.$hostElement$["s-ei"]) {
            this.internals = hostRef.$hostElement$["s-ei"];
        }
        else {
            this.internals = hostRef.$hostElement$.attachInternals();
            hostRef.$hostElement$["s-ei"] = this.internals;
        }
        this.uniqueId = v4.v4();
        this.readOnly = false;
        this.editable = true;
        /** Last value accepted by the regex mask, used to revert a rejected keystroke. */
        this.lastRegexAcceptedValue = '';
        this.hasFocus = false;
        this.isCounter = false;
        this.isPassword = false;
        this.passwordStrength = 0;
        this.hasLabelSlot = false;
        /**
         * the user cannot interact with the input.
         */
        this.disabled = false;
        /**
         * Indicates whether the input is in an invalid state
         * @defaultValue false
         */
        this.invalid = false;
        /**
         * Indicates whether the input can be cleared
         * @defaultValue false
         */
        this.clearable = false;
        /**
         * Defines the position of the icon.
         * @defaultValue left
         */
        this.iconPosition = 'left';
        /**
         * If `true`, the user cannot modify the value.
         * @defaultValue false
         */
        this.readonly = false;
        /**
         * Sets size for the component.
         * @defaultValue base
         */
        this.size = 'base';
        /**
         * if type = password safety status bar visible
         */
        this.showSafetyStatus = false;
        /**
         * Displays a red asterisk (*) next to the label for visual emphasis.
         */
        this.showAsterisk = false;
        /**
         * The key to use for option labels
         * @defaultValue label
         */
        this.chipLabelKey = 'label';
        /**
         * input type
         */
        this.mode = 'text';
        /**
         * Shows a loading spinner on the right side of the input.
         * @defaultValue false
         */
        this.loading = false;
        /**
         * Hides the password lock icon.
         * @defaultValue false
         */
        this.hidePasswordIcon = false;
        this.clampValueByLimit = (value, operation) => {
            if (value === null || value === undefined || isNaN(value)) {
                return null;
            }
            const numValue = Number(value);
            if (this.min !== null && this.min !== undefined && numValue < Number(this.min)) {
                if (operation === 'increment') {
                    return Number(this.min) + 1;
                }
                return Number(this.min);
            }
            if (this.max !== null && this.max !== undefined && numValue > Number(this.max)) {
                if (operation === 'decrement') {
                    return Number(this.max) - 1;
                }
                return Number(this.max);
            }
            return numValue;
        };
        this.handleInput = (ev) => {
            var _a, _b, _c, _d;
            if (this.mode != 'chips') {
                const input = ev.target;
                let _value;
                if (this.mode == 'number' || this.mode == 'counter') {
                    _value = input.value ? Number(input.value) : null;
                }
                else {
                    _value = input.value || '';
                }
                if (this.maskOptions) {
                    // Custom regex mask
                    if (((_a = this.maskOptions) === null || _a === void 0 ? void 0 : _a.regex) && this.mode === 'text') {
                        // The regex describes the final value, so we validate the typed value
                        // incrementally: a complete match (DONE) or a valid prefix (MORE) is
                        // accepted, while an impossible value (FAILED) is rejected and the input
                        // reverts to the last accepted value — never wiping the whole field.
                        const matcher = this.getRegexMatcher();
                        if (matcher) {
                            if (matcher(_value) === FAILED) {
                                // The new value can't be valid. Salvage the longest prefix the matcher
                                // still accepts — this keeps "ABC" when a single bad key is typed and,
                                // on a paste like "AB!CD", keeps the leading "AB" instead of discarding
                                // everything. Falls back to the last accepted value when nothing fits.
                                _value = this.longestAcceptedPrefix(matcher, _value);
                                const caretPos = _value.length;
                                input.value = _value;
                                (_b = input.setSelectionRange) === null || _b === void 0 ? void 0 : _b.call(input, caretPos, caretPos);
                                this.lastRegexAcceptedValue = _value;
                            }
                            else {
                                this.lastRegexAcceptedValue = _value;
                            }
                        }
                    }
                    else {
                        if (this.maskOptions.letterOnly) {
                            // If letterOnly option is enabled, filter out non-letters
                            _value = _value.replace(/[^a-zA-Z]/g, '');
                            input.value = _value;
                        }
                        if (this.cleaveInstance) {
                            // Re-apply the current raw value to force Cleave to re-sync its internal state before reading formatted output.
                            const rawValue = this.cleaveInstance.getRawValue();
                            (_c = this.cleaveInstance) === null || _c === void 0 ? void 0 : _c.setRawValue(rawValue);
                            _value = (_d = this.cleaveInstance) === null || _d === void 0 ? void 0 : _d.getFormattedValue();
                        }
                    }
                }
                if (!isEqual.isEqual(this.value, _value)) {
                    this.value = _value;
                    this.tkChange.emit(_value);
                }
            }
            if (this.mode == 'password' && this.showSafetyStatus) {
                this.passwordStrength = this.calculatePasswordStrength(String(this.value));
            }
        };
        this.handleInputKeyUp = (ev) => {
            var _a, _b;
            const newInput = ev.target;
            if (this.mode === 'counter' && newInput) {
                if (newInput.value === '') {
                    this.nativeInput.value = '';
                }
                else {
                    this.nativeInput.value = (_b = (_a = this.clampValueByLimit(Number(newInput.value))) === null || _a === void 0 ? void 0 : _a.toString()) !== null && _b !== void 0 ? _b : '';
                }
            }
        };
        this.handleInputBlur = () => {
            this.hasFocus = false;
            this.validateMinMax();
            this.tkBlur.emit();
        };
        // for add chip
        this.handleInputKeyDown = (e) => {
            // --- Cleave.js maske ayırıcı karakterlerinin silinmesi için genel çözüm ---
            // Regex masks have no Cleave instance and manage their own value, so this
            // Cleave-specific delimiter handling must not run for them.
            if (this.maskOptions && !this.maskOptions.regex && this.cleaveInstance && this.mode == 'text' && (e.key === 'Backspace' || e.key === 'Delete')) {
                const input = this.nativeInput;
                const value = input.value;
                const selectionStart = input.selectionStart;
                const selectionEnd = input.selectionEnd;
                // Sadece imleç varsa (seçili alan yoksa) işle
                if (selectionStart === selectionEnd) {
                    let charToCheck;
                    let posToRemove = -1;
                    if (e.key === 'Backspace' && selectionStart > 0) {
                        charToCheck = value[selectionStart - 1];
                        posToRemove = selectionStart - 1;
                    }
                    else if (e.key === 'Delete' && selectionStart < value.length) {
                        charToCheck = value[selectionStart];
                        posToRemove = selectionStart;
                    }
                    // Eğer karakter bir ayırıcı ise (rakam/harf değilse)
                    if (this.isMaskDelimiterCharacter(charToCheck)) {
                        e.preventDefault();
                        // Ayırıcıyı ve öncesindeki (Backspace) veya sonrasındaki (Delete) karakteri sil
                        let newValue;
                        let newCaretPos;
                        if (e.key === 'Backspace') {
                            // Ayırıcının öncesindeki karakteri sil
                            newValue = value.slice(0, posToRemove - 1) + value.slice(posToRemove + 1);
                            newCaretPos = posToRemove - 1;
                        }
                        else {
                            // Ayırıcının sonrasındaki karakteri sil
                            newValue = value.slice(0, posToRemove) + value.slice(posToRemove + 2);
                            newCaretPos = posToRemove;
                        }
                        this.cleaveInstance.setRawValue(newValue);
                        const formattedValue = this.cleaveInstance.getFormattedValue();
                        input.value = formattedValue;
                        this.value = formattedValue;
                        this.tkChange.emit(this.value);
                        // DOM güncellendikten sonra imleç pozisyonunu ayarla
                        setTimeout(() => {
                            input.setSelectionRange(newCaretPos, newCaretPos);
                        }, 0);
                        return; // Daha fazla işlem yapma
                    }
                }
            }
            if (e.key == 'Enter' &&
                this.nativeInput.value.trim() &&
                this.mode == 'chips' &&
                // (!this.value || (this.value as string[])?.indexOf(this.nativeInput.value) == -1) &&
                (this.el.classList.contains('allow-custom-value-select') || !this.el.classList.contains('tk-select-input'))) {
                if (this.value) {
                    this.value = [...this.value, this.nativeInput.value];
                    this.tkChange.emit(this.value);
                }
                else {
                    this.value = [this.nativeInput.value];
                    this.tkChange.emit([this.nativeInput.value]);
                }
                this.nativeInput.value = '';
            }
        };
        this.handleClearButtonClick = (e) => {
            if (this.readonly || this.disabled)
                return;
            e.stopPropagation();
            this.handleFormReset();
            this.tkClearClick.emit();
        };
        this.handleClearButtonKeyDown = (e) => {
            // Make clear button accessible via Space and Enter keys
            if (e.key === 'Enter') {
                e.preventDefault();
                e.stopPropagation();
                this.handleFormReset();
                this.tkClearClick.emit();
            }
        };
        this.handleInputFocus = () => {
            this.hasFocus = true;
            this.tkFocus.emit();
        };
        this.handleMouseDown = (event) => {
            this.visiblePassword(event, true);
        };
        this.handleMouseUp = (event) => {
            this.visiblePassword(event, false);
        };
        /**
         * Handles label click:
         * - preventDefault() stops the browser from dispatching a synthetic click
         *   on the associated <input> (caused by htmlFor).
         * - Manual focus() preserves the label-click-to-focus UX.
         */
        this.handleLabelClick = (e) => {
            var _a;
            e.preventDefault();
            (_a = this.nativeInput) === null || _a === void 0 ? void 0 : _a.focus();
        };
    }
    maskOptionsChanged(newValue, oldValue) {
        var _a;
        if (isEqual.isEqual(newValue, oldValue) || !this.nativeInput)
            return;
        (_a = this.cleaveInstance) === null || _a === void 0 ? void 0 : _a.destroy();
        this.cleaveInstance = undefined;
        // Drop any memoized regex matcher so it rebuilds for the new options, and
        // re-seed the revert target from the current field value under the new mask.
        this.regexMatcher = undefined;
        this.regexMatcherSource = undefined;
        this.lastRegexAcceptedValue = '';
        if (this.shouldUseCleave()) {
            this.cleaveInstance = new Cleave_1(this.nativeInput, Object.assign({}, this.maskOptions));
        }
        else {
            this.syncRegexAcceptedValue(this.nativeInput.value);
        }
    }
    valueChanged(newValue, oldValue) {
        if (!isEqual.isEqual(newValue, oldValue) && this.mode !== 'chips') {
            if (typeof newValue === 'object' && typeof oldValue === 'object') {
                this.nativeInput.value = objectUtils.getNestedValue(newValue, this.chipLabelKey);
            }
            else {
                this.nativeInput.value = newValue;
            }
            // A programmatic value bypasses keystroke handling; keep the regex revert
            // target in sync so a later rejected keystroke restores this value.
            this.syncRegexAcceptedValue(this.nativeInput.value);
        }
    }
    componentWillLoad() {
        this.hasLabelSlot = Array.from(this.el.children).some((child) => { var _a; return ((_a = child.getAttribute) === null || _a === void 0 ? void 0 : _a.call(child, 'slot')) === 'label'; });
        // If the tk-input has a tabindex attribute we get the value
        // and pass it down to the native input, then remove it from the
        // tk-input to avoid causing tabbing twice on the same element
        if (this.el.hasAttribute('tabindex')) {
            const tabindex = this.el.getAttribute('tabindex');
            this.tabindex = tabindex !== null ? tabindex : undefined;
            this.el.removeAttribute('tabindex');
        }
        if (this.mode == 'text') {
            this.inputType = 'text';
        }
        else if (this.mode == 'number') {
            this.inputType = 'number';
        }
        else if (this.mode == 'password') {
            this.inputType = 'password';
            this.isPassword = true;
        }
        else if (this.mode == 'counter') {
            this.inputType = 'number';
            this.isCounter = true;
        }
    }
    componentDidLoad() {
        var _a, _b;
        this.nativeInput = this.el.querySelector('input');
        if (this.mode === 'counter') {
            this.nativeInput.value = (_b = (_a = this.clampValueByLimit(this.value)) === null || _a === void 0 ? void 0 : _a.toString()) !== null && _b !== void 0 ? _b : '';
        }
        if (this.shouldUseCleave()) {
            this.cleaveInstance = new Cleave_1(this.nativeInput, Object.assign({}, this.maskOptions));
        }
        else {
            // Seed the regex revert target from any initial value so the first rejected
            // keystroke restores it instead of wiping the field.
            this.syncRegexAcceptedValue(this.nativeInput.value);
        }
    }
    componentDidUpdate() {
        var _a, _b;
        if (this.mode === 'counter') {
            this.nativeInput.value = (_b = (_a = this.clampValueByLimit(this.value)) === null || _a === void 0 ? void 0 : _a.toString()) !== null && _b !== void 0 ? _b : '';
        }
    }
    formResetCallback() {
        this.handleFormReset();
    }
    /**
     * Sets focus on the specified `tk-input`. Use this method instead of the global
     * `input.focus()`.
     */
    async setFocus() {
        var _a;
        (_a = this.nativeInput) === null || _a === void 0 ? void 0 : _a.focus();
    }
    validateMinMax() {
        if (this.min === undefined && this.max === undefined)
            return;
        const numValue = parseFloat(this.value);
        if (isNaN(numValue))
            return;
        if (this.min !== undefined && numValue < Number(this.min)) {
            this.value = this.mode === 'counter' ? Number(this.min) : this.min.toString();
            this.tkChange.emit(this.value);
        }
        else if (this.max !== undefined && numValue > Number(this.max)) {
            this.value = this.mode === 'counter' ? Number(this.max) : this.max.toString();
            this.tkChange.emit(this.value);
        }
    }
    /**
     * Toggles the visibility of the password input field.
     *
     * This method is called when the user interacts with the visibility toggle icon.
     *
     * @param event The mouse event triggered by the user interaction.
     * @param val A boolean value indicating whether to show or hide the password.
     */
    visiblePassword(event, val) {
        if (val) {
            this.nativeInput.type = 'text';
            event.target.innerHTML = 'visibility_off';
        }
        else {
            this.nativeInput.type = 'password';
            event.target.innerHTML = 'visibility';
        }
    }
    /**
     * Calculates the strength of a given password.
     *
     * The password strength is determined based on the following criteria:
     * - Length of at least 8 characters
     * - Presence of uppercase letters
     * - Presence of lowercase letters
     * - Presence of numbers
     * - Presence of special characters
     *
     * Each met criterion increases the strength by 1. The maximum strength is 5.
     *
     * @param password The password to be evaluated.
     * @returns A number representing the password strength.
     */
    calculatePasswordStrength(password) {
        let strength = 0;
        if (password.length >= 8)
            strength++;
        if (/[A-Z]/.test(password))
            strength++;
        if (/[a-z]/.test(password))
            strength++;
        if (/[0-9]/.test(password))
            strength++;
        if (/[^A-Za-z0-9]/.test(password))
            strength++;
        return strength;
    }
    isMaskDelimiterCharacter(charToCheck) {
        var _a;
        const delimiterChars = new Set();
        if (typeof this.maskOptions.delimiter === 'string' && this.maskOptions.delimiter.length > 0) {
            delimiterChars.add(this.maskOptions.delimiter);
        }
        if (Array.isArray(this.maskOptions.delimiters)) {
            this.maskOptions.delimiters.forEach(delimiter => {
                if (typeof delimiter === 'string' && delimiter.length > 0) {
                    delimiterChars.add(delimiter);
                }
            });
        }
        const decimalMark = (_a = this.maskOptions.numeralDecimalMark) !== null && _a !== void 0 ? _a : '.';
        const isConfiguredDelimiter = !!charToCheck && delimiterChars.has(charToCheck);
        const isNumericThousandsDelimiterFallback = !!charToCheck && !!this.maskOptions.numeral && delimiterChars.size === 0 && /[^0-9]/.test(charToCheck) && charToCheck !== decimalMark;
        return !!charToCheck && (isConfiguredDelimiter || isNumericThousandsDelimiterFallback) && charToCheck !== decimalMark;
    }
    /**
     * Whether a Cleave.js instance should back the current mask. Cleave is only used
     * for its own formatting options; a `regex` mask is handled by the incremental
     * matcher instead, so we never build a (useless and side-effecting) Cleave
     * instance for it.
     */
    shouldUseCleave() {
        return this.mode === 'text' && !!this.maskOptions && !this.maskOptions.regex;
    }
    /**
     * Re-seeds `lastRegexAcceptedValue` from a value that bypassed keystroke handling
     * (initial render, programmatic `value` set, mask change). Keeps the revert target
     * in sync so a later rejected keystroke restores the displayed value, not a stale one.
     */
    syncRegexAcceptedValue(value) {
        var _a;
        if (this.mode !== 'text' || !((_a = this.maskOptions) === null || _a === void 0 ? void 0 : _a.regex))
            return;
        const next = value == null ? '' : String(value);
        const matcher = this.getRegexMatcher();
        // Only adopt values the mask would accept; an invalid programmatic value should
        // not become the revert target.
        if (!matcher || matcher(next) !== FAILED) {
            this.lastRegexAcceptedValue = next;
        }
    }
    /**
     * Picks the value to keep when `candidate` is rejected by the regex mask.
     *
     * Defaults to the last accepted value, which correctly handles a single bad key
     * pressed anywhere (the valid suffix is preserved). For a paste that introduces
     * new valid content followed by junk, it instead keeps the longest leading prefix
     * the matcher still accepts when that prefix is longer than the last accepted value.
     */
    longestAcceptedPrefix(matcher, candidate) {
        let best = this.lastRegexAcceptedValue;
        for (let end = candidate.length; end > best.length; end--) {
            const prefix = candidate.slice(0, end);
            if (matcher(prefix) !== FAILED) {
                best = prefix;
                break;
            }
        }
        return best;
    }
    /**
     * Returns the incremental matcher for the current `maskOptions.regex`, rebuilding
     * it only when the regex source changes. Returns null when the regex is invalid or
     * uses syntax we cannot model (caller then skips masking and warns once).
     */
    getRegexMatcher() {
        var _a, _b;
        const regex = (_a = this.maskOptions) === null || _a === void 0 ? void 0 : _a.regex;
        if (!regex)
            return null;
        const source = typeof regex === 'string' ? regex : regex.source;
        if (this.regexMatcherSource !== source) {
            this.regexMatcherSource = source;
            this.regexMatcher = createIncrementalMatcher(stripAnchors(source));
            if (!this.regexMatcher) {
                // eslint-disable-next-line no-console
                console.warn(`[tk-input] maskOptions.regex "${source}" is invalid or unsupported (lookarounds/back-references are not supported); the regex mask is disabled.`);
            }
        }
        return (_b = this.regexMatcher) !== null && _b !== void 0 ? _b : null;
    }
    handleFormReset() {
        if (this.mode === 'chips' && this.chipDisabled && Array.isArray(this.value)) {
            this.value = this.value.filter(item => this.chipDisabled(item));
        }
        else {
            this.value = null;
        }
        this.lastRegexAcceptedValue = '';
        this.tkChange.emit(this.value);
    }
    handleMinusButtonClick() {
        if (!this.disabled) {
            const currentValue = Number(this.value) || 0;
            const newValue = this.clampValueByLimit(currentValue - 1, 'decrement');
            if (newValue !== null && newValue !== Number(this.value)) {
                this.value = newValue;
                this.tkChange.emit(newValue);
            }
        }
    }
    handlePlusButtonClick() {
        if (!this.disabled) {
            let currentValue;
            if (this.value === '' || this.value === null || this.value === undefined) {
                currentValue = !isNil(this.min) ? Number(this.min) : 0;
            }
            else {
                currentValue = Number(this.value);
            }
            const newValue = this.clampValueByLimit(currentValue + 1, 'increment');
            if (newValue !== null && newValue !== Number(this.value)) {
                this.value = newValue;
                this.tkChange.emit(newValue);
            }
        }
    }
    handleChipsRemove(index) {
        if (this.readonly || this.disabled)
            return;
        const chipsArr = [...this.value];
        if (index >= 0 && index < chipsArr.length) {
            chipsArr.splice(index, 1);
            this.value = chipsArr;
            this.tkChange.emit(chipsArr);
        }
    }
    /**
     * Renders the password strength indicator lines.
     *
     * The strength lines visually indicate the password strength:
     * - 1 or 2 filled lines: Weak (red color)
     * - 3 filled lines: Medium (yellow color)
     * - 4 filled lines: Strong (green color)
     *
     * The method creates four lines and assigns a CSS class based on the current password strength.
     *
     * @returns An array of JSX elements representing the strength indicator lines.
     */
    renderStrengthLines() {
        const lines = [];
        for (let i = 0; i < 4; i++) {
            let className = 'line';
            if (i < this.passwordStrength) {
                if (this.passwordStrength < 3)
                    className += ' weak';
                else if (this.passwordStrength < 4)
                    className += ' medium';
                else
                    className += ' strong';
            }
            lines.push(index.h("span", { class: className, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'strength-line', i.toString()) }, "\u00A0"));
        }
        return lines;
    }
    renderChips() {
        var _a;
        if (this.mode == 'chips' && typeof this.value == 'object' && ((_a = this.value) === null || _a === void 0 ? void 0 : _a.length) > 0) {
            return this.value.map((item, index$1) => {
                var _a, _b, _c, _d;
                const itemChipOptions = this.chipOptions || {};
                let isRemovable;
                if (((_a = this.chipDisabled) === null || _a === void 0 ? void 0 : _a.call(this, item)) || this.disabled) {
                    isRemovable = false;
                }
                else if (typeof item === 'object' && item !== null && item.hasOwnProperty('removable')) {
                    isRemovable = item.removable;
                }
                else {
                    isRemovable = true;
                }
                const baseProps = Object.assign(Object.assign({}, itemChipOptions), { removable: isRemovable, key: index$1, autoSelfDestroy: false, value: item, variant: ((_b = itemChipOptions.variant) !== null && _b !== void 0 ? _b : 'neutral'), type: ((_c = itemChipOptions.type) !== null && _c !== void 0 ? _c : 'outlined'), size: ((_d = itemChipOptions.size) !== null && _d !== void 0 ? _d : 'small'), disabled: this.disabled });
                const isIndicator = typeof item === 'object' && item !== null && (item.__isOthersIndicator || item.__isAllIndicator);
                const label = isIndicator ? item.label : typeof item === 'object' ? objectUtils.getNestedValue(item, this.chipLabelKey) : String(item);
                return (index.h("tk-chips", Object.assign({ label: label, "onTk-remove": () => this.handleChipsRemove(index$1) }, baseProps, { dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'chips', index$1.toString()) })));
            });
        }
    }
    renderInput() {
        return (index.h("input", { id: this.uniqueId, ref: el => (this.nativeInput = el), disabled: this.disabled, autoComplete: "off", type: this.inputType, name: this.name, min: this.min, max: this.max, step: this.step, placeholder: this.placeholder || '', readOnly: this.readOnly || !this.editable, tabindex: this.tabindex, value: this.mode === 'chips' ? undefined : typeof this.value === 'object' && this.value !== null ? objectUtils.getNestedValue(this.value, this.chipLabelKey) : this.value, onInput: this.handleInput, onBlur: this.handleInputBlur, onFocus: this.handleInputFocus, onKeyDown: this.handleInputKeyDown, onKeyUp: this.handleInputKeyUp, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'native-input') }));
    }
    renderLabel() {
        var _a;
        let label;
        if (((_a = this.label) === null || _a === void 0 ? void 0 : _a.length) > 0 || this.hasLabelSlot) {
            const asterisk = (index.h("span", { class: "asterisk", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'label-asterisk') }, "*"));
            const labelContent = this.hasLabelSlot ? index.h("slot", { name: "label" }) : this.label;
            label = (index.h("label", { htmlFor: this.uniqueId, class: "label", onClick: this.handleLabelClick, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'label') }, labelContent, this.showAsterisk ? asterisk : ''));
        }
        return label;
    }
    renderAlignmentButtons() {
        let leftButton;
        let rightButton;
        if (this.isCounter) {
            leftButton = (index.h("tk-icon", Object.assign({}, iconUtils.getIconElementProps('remove', {
                class: index$1.classNames('counter-icon clickable', { disabled: this.disabled || Number(this.value) <= Number(this.min) }),
                onClick: this.handleMinusButtonClick.bind(this),
            }, undefined, 'span'), { dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'counter-decrease-icon') })));
            rightButton = (index.h("tk-icon", Object.assign({}, iconUtils.getIconElementProps('add', {
                class: index$1.classNames('counter-icon clickable', { disabled: this.disabled || Number(this.value) >= Number(this.max) }),
                onClick: this.handlePlusButtonClick.bind(this),
            }, undefined, 'span'), { dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'counter-increase-icon') })));
        }
        return { left: leftButton, right: rightButton };
    }
    renderPasswordIcons() {
        let passwordLeftIcon;
        let passwordRightIcon;
        if (this.inputType == 'password') {
            if (!this.hidePasswordIcon) {
                passwordLeftIcon = (index.h("tk-icon", Object.assign({}, iconUtils.getIconElementProps('lock', {
                    color: 'var(--icon-base)',
                }), { dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'password-lock-icon') })));
            }
            passwordRightIcon = (index.h("tk-icon", Object.assign({}, iconUtils.getIconElementProps('visibility', {
                class: 'clickable',
                color: 'var(--icon-base)',
                onMouseDown: this.handleMouseDown,
                onMouseUp: this.handleMouseUp,
            }), { dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'password-visibility-icon') })));
        }
        return { left: passwordLeftIcon, right: passwordRightIcon };
    }
    render() {
        var _a;
        let _leftIcon;
        let _rightIcon;
        let safetyStatus;
        if (this.showSafetyStatus) {
            safetyStatus = (index.h("div", { key: '6e05f5263d22c2d15e15d9a768f44ad5141befba', class: "safety-status", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'safety-status') }, this.renderStrengthLines()));
        }
        const rootClasses = index$1.classNames('tk-input-container', this.size, { focus: this.hasFocus, counter: this.isCounter, chips: this.mode == 'chips' });
        const prefixClass = index$1.classNames('tk-input-prefix-container', this.size);
        // Handle icon rendering using utility function
        if (this.icon && !this.isCounter) {
            const { leftIcon, rightIcon } = iconUtils.renderIcons(this.icon, { additionalProps: { color: 'var(--icon-base)' }, dataTestid: this.dataTestid }, this.iconPosition);
            _leftIcon = leftIcon;
            _rightIcon = rightIcon;
        }
        const showClearButton = this.clearable && !this.readonly && ((this.mode !== 'chips' && this.value) || (this.mode === 'chips' && ((_a = this.value) === null || _a === void 0 ? void 0 : _a.length) > 0));
        if (this.el.classList.contains('tk-select-input')) {
            this.readOnly = this.el.classList.contains('readonly-select');
            // readOnly hides clear button and makes input readOnly(disables input), editable only impacts input readOnly
            this.editable = this.el.classList.contains('editable-select');
        }
        else {
            this.readOnly = this.readonly;
        }
        return (index.h("div", { key: '7f0aba56ba2085b7c84c135761b965f3a5427e48', "aria-readonly": this.readonly, "aria-disabled": this.disabled, "aria-invalid": this.invalid, class: rootClasses, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'container') }, this.renderLabel(), index.h("div", { key: 'c7484ee434ece1bee8882f9ac716356491d784b8', class: "tk-input", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'control') }, this.renderChips(), !_leftIcon && this.renderPasswordIcons().left, _leftIcon, this.renderAlignmentButtons().left, this.pre && (index.h("div", { key: '8c36c9048c2d4cf7016e8e1ae1aef112229694fe', class: prefixClass, "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'prefix') }, index.h("span", { key: '55dbdec3f509190367ad85c1250b80a4ba58657a', class: "tk-input-prefix-text", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'prefix-text') }, this.pre), index.h("span", { key: '04d818bda405ea9c7594aa517fe007fb02bca5b5', class: "tk-input-divider", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'prefix-divider') }))), this.renderInput(), this.loading && (index.h("tk-spinner", { key: 'a058dbf05b428a127091947a29c4b729c748ab6f', size: this.size === 'large' ? 'small' : this.size === 'base' ? 'xsmall' : 'xxsmall', dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'loading-spinner') })), showClearButton && (index.h("tk-icon", Object.assign({ key: '233f068cae5c3cc9659deaf808b1235facf7ac51' }, iconUtils.getIconElementProps('close', {
            class: index$1.classNames('tk-input-clear-button clickable', { disabled: this.disabled || this.readonly }),
            color: 'var(--icon-base)',
            onClick: this.handleClearButtonClick,
            onKeyDown: this.handleClearButtonKeyDown,
            tabindex: this.disabled || this.readonly ? -1 : 0,
        }), { dataTestid: testIdUtils.getDataTestId(this.dataTestid, 'clear-icon') }))), _rightIcon, !_rightIcon && this.renderPasswordIcons().right, this.renderAlignmentButtons().right), safetyStatus, hintUtils.renderHint(this.hint, this.error, this.invalid, this.dataTestid)));
    }
    static get formAssociated() { return true; }
    get el() { return index.getElement(this); }
    static get watchers() { return {
        "maskOptions": [{
                "maskOptionsChanged": 0
            }],
        "value": [{
                "valueChanged": 0
            }]
    }; }
};
TkInput.style = tkInputCss();

const tkSelectCss = () => `@font-face{font-family:"Material Symbols Outlined";src:url("./assets/fonts/material-symbols/Material-Symbols-Outlined.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-outlined{font-family:"Material Symbols Outlined";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-outlined{font-variation-settings:"FILL" 0}.material-symbols-outlined.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Rounded";src:url("./assets/fonts/material-symbols/Material-Symbols-Rounded.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-rounded{font-family:"Material Symbols Rounded";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-rounded{font-variation-settings:"FILL" 0}.material-symbols-rounded.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Sharp";src:url("./assets/fonts/material-symbols/Material-Symbols-Sharp.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-sharp{font-family:"Material Symbols Sharp";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-sharp{font-variation-settings:"FILL" 0}.material-symbols-sharp.fill{font-variation-settings:"FILL" 1}tk-select{display:block}.tk-select-container .tk-select-panel{position:fixed;max-height:320px;overflow:hidden;border-radius:16px;border:1px solid var(--border-light);background:var(--static-light);z-index:800;box-shadow:var(--effect-1-default-sm);font-family:var(--family-body)}.tk-select-container .tk-select-panel .dropdown-item-holder{margin:8px;padding:1px;overflow-y:auto;display:flex;flex-direction:column;gap:6px}.tk-select-container .tk-select-panel .dropdown-item-holder::-webkit-scrollbar{width:4px;height:4px}.tk-select-container .tk-select-panel .dropdown-item-holder::-webkit-scrollbar-track{background:var(--background-lightest);border-radius:2px}.tk-select-container .tk-select-panel .dropdown-item-holder::-webkit-scrollbar-thumb{border-radius:3px;background:var(--secondary-sub-base)}.tk-select-container .tk-select-panel .dropdown-item-holder::-webkit-scrollbar-thumb:hover{background:var(--secondary-dark)}.tk-select-container .tk-select-panel .dropdown-item-holder::-webkit-scrollbar-corner{background:var(--background-lightest)}.tk-select-container .tk-select-panel .dropdown-item-holder .dropdown-item-top{position:sticky;top:0;z-index:1}.tk-select-container .tk-select-panel .dropdown-item-holder .dropdown-group{display:flex;flex-direction:column;gap:6px}.tk-select-container .tk-select-panel .dropdown-item-holder .dropdown-group .dropdown-group-label{display:flex;align-items:center;gap:16px;color:var(--text-dark);font-size:var(--desktop-body-xs-size);padding:0px 8px}.tk-select-container .tk-select-panel .dropdown-item-holder .dropdown-group .dropdown-group-label label{white-space:nowrap}.tk-select-container .tk-select-panel .dropdown-item-holder .dropdown-group .dropdown-group-label .line{width:100%;height:1px;background-color:var(--background-light)}.tk-select-container .tk-select-panel .dropdown-item-holder .dropdown-item{color:var(--text-darkest);border-radius:8px;padding:var(--dropdown-items-basic-base-v-padding) var(--dropdown-items-basic-base-h-padding, 8px)}.tk-select-container .tk-select-panel .dropdown-item-holder .dropdown-item.multiple{display:flex;align-items:center;gap:8px}.tk-select-container .tk-select-panel .dropdown-item-holder .dropdown-item .multiple-option-content{flex:1}.tk-select-container .tk-select-panel .dropdown-item-holder .dropdown-item[data-selected=true]{cursor:pointer;background:var(--states-info-lightest);outline:1px solid var(--states-info-light)}.tk-select-container .tk-select-panel .dropdown-item-holder .dropdown-item.disabled{pointer-events:none;cursor:not-allowed;background:var(--background-lightest);color:var(--text-sub-base);outline:none}.tk-select-container .tk-select-panel .dropdown-item-holder .dropdown-item:hover{cursor:pointer;background:var(--background-lightest)}.tk-select-container .tk-select-panel .dropdown-item-holder .dropdown-item[data-active=true]{cursor:pointer;outline:1px solid var(--background-light)}.tk-select-container.large .dropdown-item-holder{max-height:272px}.tk-select-container.large .dropdown-item-holder .dropdown-item{font-size:var(--desktop-body-m-base-size)}.tk-select-container.large .dropdown-item-holder tk-spinner{min-height:48px}.tk-select-container.base .dropdown-item-holder{max-height:254px}.tk-select-container.base .dropdown-item-holder .dropdown-item{font-size:var(--desktop-body-s-size)}.tk-select-container.base .dropdown-item-holder tk-spinner{min-height:38px}.tk-select-container.small .dropdown-item-holder{max-height:236px}.tk-select-container.small .dropdown-item-holder .dropdown-item{font-size:var(--desktop-body-xs-size)}.tk-select-container.small .dropdown-item-holder tk-spinner{min-height:28px}`;

const TkSelect = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.tkChange = index.createEvent(this, "tk-change", 7);
        this.tkSelectAll = index.createEvent(this, "tk-select-all", 7);
        this.tkOpen = index.createEvent(this, "tk-open", 3);
        this.tkClose = index.createEvent(this, "tk-close", 3);
        if (hostRef.$hostElement$["s-ei"]) {
            this.internals = hostRef.$hostElement$["s-ei"];
        }
        else {
            this.internals = hostRef.$hostElement$.attachInternals();
            hostRef.$hostElement$["s-ei"] = this.internals;
        }
        this.hasEmptyDataSlot = false;
        this.uniqueId = v4.v4();
        this.isItemClickFlag = false;
        this.flatOptions = [];
        this.isOpen = false;
        /**
         * The key to use for option group names.
         * Required if grouped options are used.
         */
        this.groupNameKey = 'label';
        /**
         * The key to use for accessing grouped options array.
         * Required if grouped options are used.
         */
        this.groupOptionsKey = 'options';
        /**
         * Represents whether the options are fethecd from service or not.
         * If true renders spinner in options dropdown.
         */
        this.loading = false;
        /**
         * Enables users to enter custom values that are not part of the predefined options.
         * @defaultValue false
         */
        this.allowCustomValue = false;
        /**
         * Indicates whether the input can be cleared
         * @defaultValue false
         */
        this.clearable = false;
        /**
         * If `true`, the user cannot interact with the input.
         * @defaultValue false
         */
        this.disabled = false;
        /**
         * Determines the width of the dropdown. Accepts values like 'match-parent', 'auto', or a specific width in '300px'.
         * @defaultValue match-parent
         */
        this.dropdownWidthMode = 'match-parent';
        /**
         * This property determines whether the input field within the select box is editable.
         * @defaultValue false
         */
        this.editable = false;
        /**
         * The message to display when there is no data available.
         */
        this.emptyMessage = 'No options available';
        /**
         *  Function used to filter current options based on the input value. Comes with a default filter function, but can be overridden with a custom function.
         */
        this.filter = this.defaultFilter;
        /**
         * Sets the delay (in ms) before triggering the filter function.
         */
        this.filterDebounceDelay = 0;
        /**
         * Indicates whether the input is in an invalid state
         * @defaultValue false
         */
        this.invalid = false;
        /**
         * If `true`, the user cannot modify the value.
         * @defaultValue false
         */
        this.readonly = false;
        /**
         * Sets size for the component.
         * @defaultValue base
         */
        this.size = 'base';
        /**
         * Displays a red asterisk (*) next to the label for visual emphasis.
         */
        this.showAsterisk = false;
        /**
         * The key to use for option labels
         * @defaultValue label
         */
        this.optionLabelKey = 'label';
        /**
         * If true enables selectAll option
         *  @defaultValue false
         */
        this.selectAll = false;
        /**
         * Sets the label of the selectAll option
         *  @defaultValue 'All'
         */
        this.selectAllLabel = 'All';
        /**
         * If `true`, collapses the selection into a single chip labelled with `selectAllLabel`
         * once every option is selected.
         * @defaultValue false
         */
        this.showSelectAllChip = false;
        /**
         * Click outside handler implementation - called by the mixin
         */
        this.closeHandler = () => {
            this.isOpen = false;
        };
        this.boundRunFilterForMultiple = this.runFilterForMultiple.bind(this);
    }
    isOpenChanged(newValue) {
        if (newValue)
            this.tkOpen.emit();
        else
            this.tkClose.emit();
    }
    disabledChanged(newValue) {
        if (newValue)
            this.isOpen = false;
    }
    optionsChanged(newValue, oldValue) {
        var _a;
        if (isEqual.isEqual(newValue, oldValue))
            return;
        this.renderOptions = ((_a = this.options) === null || _a === void 0 ? void 0 : _a.length) > 0 ? [...this.options] : [];
        this.setValue();
    }
    /**
     * Update the native input element when the value changes
     */
    valueChanged(newValue, oldValue) {
        if (isEqual.isEqual(newValue, oldValue))
            return;
        this.setValue();
        if (this.multiple && this.selectAll) {
            const newValues = Array.isArray(newValue) ? newValue : [];
            this.tkSelectAll.emit(this.isAllSelected(newValues));
        }
    }
    componentWillLoad() {
        var _a;
        this.hasEmptyDataSlot = !!this.el.querySelector('[slot="empty-data"]');
        this.setFlatOptions();
        this.renderOptions = ((_a = this.options) === null || _a === void 0 ? void 0 : _a.length) > 0 ? [...this.options] : [];
    }
    componentDidLoad() {
        var _a, _b;
        (_b = (_a = this.internals) === null || _a === void 0 ? void 0 : _a.form) === null || _b === void 0 ? void 0 : _b.addEventListener('reset', () => {
            this.handleFormReset();
        });
        this.nativeInputRef = this.inputRef.querySelector('input');
        const tkInputArea = this.inputRef.querySelector('.tk-input');
        this.clickOutsideMixin = new clickoutsideMixin.ClickOutsideMixin({
            referenceElement: tkInputArea !== null && tkInputArea !== void 0 ? tkInputArea : this.el,
            handler: this.closeHandler,
            disabled: this.disabled || this.readonly || !this.isOpen,
        });
        if (this.allowCustomValue) {
            this.editable = true;
        }
        if (this.value !== undefined && this.value !== null) {
            this.setValue();
        }
    }
    componentDidRender() {
        var _a, _b, _c;
        // multiple durumda chips li input çalıştığı için ve tk-input value olarak chips leri geri döndürdüğü için
        // tk-input'un içindeki inputa yazılan değerlerin filtering için çalışabilmesini sağlamak için yapılmıştır.
        if (this.readonly) {
            const nativeInput = (_a = this.inputRef) === null || _a === void 0 ? void 0 : _a.querySelector('input');
            if (nativeInput) {
                nativeInput.setAttribute('readonly', 'true');
            }
        }
        if (this.multiple && this.editable) {
            (_b = this.nativeInputRef) === null || _b === void 0 ? void 0 : _b.removeEventListener('input', this.boundRunFilterForMultiple);
            (_c = this.nativeInputRef) === null || _c === void 0 ? void 0 : _c.addEventListener('input', this.boundRunFilterForMultiple);
        }
    }
    componentDidUpdate() {
        var _a, _b, _c, _d;
        this.nativeInputRef = this.inputRef.querySelector('input');
        // Update click outside mixin configuration based on current state
        (_a = this.clickOutsideMixin) === null || _a === void 0 ? void 0 : _a.updateConfig({
            disabled: this.disabled || this.readonly || !this.isOpen,
            ignoredElements: this.panelRef ? [this.panelRef] : [],
        });
        if (this.isOpen) {
            if (this.inputRef && this.panelRef) {
                // Clean up old floating UI listeners before setting up new ones
                (_b = this.cleanup) === null || _b === void 0 ? void 0 : _b.call(this);
                this.updatePosition();
                this.setFlatOptions();
                // Panel açıldığında ilk itemin active olmasını sağlamak için
                const activeItem = this.el.querySelector('.dropdown-item[data-active]');
                if (!activeItem && !this.allowCustomValue) {
                    const firstEnabledIndex = this.getNextEnabledItemIndex(-1, 'down');
                    if (firstEnabledIndex !== null) {
                        const firstItem = this.el.querySelector(`.dropdown-item[data-option-index="${firstEnabledIndex}"]`);
                        firstItem === null || firstItem === void 0 ? void 0 : firstItem.setAttribute('data-active', 'true');
                    }
                }
            }
        }
        else {
            // Remove floating UI listeners when select closes
            (_c = this.cleanup) === null || _c === void 0 ? void 0 : _c.call(this);
            // Clear reference to allow garbage collection
            this.cleanup = null;
            (_d = this.panelRef) === null || _d === void 0 ? void 0 : _d.remove();
        }
    }
    disconnectedCallback() {
        var _a, _b, _c, _d;
        // Clean up floating UI listeners on component unmount
        (_a = this.cleanup) === null || _a === void 0 ? void 0 : _a.call(this);
        // Clear reference to allow garbage collection
        this.cleanup = null;
        (_c = (_b = this.internals) === null || _b === void 0 ? void 0 : _b.form) === null || _c === void 0 ? void 0 : _c.removeEventListener('reset', this.handleFormReset.bind(this));
        // Call mixin's disconnectedCallback for cleanup
        (_d = this.clickOutsideMixin) === null || _d === void 0 ? void 0 : _d.disconnectedCallback();
    }
    formResetCallback() {
        this.handleFormReset();
    }
    /**
     * Closes the select dropdown;
     */
    async close() {
        this.isOpen = false;
    }
    updatePosition() {
        const dropdownWidthMode = this.dropdownWidthMode;
        const tkInputRootEl = this.inputRef.querySelector('.tk-input');
        this.cleanup = positionUtils.floatingElementAutoUpdate(tkInputRootEl, this.panelRef, undefined, {
            placement: 'bottom-start',
            size: {
                apply({ rects, elements }) {
                    if (dropdownWidthMode === 'match-parent') {
                        positionUtils.applyStyles(elements.floating, {
                            width: `${rects.reference.width}px`,
                        });
                    }
                    else if (dropdownWidthMode !== 'auto' && dropdownWidthMode.length > 0) {
                        positionUtils.applyStyles(elements.floating, {
                            width: dropdownWidthMode,
                        });
                    }
                },
            },
        });
    }
    isGrouped() {
        var _a, _b;
        return ((_a = this.options) === null || _a === void 0 ? void 0 : _a.length) > 0 && ((_b = this.options[0]) === null || _b === void 0 ? void 0 : _b[this.groupOptionsKey]);
    }
    setFlatOptions() {
        if (this.isGrouped()) {
            this.flatOptions = this.options.flatMap(group => group[this.groupOptionsKey]);
        }
        else {
            this.flatOptions = this.options;
        }
    }
    async runFilterForMultiple() {
        this.renderOptions = await this.filter(this.nativeInputRef.value, this.options);
    }
    async defaultFilter(text, options) {
        if (!text) {
            return [...this.options];
        }
        if (this.isGrouped()) {
            return options
                .map(group => (Object.assign(Object.assign({}, group), { [this.groupOptionsKey]: group[this.groupOptionsKey].filter(option => this.getOptionLabel(option).toLowerCase().includes(text.toLowerCase())) })))
                .filter(group => group[this.groupOptionsKey].length > 0);
        }
        else {
            return options.filter(item => this.getOptionLabel(item).toLowerCase().indexOf(text.toLowerCase()) > -1);
        }
    }
    isOptionSelected(valueArr, optionValue) {
        if (typeof optionValue === 'object' && !Array.isArray(optionValue) && optionValue !== null) {
            return valueArr.some(v => isEqual.isEqual(v, optionValue));
        }
        else {
            return valueArr.includes(optionValue);
        }
    }
    //edited to omit disabled options from select all check
    isAllSelected(valueArr) {
        var _a, _b;
        let arr;
        if (Array.isArray(valueArr)) {
            arr = valueArr;
        }
        else if (Array.isArray((_b = (_a = this.value) === null || _a === void 0 ? void 0 : _a.filter) === null || _b === void 0 ? void 0 : _b.call(_a, item => { var _a; return !((_a = this.optionDisabled) === null || _a === void 0 ? void 0 : _a.call(this, item)); }))) {
            arr = this.value;
        }
        else {
            arr = [];
        }
        const optionValues = this.flatOptions.filter(item => { var _a; return !((_a = this.optionDisabled) === null || _a === void 0 ? void 0 : _a.call(this, item)); }).map(opt => this.getOptionValue(opt));
        return optionValues.length > 0 && optionValues.every(val => this.isOptionSelected(arr, val));
    }
    getOptionLabel(item) {
        if (typeof item === 'object' && item !== null) {
            const label = objectUtils.getNestedValue(item, this.optionLabelKey);
            return label != null ? String(label) : '';
        }
        return item != null ? String(item) : '';
    }
    getOptionValue(item) {
        var _a;
        if (typeof item === 'object' && item !== null) {
            if (((_a = this.optionValueKey) === null || _a === void 0 ? void 0 : _a.length) > 0) {
                return objectUtils.getNestedValue(item, this.optionValueKey);
            }
            else {
                return item;
            }
        }
        else {
            return item;
        }
    }
    // Keep only the currently-selected values whose option is disabled. optionDisabled expects the
    // option object, so when optionValueKey is set each stored value is resolved back to its option
    // before testing it — a raw value key would never look disabled and the item would be dropped.
    getSelectedDisabledValues() {
        if (!Array.isArray(this.value) || !this.optionDisabled)
            return [];
        return this.value.filter(val => {
            const option = this.optionValueKey ? this.flatOptions.find(opt => this.getOptionValue(opt) === val) : val;
            return option != null && this.optionDisabled(option);
        });
    }
    // allowCustomValue is excluded on purpose: a custom value lives outside the option list, so the
    // selection can be a superset of "all" — which the collapsed chip can neither state nor let the
    // user edit. isAllSelected() only inspects the options, so it cannot see those extras either.
    isAllChipActive() {
        return this.showSelectAllChip && this.multiple && this.selectAll && !this.allowCustomValue && this.isAllSelected();
    }
    getDisplayValueForMultiple(selectedItems) {
        if (this.isAllChipActive()) {
            return [
                {
                    __isAllIndicator: true,
                    label: this.selectAllLabel,
                    removable: !this.readonly,
                },
            ];
        }
        if (!this.visibleItemCount || selectedItems.length <= this.visibleItemCount) {
            return selectedItems;
        }
        // When allowCustomValue is true, prioritize predefined options over custom values
        if (this.allowCustomValue) {
            // Separate predefined options from custom values
            const predefinedItems = selectedItems.filter(item => {
                return this.flatOptions.some(opt => {
                    if (this.optionValueKey) {
                        return this.getOptionValue(opt) === this.getOptionValue(item);
                    }
                    else {
                        return isEqual.isEqual(opt, item);
                    }
                });
            });
            const customItems = selectedItems.filter(item => {
                return !this.flatOptions.some(opt => {
                    if (this.optionValueKey) {
                        return this.getOptionValue(opt) === this.getOptionValue(item);
                    }
                    else {
                        return isEqual.isEqual(opt, item);
                    }
                });
            });
            // Take predefined items first, then custom items if there's room
            const visibleItems = [...predefinedItems.slice(0, this.visibleItemCount), ...customItems.slice(0, Math.max(0, this.visibleItemCount - predefinedItems.length))];
            if (selectedItems.length > this.visibleItemCount) {
                const remainingCount = selectedItems.length - this.visibleItemCount;
                const othersIndicator = {
                    __isOthersIndicator: true,
                    label: `+${remainingCount}`,
                    removable: false,
                };
                return [...visibleItems, othersIndicator];
            }
            return visibleItems;
        }
        // Original logic for when allowCustomValue is false
        const visibleItems = selectedItems.slice(0, this.visibleItemCount);
        const remainingCount = selectedItems.length - this.visibleItemCount;
        const othersIndicator = {
            __isOthersIndicator: true,
            label: `+${remainingCount}`,
            removable: false,
        };
        return [...visibleItems, othersIndicator];
    }
    async setRenderOptions(value) {
        this.renderOptions = await this.filter(value, this.options);
    }
    getSelectedItem() {
        var _a, _b;
        if (((_a = this.renderOptions) === null || _a === void 0 ? void 0 : _a.length) > 0) {
            let searchOptions = this.renderOptions;
            if (this.isGrouped()) {
                searchOptions = this.renderOptions.flatMap(group => group[this.groupOptionsKey] || []);
            }
            if (typeof this.value != 'object' && (searchOptions === null || searchOptions === void 0 ? void 0 : searchOptions.every(item => typeof item != 'object'))) {
                // value ve her bir option object değilse. Yani bu primitive tiplerle çalışan bir selectbox ise
                return searchOptions.find(item => item == this.value);
            }
            else if (searchOptions === null || searchOptions === void 0 ? void 0 : searchOptions.every(item => typeof item === 'object')) {
                if (((_b = this.optionValueKey) === null || _b === void 0 ? void 0 : _b.length) > 0) {
                    return searchOptions.find(item => this.getOptionValue(item) == this.value);
                }
                else {
                    return searchOptions.find(item => isEqual.isEqual(item, this.value));
                }
            }
            else {
                return null;
            }
        }
        else {
            return null;
        }
    }
    setValue() {
        var _a, _b, _c, _d, _e;
        if (!this.inputRef)
            return;
        this.setFlatOptions();
        // Handle multiple selection case
        if (this.multiple) {
            // Ensure value is always an array
            const currentValue = Array.isArray(this.value) ? this.value : [];
            this.selectedItem = currentValue
                .map(val => {
                let found;
                if (this.optionValueKey) {
                    found = this.flatOptions.find(opt => this.getOptionValue(opt) === val);
                }
                else {
                    found = this.flatOptions.find(opt => isEqual.isEqual(opt, val));
                }
                if (found !== undefined)
                    return found;
                if (this.allowCustomValue)
                    return val;
                return null;
            })
                .filter(val => val !== null && val !== undefined);
            // Apply visible item count logic for display
            const displayValue = this.getDisplayValueForMultiple(this.selectedItem);
            this.inputRef.value = displayValue;
            return;
        }
        // Handle single selection case
        if (this.editable && this.allowCustomValue) {
            // For editable with custom values, show the value directly
            if (this.value !== undefined && this.value !== null) {
                this.inputRef.value = this.getOptionLabel(this.value);
            }
            else {
                this.inputRef.value = null;
            }
            return;
        }
        // Find the selected item based on value type
        if (typeof this.value !== 'object' && ((_a = this.flatOptions) === null || _a === void 0 ? void 0 : _a.every(item => typeof item !== 'object'))) {
            // Handle primitive values
            this.selectedItem = (_b = this.flatOptions) === null || _b === void 0 ? void 0 : _b.find(item => item === this.value);
        }
        else if (((_c = this.optionValueKey) === null || _c === void 0 ? void 0 : _c.length) > 0) {
            // Handle object values with optionValueKey
            this.selectedItem = (_d = this.flatOptions) === null || _d === void 0 ? void 0 : _d.find(item => this.getOptionValue(item) === this.value);
        }
        else {
            // Handle object values without optionValueKey
            this.selectedItem = (_e = this.flatOptions) === null || _e === void 0 ? void 0 : _e.find(item => isEqual.isEqual(item, this.value));
        }
        // Set input value based on selection state
        if (this.selectedItem !== null && this.selectedItem !== undefined) {
            if (this.multiple) {
                this.inputRef.value = this.selectedItem;
            }
            else {
                const label = this.getOptionLabel(this.selectedItem);
                this.inputRef.value = label;
            }
        }
        else {
            if (this.filter) {
                this.selectedItem = this.getSelectedItem();
                if (this.selectedItem) {
                    if (this.multiple) {
                        this.inputRef.value = this.selectedItem;
                    }
                    else {
                        this.inputRef.value = this.getOptionLabel(this.selectedItem);
                    }
                }
                else {
                    this.inputRef.value = null;
                }
            }
            else {
                this.inputRef.value = null;
            }
        }
    }
    scrollItem(item) {
        item.scrollIntoView({
            behavior: 'smooth',
            block: 'nearest',
        });
    }
    getNextEnabledItemIndex(currentIndex, direction, visited = 0) {
        const totalItems = this.flatOptions.length;
        const maxSteps = this.selectAll && this.multiple ? totalItems + 1 : totalItems;
        if (visited >= maxSteps)
            return null;
        const startIndex = this.selectAll && this.multiple ? -1 : 0;
        const endIndex = totalItems - 1;
        let nextIndex = direction === 'down' ? currentIndex + 1 : currentIndex - 1;
        if (nextIndex > endIndex) {
            nextIndex = startIndex;
        }
        else if (nextIndex < startIndex) {
            nextIndex = endIndex;
        }
        if (nextIndex === -1 && this.selectAll && this.multiple) {
            return -1;
        }
        const item = this.flatOptions[nextIndex];
        const isDisabled = this.optionDisabled ? this.optionDisabled(item) : false;
        if (isDisabled)
            return this.getNextEnabledItemIndex(nextIndex, direction, visited + 1);
        else
            return nextIndex;
    }
    navigateToItem(direction) {
        const activeItem = this.el.querySelector('.dropdown-item[data-active="true"]');
        const activeIndex = activeItem ? Number(activeItem.getAttribute('data-option-index')) : null;
        // activeindex varsa o indexten başlayarak yönüne göre sonraki enabled itemi bul, yoksa aşağı yönünde ilk enabled itemi bul
        const nextIndex = activeIndex !== null ? this.getNextEnabledItemIndex(activeIndex, direction) : this.getNextEnabledItemIndex(-1, 'down');
        if (nextIndex !== null) {
            const newActiveItem = this.el.querySelector(`.dropdown-item[data-option-index='${nextIndex}']`);
            if (newActiveItem) {
                activeItem === null || activeItem === void 0 ? void 0 : activeItem.setAttribute('data-active', 'false');
                newActiveItem.setAttribute('data-active', 'true');
                this.scrollItem(newActiveItem);
            }
        }
    }
    handleFormReset() {
        if (this.multiple && this.optionDisabled && Array.isArray(this.value)) {
            this.value = this.getSelectedDisabledValues();
        }
        else {
            this.value = null;
        }
        this.tkChange.emit(this.value);
    }
    async handleSelectAllClick() {
        var _a;
        if (this.readonly)
            return;
        if (this.multiple) {
            let tmpValue;
            const checking = this.isAllSelected();
            if (checking) {
                // Deselect all
                tmpValue = this.getSelectedDisabledValues();
                this.tkSelectAll.emit(false);
            }
            else {
                //optionsdaki değerleri almak için
                const optionValues = this.flatOptions.filter(item => { var _a; return !((_a = this.optionDisabled) === null || _a === void 0 ? void 0 : _a.call(this, item)); }).map(opt => this.getOptionValue(opt));
                // allowcustom trueyken optionsda olmayan valueların eklenmesi için
                const customValues = Array.isArray(this.value) ? (_a = this.value) === null || _a === void 0 ? void 0 : _a.filter(val => !this.isOptionSelected(optionValues, val)) : [];
                // Select all (optionValue + custom values)
                tmpValue = [...optionValues, ...customValues];
                this.tkSelectAll.emit(true);
            }
            // filtreleme ardında yapılan seçimden sonra filtrelem için kullandığımız tk-input içerisindeki native inputu temizleme işlemi
            if (this.multiple && this.editable) {
                this.nativeInputRef.value = null;
                this.renderOptions = await this.filter(null, this.options);
            }
            // Prevent adding 'all' item to value
            this.inputRef.value = [...tmpValue];
            this.value = [...tmpValue];
            this.tkChange.emit([...tmpValue]);
        }
        this.isItemClickFlag = false;
    }
    async handleItemClick(item) {
        var _a;
        if (this.readonly || ((_a = this.optionDisabled) === null || _a === void 0 ? void 0 : _a.call(this, item)))
            return;
        if (this.multiple) {
            const tmpValue = Array.isArray(this.value) ? [...this.value] : [];
            const tmpItem = this.getOptionValue(item);
            if (objectUtils.some(tmpValue, itemValue => isEqual.isEqual(itemValue, this.getOptionValue(tmpItem)))) {
                // tıklanan item zaten seçili ise seçimi kaldırır
                remove(tmpValue, itemValue => isEqual.isEqual(itemValue, tmpItem));
            }
            else {
                // tıklanan item seçili değilse ekler
                tmpValue.push(tmpItem);
            }
            // filtreleme ardında yapılan seçimden sonra filtrelem için kullandığımız tk-input içerisindeki native inputu temizleme işlemi
            if (this.multiple && this.editable) {
                this.nativeInputRef.value = null;
            }
            this.inputRef.value = [...tmpValue];
            this.value = [...tmpValue];
            this.tkChange.emit([...tmpValue]);
        }
        else {
            this.value = this.getOptionValue(item);
            this.tkChange.emit(this.getOptionValue(item));
            this.isOpen = false;
        }
        // seçim yapıldıktan sonra eğer filtreleme yapılarak bir seçim yapıldıysa eski filtreleme sonuçlarının tutulmaması ve tüm listesinin optionlarda render edilebilmesi için yapılmıştır.
        this.renderOptions = await this.filter(null, this.options);
        this.isItemClickFlag = false;
    }
    async handleInputChange(value) {
        if (this.multiple) {
            // Selection is collapsed into the single "all" chip, so removing that chip is the
            // only change the input can report here. It clears everything but the disabled items.
            if (this.isAllChipActive()) {
                const incomingChips = value == null ? [] : Array.isArray(value) ? value : [value];
                const isAllChipRemoved = !incomingChips.some(val => typeof val === 'object' && val !== null && val.__isAllIndicator);
                if (isAllChipRemoved) {
                    this.value = this.getSelectedDisabledValues();
                    this.tkChange.emit(this.value);
                }
                return;
            }
            if (value == null) {
                this.value = [];
            }
            else {
                const incomingChips = Array.isArray(value) ? value : [value];
                // Filter out the "others" indicator
                const validChips = incomingChips.filter(val => !(typeof val === 'object' && val !== null && val.__isOthersIndicator));
                // When visibleItemCount is active and we have reordered display, we need to handle removals carefully
                if (this.visibleItemCount && this.selectedItem && this.selectedItem.length > this.visibleItemCount) {
                    // Get the current display value (what the user sees)
                    const currentDisplayValue = this.getDisplayValueForMultiple(this.selectedItem);
                    const currentValidDisplayChips = currentDisplayValue.filter(val => !(typeof val === 'object' && val !== null && val.__isOthersIndicator));
                    // Find which chip was removed by comparing the arrays
                    const removedChip = currentValidDisplayChips.find(displayChip => !validChips.some(validChip => {
                        if (this.optionValueKey && typeof displayChip === 'object' && typeof validChip === 'object') {
                            return this.getOptionValue(displayChip) === this.getOptionValue(validChip);
                        }
                        return isEqual.isEqual(displayChip, validChip);
                    }));
                    if (removedChip) {
                        // Remove the chip from the actual value array (not the display array)
                        const currentValue = Array.isArray(this.value) ? [...this.value] : [];
                        const removedValue = this.optionValueKey && typeof removedChip === 'object' ? this.getOptionValue(removedChip) : removedChip;
                        const updatedValue = currentValue.filter(val => {
                            if (this.optionValueKey && typeof removedChip === 'object') {
                                return val !== removedValue;
                            }
                            return !isEqual.isEqual(val, removedValue);
                        });
                        this.value = updatedValue;
                        this.tkChange.emit(this.value);
                        return;
                    }
                }
                // Normal case: no visibleItemCount or no reordering needed
                const resolvedValues = validChips.map(val => {
                    if (typeof val === 'object' && val !== null && this.optionValueKey) {
                        return this.getOptionValue(val);
                    }
                    return val;
                });
                this.value = resolvedValues;
            }
            this.tkChange.emit(this.value);
        }
        else {
            if (this.editable && this.allowCustomValue) {
                this.value = value;
                this.tkChange.emit(value);
            }
            // hızlıca inputda arama yapılırsa verilen filterDebounceDelay süresi içinde
            // tekrar inputda değişiklik yapılırsa sadece bir defa filtre fonksiyonu çalıştırma için yapıldı.
            if (this.filterDebounceDelay > 0) {
                clearTimeout(this.filterDebounceTimeout);
                this.filterDebounceTimeout = setTimeout(async () => {
                    await this.setRenderOptions(value);
                }, this.filterDebounceDelay);
            }
            else {
                await this.setRenderOptions(value);
            }
            if (value === '') {
                this.value = null;
                this.tkChange.emit(null);
            }
        }
    }
    handleInputClick(e) {
        var _a;
        if (this.disabled || this.readonly)
            return;
        const path = e.composedPath();
        const isClearButton = path.some(el => { var _a; return (_a = el.classList) === null || _a === void 0 ? void 0 : _a.contains('tk-input-clear-button'); });
        const isChevron = path.some((el) => el.tagName === 'TK-ICON' && (el.icon === 'keyboard_arrow_up' || el.icon === 'keyboard_arrow_down'));
        const isChipsClearButton = path.some((el) => { var _a; return (_a = el.classList) === null || _a === void 0 ? void 0 : _a.contains('tk-chips-clear-button'); });
        const isInputElement = path.some((el) => { var _a; return (_a = el.classList) === null || _a === void 0 ? void 0 : _a.contains('tk-input'); });
        if (isClearButton || isChipsClearButton)
            return;
        if (isChevron) {
            const willOpen = !this.isOpen;
            this.isOpen = willOpen;
            if (willOpen) {
                (_a = this.nativeInputRef) === null || _a === void 0 ? void 0 : _a.focus({ preventScroll: true });
            }
            return;
        }
        if (isInputElement && !this.isOpen) {
            this.isOpen = true;
        }
    }
    async handleInputBlur() {
        // item click'den geldiğinde blur çalıştırma
        if (this.isItemClickFlag) {
            this.isItemClickFlag = false;
            return;
        }
        if (!this.editable)
            return;
        // filtreleme ardında yapılan seçimden sonra filtrelem için kullandığımız tk-input içerisindeki native inputu temizleme işlemi
        if (this.multiple) {
            this.nativeInputRef.value = null;
        }
        if (!this.multiple && !this.allowCustomValue) {
            const selectedItem = this.getSelectedItem();
            const inputValue = this.nativeInputRef.value;
            if (!inputValue)
                return;
            // custom value'ya izin verilmiyor ise inputu boşalt
            if (
            // seçili item yok ise ama inutda bir değer var ise
            (!selectedItem && inputValue) ||
                // seçili item var ise ama inputta yazar değer seçili item ile uyuşmuyor ise
                (selectedItem && this.getOptionLabel(selectedItem) != inputValue)) {
                this.value = null;
                this.inputRef.value = null;
                this.tkChange.emit(null);
            }
        }
        this.renderOptions = await this.filter(null, this.options);
    }
    async handleInputKeydown(e) {
        // Prevent default behavior for accessibility keys
        if (['ArrowDown', 'ArrowUp', 'Enter', 'Escape'].includes(e.key)) {
            e.preventDefault();
        }
        // Handle keyboard shortcuts when dropdown is closed
        if (!this.isOpen) {
            if (e.key === 'Enter' || e.key === 'ArrowDown' || e.key === 'ArrowUp') {
                // Enter, Arrow Up/Down: Open dropdown
                if (!this.disabled && !this.readonly) {
                    this.isOpen = true;
                }
                return;
            }
        }
        // Handle keyboard shortcuts when dropdown is open
        if (this.isOpen) {
            if (e.key === 'Escape') {
                // Escape: Close dropdown without selecting
                this.isOpen = false;
                return;
            }
            if (e.key === 'ArrowDown') {
                this.navigateToItem('down');
                return;
            }
            if (e.key === 'ArrowUp') {
                this.navigateToItem('up');
                return;
            }
            if (e.key === 'Enter') {
                const activeItem = this.el.querySelector('.dropdown-item[data-active="true"]');
                if (this.multiple && this.editable && this.allowCustomValue) {
                    this.nativeInputRef.dispatchEvent(new InputEvent('input', { bubbles: true }));
                }
                if (activeItem) {
                    activeItem.click();
                }
                return;
            }
        }
        // Handle Tab key
        if (e.key === 'Tab') {
            this.isOpen = false;
        }
    }
    handleInputClearClick() {
        if (this.multiple && this.optionDisabled && Array.isArray(this.value)) {
            this.value = this.getSelectedDisabledValues();
        }
        else {
            this.value = null;
        }
        this.tkChange.emit(this.value);
        this.selectAll && this.multiple && this.isAllSelected() && this.tkSelectAll.emit(false);
    }
    createOptionItem(options, startIndex = 0) {
        return options === null || options === void 0 ? void 0 : options.map((item, index$2) => {
            var _a;
            const isDisabled = this.optionDisabled ? (_a = this.optionDisabled) === null || _a === void 0 ? void 0 : _a.call(this, item) : false;
            let itemProps = {};
            let children;
            const checking = objectUtils.some(this.value, itemValue => isEqual.isEqual(itemValue, this.getOptionValue(item)));
            if (this.multiple) {
                if (this.optionHtml != undefined) {
                    children = (index.h(index.Fragment, null, index.h("tk-checkbox", { value: checking, disabled: isDisabled, "onTk-change": e => e.stopPropagation(), onClick: e => e.preventDefault() }), index.h("div", { class: "multiple-option-content", innerHTML: this.optionHtml(item) })));
                }
                else {
                    children = (index.h(index.Fragment, null, index.h("tk-checkbox", { value: checking, disabled: isDisabled, "onTk-change": e => e.stopPropagation(), onClick: e => e.preventDefault() }), index.h("div", null, this.getOptionLabel(item))));
                }
            }
            else {
                if (this.optionHtml != undefined) {
                    itemProps = { innerHTML: this.optionHtml(item) };
                }
                else {
                    itemProps = { innerHTML: this.getOptionLabel(item) };
                }
            }
            return (index.h("div", Object.assign({ class: index$1.classNames('dropdown-item', { multiple: this.multiple }, { disabled: isDisabled }), "data-option-index": startIndex + index$2, "data-selected": this.multiple && checking ? 'true' : this.value === this.getOptionValue(item) ? 'true' : 'false', onPointerDown: () => (this.isItemClickFlag = true), onClick: () => this.handleItemClick(item) }, itemProps), children));
        });
    }
    createSelectAllOption() {
        var _a;
        if (this.selectAll && this.multiple) {
            const isIndeterminate = !this.isAllSelected() && ((_a = this.value) === null || _a === void 0 ? void 0 : _a.length) > 0;
            const checking = this.isAllSelected();
            return (index.h("div", null, index.h("div", { class: index$1.classNames('dropdown-item', { multiple: this.multiple }), "data-selected": this.multiple && checking ? 'true' : 'false', onPointerDown: () => (this.isItemClickFlag = true), onClick: () => this.handleSelectAllClick(), "data-option-index": "-1" }, index.h("tk-checkbox", { indeterminate: isIndeterminate, value: isIndeterminate ? null : checking, "onTk-change": e => e.stopPropagation(), onClick: e => e.preventDefault() }), index.h("div", null, this.selectAllLabel)), index.h("tk-divider", { my: 1, style: { margin: '6px 4px 0px' } })));
        }
    }
    createOptions() {
        if (this.isGrouped()) {
            let currentIndex = 0;
            return this.renderOptions.map(group => {
                var _a;
                const groupItems = this.createOptionItem(group[this.groupOptionsKey], currentIndex);
                currentIndex += ((_a = group[this.groupOptionsKey]) === null || _a === void 0 ? void 0 : _a.length) || 0;
                return (index.h("div", { class: "dropdown-group" }, index.h("div", { class: "dropdown-group-label" }, index.h("label", null, group[this.groupNameKey]), index.h("div", { class: "line" })), groupItems));
            });
        }
        else {
            return this.createOptionItem(this.renderOptions);
        }
    }
    renderInput() {
        var _a;
        return (index.h("tk-input", { ref: el => (this.inputRef = el), class: index$1.classNames('tk-select-input', {
                'editable-select': this.editable,
                'readonly-select': this.readonly,
                'tk-table-input': this.el.classList.contains('tk-table-select'),
                'multiple-select': this.multiple,
                'allow-custom-value-select': this.allowCustomValue,
            }), label: this.label, size: this.size, showAsterisk: this.showAsterisk, hint: this.hint, placeholder: ((_a = this.value) === null || _a === void 0 ? void 0 : _a.length) > 0 ? '' : this.placeholder, invalid: this.invalid, error: this.error, icon: { left: this.icon, right: this.isOpen ? 'keyboard_arrow_up' : 'keyboard_arrow_down' }, mode: this.multiple ? 'chips' : 'text', chipLabelKey: this.optionLabelKey, readonly: this.readonly, disabled: this.disabled, loading: this.loading, clearable: this.clearable, chipOptions: this.chipOptions, chipDisabled: this.optionDisabled, "aria-describedby": "dropdown", "aria-expanded": !!this.isOpen, onClick: e => this.handleInputClick(e), "onTk-change": e => {
                e.stopPropagation();
                this.handleInputChange(e.detail);
            }, "onTk-blur": () => this.handleInputBlur(), "onTk-clear-click": () => this.handleInputClearClick(), onKeyDown: e => this.handleInputKeydown(e) }));
    }
    renderDropdown() {
        var _a;
        if (!this.isOpen)
            return null;
        return (index.h("div", { class: "tk-select-panel", ref: el => (this.panelRef = el), "data-tk-select-id": this.uniqueId }, index.h("div", { class: "dropdown-item-holder" }, this.loading ? (index.h("tk-spinner", { size: this.size })) : (index.h(index.Fragment, null, this.panelTopHtml && (index.h("div", { class: "dropdown-item-top", ref: el => {
                if (el) {
                    const htmlContent = this.panelTopHtml();
                    if (htmlContent instanceof HTMLElement) {
                        el.innerHTML = '';
                        el.appendChild(htmlContent);
                    }
                    else {
                        el.innerHTML = htmlContent;
                    }
                }
            } })), ((_a = this.renderOptions) === null || _a === void 0 ? void 0 : _a.length) > 0 ? (index.h(index.Fragment, null, this.createSelectAllOption(), this.createOptions())) : this.hasEmptyDataSlot ? (index.h("slot", { name: "empty-data" })) : (this.emptyMessage))))));
    }
    render() {
        const rootClasses = index$1.classNames('tk-select-container', this.size);
        return (index.h("div", { key: 'f97a2bf57681ad4b267b1feb27e33106d86cda60', "aria-readonly": this.readonly, "aria-disabled": this.disabled, "aria-invalid": this.invalid, class: rootClasses }, this.renderInput(), this.renderDropdown()));
    }
    static get formAssociated() { return true; }
    get el() { return index.getElement(this); }
    static get watchers() { return {
        "isOpen": [{
                "isOpenChanged": 0
            }],
        "disabled": [{
                "disabledChanged": 0
            }],
        "options": [{
                "optionsChanged": 0
            }],
        "value": [{
                "valueChanged": 0
            }]
    }; }
};
TkSelect.style = tkSelectCss();

const tkSpinnerCss = () => `@font-face{font-family:"Material Symbols Outlined";src:url("./assets/fonts/material-symbols/Material-Symbols-Outlined.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-outlined{font-family:"Material Symbols Outlined";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-outlined{font-variation-settings:"FILL" 0}.material-symbols-outlined.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Rounded";src:url("./assets/fonts/material-symbols/Material-Symbols-Rounded.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-rounded{font-family:"Material Symbols Rounded";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-rounded{font-variation-settings:"FILL" 0}.material-symbols-rounded.fill{font-variation-settings:"FILL" 1}@font-face{font-family:"Material Symbols Sharp";src:url("./assets/fonts/material-symbols/Material-Symbols-Sharp.woff2") format("woff2");font-weight:400;font-style:normal;font-display:swap}.material-symbols-sharp{font-family:"Material Symbols Sharp";font-weight:normal;font-style:normal;font-size:24px;line-height:1;letter-spacing:normal;text-transform:none;display:inline-block;white-space:nowrap;word-wrap:normal;direction:ltr;-webkit-font-feature-settings:"liga";-webkit-font-smoothing:antialiased}.material-symbols-sharp{font-variation-settings:"FILL" 0}.material-symbols-sharp.fill{font-variation-settings:"FILL" 1}.tk-spin-container{--spinner-size:32px;--spinner-stroke-width:5.33px;--spinner-dot-size:6px;--spinner-corner-offset:calc(var(--spinner-size) / 10);--spinner-track-color:var(--background-light);--spinner-active-color:var(--background-darkest);--spinner-label-color:var(--text-sub-base);display:flex;align-items:center;justify-content:center}.tk-spin-container .spinner-rounded,.tk-spin-container .spinner-dots,.tk-spin-container .spinner-lines,.tk-spin-container .spinner-pulse,.tk-spin-container .spinner-three-dots,.tk-spin-container .spinner-loader,.tk-spin-container .spinner-logo{display:flex;width:var(--spinner-size);height:var(--spinner-size)}.tk-spin-container .dot,.tk-spin-container .dot1,.tk-spin-container .dot2,.tk-spin-container .dot3{width:var(--spinner-dot-size);height:var(--spinner-dot-size)}.tk-spin-container .line{width:calc(var(--spinner-stroke-width) / 2);height:calc(var(--spinner-size) / 4)}.tk-spin-container.vertical{flex-direction:column}.tk-spin-container.horizontal{flex-direction:row}.tk-spin-container.xxsmall{--spinner-size:16px;--spinner-stroke-width:2.67px;--spinner-dot-size:3px}.tk-spin-container.xsmall{--spinner-size:20px;--spinner-stroke-width:3.33px;--spinner-dot-size:4px}.tk-spin-container.small{--spinner-size:24px;--spinner-stroke-width:4px;--spinner-dot-size:5px}.tk-spin-container.base{--spinner-size:32px;--spinner-stroke-width:5.33px;--spinner-dot-size:6px}.tk-spin-container.large{--spinner-size:40px;--spinner-stroke-width:6.67px;--spinner-dot-size:7px}.tk-spin-container.xlarge{--spinner-size:48px;--spinner-stroke-width:8px;--spinner-dot-size:8px}.tk-spin-container.primary{--spinner-track-color:var(--primary-light);--spinner-active-color:var(--primary-dark);--spinner-label-color:var(--primary-base)}.tk-spin-container.neutral{--spinner-track-color:var(--background-light);--spinner-active-color:var(--background-darkest);--spinner-label-color:var(--text-sub-base)}.tk-spin-container.info{--spinner-track-color:var(--states-info-light);--spinner-active-color:var(--states-info-dark);--spinner-label-color:var(--states-info-base)}.tk-spin-container.success{--spinner-track-color:var(--states-success-light);--spinner-active-color:var(--states-success-dark);--spinner-label-color:var(--states-success-base)}.tk-spin-container.warning{--spinner-track-color:var(--states-warning-light);--spinner-active-color:var(--states-warning-dark);--spinner-label-color:var(--states-warning-base)}.tk-spin-container.danger{--spinner-track-color:var(--states-danger-light);--spinner-active-color:var(--states-danger-dark);--spinner-label-color:var(--states-danger-base)}.tk-spinner-label{font-size:var(--desktop-body-s-size);color:var(--spinner-label-color);font-family:var(--family-body)}.vertical .tk-spinner-label{margin-top:4px;margin-left:0}.horizontal .tk-spinner-label{margin-top:0;margin-left:4px}.spinner-rounded{border:var(--spinner-stroke-width) solid var(--spinner-track-color);border-top:var(--spinner-stroke-width) solid var(--spinner-active-color);border-radius:50%;animation:rounded-animation 1s linear infinite;box-sizing:border-box}.spinner-dots{position:relative;align-items:center;justify-content:center}.spinner-dots .dot{position:absolute;background-color:var(--spinner-active-color);border-radius:50%;animation:dots-animation 1.2s linear infinite}.spinner-dots .dot:nth-child(1){top:-12%;left:50%;transform:translateX(-50%);animation-delay:-1.1s}.spinner-dots .dot:nth-child(2){top:14%;left:85%;transform:translate(-50%, -50%);animation-delay:-1s}.spinner-dots .dot:nth-child(3){top:50%;left:100%;transform:translate(-50%, -50%);animation-delay:-0.9s}.spinner-dots .dot:nth-child(4){top:85%;left:85%;transform:translate(-50%, -50%);animation-delay:-0.8s}.spinner-dots .dot:nth-child(5){top:100%;left:50%;transform:translate(-50%, -50%);animation-delay:-0.7s}.spinner-dots .dot:nth-child(6){top:85%;left:14%;transform:translate(-50%, -50%);animation-delay:-0.6s}.spinner-dots .dot:nth-child(7){top:50%;left:0;transform:translate(-50%, -50%);animation-delay:-0.5s}.spinner-dots .dot:nth-child(8){top:14%;left:14%;transform:translate(-50%, -50%);animation-delay:-0.4s}.spinner-lines{position:relative;align-items:center;justify-content:center}.spinner-lines .line{position:absolute;background-color:var(--spinner-active-color);border-radius:2px;animation:lines-animation 1.2s linear infinite}.spinner-lines .line:nth-child(1){top:-15%;left:50%;transform:translateX(-50%);animation-delay:-1.1s}.spinner-lines .line:nth-child(2){top:14%;left:85%;transform:translate(-50%, -50%) rotate(45deg);animation-delay:-1s}.spinner-lines .line:nth-child(3){top:50%;left:100%;transform:translate(-50%, -50%) rotate(90deg);animation-delay:-0.9s}.spinner-lines .line:nth-child(4){top:85%;left:85%;transform:translate(-50%, -50%) rotate(135deg);animation-delay:-0.8s}.spinner-lines .line:nth-child(5){top:100%;left:50%;transform:translate(-50%, -50%) rotate(180deg);animation-delay:-0.7s}.spinner-lines .line:nth-child(6){top:85%;left:14%;transform:translate(-50%, -50%) rotate(225deg);animation-delay:-0.6s}.spinner-lines .line:nth-child(7){top:50%;left:0;transform:translate(-50%, -50%) rotate(270deg);animation-delay:-0.5s}.spinner-lines .line:nth-child(8){top:14%;left:14%;transform:translate(-50%, -50%) rotate(315deg);animation-delay:-0.4s}.spinner-pulse{border-radius:50%;background-color:var(--spinner-active-color);animation:pulse-animation 1.5s infinite}.spinner-three-dots{display:flex;align-items:center;justify-content:center}.spinner-three-dots .dot1,.spinner-three-dots .dot2,.spinner-three-dots .dot3{background-color:var(--spinner-active-color);border-radius:50%;margin:1px;animation:three-dots-animation 1.2s infinite ease-in-out}.spinner-three-dots .dot1{animation-delay:-0.45s}.spinner-three-dots .dot2{animation-delay:-0.3s}.spinner-three-dots .dot3{animation-delay:-0.15s}.spinner-loader{border:var(--spinner-stroke-width) solid transparent;border-top:var(--spinner-stroke-width) solid var(--spinner-active-color);border-radius:50%;animation:loader-animation 1s linear infinite}.spinner-logo{animation:logo-animation 1s steps(4) infinite}.spinner-logo path{fill:var(--spinner-active-color)}@keyframes rounded-animation{0%{transform:rotate(0deg)}100%{transform:rotate(360deg)}}@keyframes dots-animation{0%,20%{opacity:1}80%,100%{opacity:0}}@keyframes lines-animation{0%,20%{opacity:1}80%,100%{opacity:0}}@keyframes pulse-animation{0%{transform:scale(calc(var(--spinner-stroke-width) / var(--spinner-size)));opacity:1}100%{transform:scale(1);opacity:0}}@keyframes three-dots-animation{0%,80%,100%{transform:scale(0)}40%{transform:scale(1)}}@keyframes loader-animation{0%{transform:rotate(0deg)}100%{transform:rotate(360deg)}}@keyframes logo-animation{0%{transform:rotate(0deg)}100%{transform:rotate(360deg)}}`;

const TkSpinner = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.isButton = false;
        /**
         * Controls the orientation of the spinner component.
         * @defaultValue 'horizontal'
         */
        this.orientation = 'vertical';
        /**
         * Controls the size of the spinner component.
         * @defaultValue 'base'
         */
        this.size = 'base';
        /**
         * Sets the style of spinner component.
         * @defaultValue 'rounded'
         */
        this.type = 'rounded';
        /**
         * Sets the color variant of spinner component.
         * @defaultValue 'neutral'
         */
        this.variant = 'neutral';
    }
    componentWillLoad() {
        var _a;
        this.isButton = (_a = this.el.closest('button')) === null || _a === void 0 ? void 0 : _a.classList.contains('tk-button');
    }
    renderSpinner() {
        switch (this.type) {
            case 'rounded':
                if (this.isButton) {
                    const button = this.el.closest('button');
                    const borderColor = window.getComputedStyle(button).color;
                    return index.h("div", { style: { borderColor: borderColor, borderTopColor: 'transparent' }, class: "spinner-rounded", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'rounded') });
                }
                return index.h("div", { class: "spinner-rounded", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'rounded') });
            case 'dots':
                return (index.h("div", { class: "spinner-dots", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'dots') }, index.h("div", { class: "dot", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'dot-0') }), index.h("div", { class: "dot", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'dot-1') }), index.h("div", { class: "dot", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'dot-2') }), index.h("div", { class: "dot", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'dot-3') }), index.h("div", { class: "dot", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'dot-4') }), index.h("div", { class: "dot", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'dot-5') }), index.h("div", { class: "dot", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'dot-6') }), index.h("div", { class: "dot", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'dot-7') })));
            case 'lines':
                return (index.h("div", { class: "spinner-lines", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'lines') }, index.h("div", { class: "line", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'line-0') }), index.h("div", { class: "line", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'line-1') }), index.h("div", { class: "line", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'line-2') }), index.h("div", { class: "line", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'line-3') }), index.h("div", { class: "line", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'line-4') }), index.h("div", { class: "line", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'line-5') }), index.h("div", { class: "line", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'line-6') }), index.h("div", { class: "line", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'line-7') })));
            case 'three-dots':
                return (index.h("div", { class: "spinner-three-dots", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'three-dots') }, index.h("span", { class: "dot1", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'dot-0') }), index.h("span", { class: "dot2", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'dot-1') }), index.h("span", { class: "dot3", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'dot-2') })));
            case 'pulse':
                return index.h("div", { class: "spinner-pulse", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'pulse') });
            case 'loader':
                return index.h("div", { class: "spinner-loader", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'loader') });
            case 'logo':
                return (index.h("div", { class: "spinner-logo", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'logo') }, index.h("svg", { viewBox: "0 0 48 48", fill: "none", xmlns: "http://www.w3.org/2000/svg", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'logo-icon') }, index.h("path", { "fill-rule": "evenodd", "clip-rule": "evenodd", d: "M24 0C10.7452 0 0 10.7452 0 24C0 37.2548 10.7452 48 24 48C37.2548 48 48 37.2548 48 24V4C48 1.79086 46.2091 0 44 0H24ZM24 8C15.1634 8 8 15.1634 8 24C8 32.8366 15.1634 40 24 40C32.8366 40 40 32.8366 40 24C40 15.1634 32.8366 8 24 8Z" }))));
            default:
                return null;
        }
    }
    render() {
        return (index.h("div", { key: 'feaad56cf7a2a12da8ba262b706fb979692ef6de', role: "progressbar", class: index$1.classNames('tk-spin-container', {
                [this.orientation]: true,
                [this.size]: true,
                [this.variant]: true,
            }), "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'container') }, this.renderSpinner(), this.label && (index.h("div", { key: 'c60900da360c9ac5085a0260b0139478c3ae76fa', class: "tk-spinner-label", "data-testid": testIdUtils.getDataTestId(this.dataTestid, 'label') }, this.label))));
    }
    get el() { return index.getElement(this); }
};
TkSpinner.style = tkSpinnerCss();

exports.tk_button = TkButton;
exports.tk_checkbox = TkCheckbox;
exports.tk_chips = TkChips;
exports.tk_color_picker = TkColorPicker;
exports.tk_divider = TkDivider;
exports.tk_icon = TkIcon;
exports.tk_input = TkInput;
exports.tk_select = TkSelect;
exports.tk_spinner = TkSpinner;
