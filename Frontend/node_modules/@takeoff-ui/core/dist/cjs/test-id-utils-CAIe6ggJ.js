'use strict';

const getDataTestId = (testid, ...suffixes) => {
    if (!testid)
        return undefined;
    const normalizedSuffix = suffixes.filter(Boolean).join('-');
    return normalizedSuffix ? `${testid}-${normalizedSuffix}` : testid;
};

exports.getDataTestId = getDataTestId;
