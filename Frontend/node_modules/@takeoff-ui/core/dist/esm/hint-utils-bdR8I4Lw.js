import { h } from './index-BKCDur6H.js';
import { g as getIconElementProps } from './icon-utils-Bf8c2EML.js';
import { c as classNames } from './index-D7YaJBkB.js';
import { g as getDataTestId } from './test-id-utils-BDMnqEGL.js';

/**
 * Utility to render hint or error message with icon.
 * @param params - ( hint?: string, error?: string, invalid?: boolean, dataTestid?: string, componentTag?: string )
 * @returns JSX.Element| null
 */
function renderHint(hint, error, invalid, dataTestid) {
    const isError = error && error.length > 0;
    const isHint = hint && hint.length > 0;
    if (!isError && !isHint) {
        return null;
    }
    const icon = isError || invalid ? 'error' : 'info';
    const wrapperTestId = getDataTestId(dataTestid, isError ? 'error' : 'hint');
    const iconTestId = getDataTestId(dataTestid, isError ? 'error-icon' : 'hint-icon');
    const textTestId = getDataTestId(dataTestid, isError ? 'error-text' : 'hint-text');
    return (h("div", { class: classNames('tk-hint-wrapper', { invalid: invalid, error: isError }), "data-testid": wrapperTestId }, h("tk-icon", Object.assign({ dataTestid: iconTestId }, getIconElementProps(icon, { color: isError || invalid ? 'var(--states-danger-base)' : 'var(--icon-base)', size: 'small' }))), h("span", { "data-testid": textTestId }, isError ? error : hint)));
}

export { renderHint as r };
